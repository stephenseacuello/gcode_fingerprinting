═══════════════════════════════════════════════════════════════
  W&B HYPERPARAMETER SWEEP - QUICK REFERENCE
═══════════════════════════════════════════════════════════════

📋 SETUP (One-time)
────────────────────────────────────────────────────────────────
# Login to W&B
.venv/bin/wandb login
# → Paste API key from https://wandb.ai/authorize

# Verify login
.venv/bin/wandb status
# → Should show "api_key": "<your-key>"


🚀 CREATE & RUN SWEEP
────────────────────────────────────────────────────────────────
# 1. Create sweep (returns SWEEP_ID)
.venv/bin/wandb sweep configs/sweep_config.yaml

# 2. Run agent (replace YOUR_USERNAME and SWEEP_ID)
PYTORCH_ENABLE_MPS_FALLBACK=1 .venv/bin/wandb agent YOUR_USERNAME/gcode-fingerprinting/SWEEP_ID

# Example:
PYTORCH_ENABLE_MPS_FALLBACK=1 .venv/bin/wandb agent stepheneacuello/gcode-fingerprinting/abc123xyz

# 3. Stop anytime with Ctrl+C


📊 ANALYZE RESULTS & GENERATE FIGURES
────────────────────────────────────────────────────────────────
.venv/bin/python scripts/analyze_sweep.py \
    --sweep-id SWEEP_ID \
    --entity YOUR_USERNAME \
    --output outputs/sweep_analysis/

# Generates:
#   - sweep_results.csv
#   - parameter_importance.png
#   - parallel_coordinates.png
#   - metric_distributions.png
#   - best_vs_worst.png
#   - summary_report.txt


📈 MONITOR SWEEP
────────────────────────────────────────────────────────────────
Open in browser:
https://wandb.ai/YOUR_USERNAME/gcode-fingerprinting/sweeps/SWEEP_ID


🔧 PARALLEL AGENTS (Faster Results)
────────────────────────────────────────────────────────────────
# Terminal 1:
PYTORCH_ENABLE_MPS_FALLBACK=1 .venv/bin/wandb agent YOUR_USERNAME/gcode-fingerprinting/SWEEP_ID

# Terminal 2:
PYTORCH_ENABLE_MPS_FALLBACK=1 .venv/bin/wandb agent YOUR_USERNAME/gcode-fingerprinting/SWEEP_ID

# Terminal 3:
PYTORCH_ENABLE_MPS_FALLBACK=1 .venv/bin/wandb agent YOUR_USERNAME/gcode-fingerprinting/SWEEP_ID


🎯 COMPLETE EXAMPLE
────────────────────────────────────────────────────────────────
# Login (one time)
.venv/bin/wandb login

# Create sweep
.venv/bin/wandb sweep configs/sweep_config.yaml
# → Output: sweep ID = abc123xyz

# Run agent
PYTORCH_ENABLE_MPS_FALLBACK=1 .venv/bin/wandb agent stepheneacuello/gcode-fingerprinting/abc123xyz

# After ~20 runs, press Ctrl+C

# Analyze & generate figures
.venv/bin/python scripts/analyze_sweep.py \
    --sweep-id abc123xyz \
    --entity stepheneacuello \
    --output outputs/sweep_analysis/


💡 TIPS
────────────────────────────────────────────────────────────────
• Run 20+ runs before analyzing for meaningful results
• Mac M1/M2: Use 1-2 agents
• NVIDIA GPU: Use 2-4 agents
• Check dashboard: https://wandb.ai/YOUR_USERNAME/gcode-fingerprinting
• Best checkpoint saved in: outputs/training_50epoch/checkpoint_best.pt


📚 DOCUMENTATION
────────────────────────────────────────────────────────────────
• Detailed Guide: SWEEP_QUICKSTART.md
• Full Documentation: docs/SWEEPS_GUIDE.md
• Training Guide: docs/TRAINING.md

═══════════════════════════════════════════════════════════════
