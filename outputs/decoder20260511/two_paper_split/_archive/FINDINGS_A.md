# PAPER A counts={'total': 125, 'substantive': 17, 'moderate': 47, 'minor': 61} deduped=125

============================================================
## SUBSTANTIVE (17)
============================================================

[A-sub-1] (A-ml) Sec 4.5.3 line 359; audit/hp_sweep_all_stages_summary.md :: claim/rigor
ISSUE: Says p_max=0.5 selected on fold-1 VALIDATION token accuracy and validation-best; artifact val_token winners are pointer_x_cross_window (0.7449) and n_layers_10 (0.7447); SS=0.5 (0.7441) wins only on a composite dominated by TEST command 0.4993/numeric/token. Grid {0,0.25,0.5,0.75} mismatches {0.1,0.2,0.5,1.0}; 34 cells is 30 (Stage3=10).
FIX: State composite-score winner with held-out test metrics (model-selection-on-test caveat); list real grid; correct counts.

[A-sub-2] (A-critic) decoder_paper_v2.tex Abstract (L75), Headline findings (L106), Conclusion (L909); confound section (L733) :: claim/rigor
ISSUE: The single most load-bearing quantity in the paper -- the +22 pp within-class command increment -- is, by the authors' own honest admission, a 4-of-5-fold effect that is zero (slightly negative, -0.016) on fold 1, with a paired-bootstrap 95% CI of approximately [+0.0, +0.30] whose lower edge touches zero. The paper states this would 'not survive removal of more than one fold.' Yet the abstract elevates it to THE 'load-bearing result' without any quantitative fold-sensitivity analysis. A completeness gap the lens panel missed: there is no reported leave-one-fold-out / jackknife sensitivity on the increment, no report of the increment computed on folds 2-5 only (which would be ~+0.28, the honest 'when it works' number), and no statement of how the 95% CI lower bound behaves under the jackknife. Because the entire framing rests on this number being a real upper bound rather than noise, the reader needs the jackknife to judge robustness.
FIX: Add a one-paragraph (or supplementary-table) fold-sensitivity analysis of the +22 pp increment: report the jackknife distribution of the mean increment under each single-fold deletion (which will show the increment ranges from ~0 with fold-1 retained to ~+0.28 with fold-1 deleted), state the folds-2-5 increment explicitly, and reconcile this with the abstract's framing. Consider softening the abstract to 'a fold-dependent within-class increment (mean +22 pp, driven by 4 of 5 folds)' so the headline matches the body's own caveat.

[A-sub-3] (A-write) decoder_paper_v2.tex line 534 (Numeric Digit Head Decomposition, 2492 chars single sentence-chain) :: clarity
ISSUE: This paragraph contains a single ~2500-character sentence enumerating all six digit slots with their accuracies, then the slot-5 tokenizer-quantization explanation, then the 'coarse-resolution coordinate recoverer' summary, then the patched-correction note, then the entropy-anticorrelation Pearson values — all chained with semicolons, em-dashes, and parentheticals. It is the densest passage in the paper and the load-bearing 'bathtub' insight is hard to extract.
FIX: Break into three sentences/units: (1) the per-slot numbers (which the figure already shows — consider just citing Figure 9 and giving slot-0/slot-4/slot-5 endpoints rather than all six values inline), (2) the slot-5 upturn mechanism (tokenizer $0.001$ grid → structural zero), (3) the interpretive summary ('coarse-resolution coordinate recoverer'). Move the Pearson correlation values ($-0.89$, $-0.83$) to a parenthetical at the end or to the companion-paper pointer, since they belong to Paper B's account.

[A-sub-4] (A-stats) decoder_paper_v2.tex line 733 (Sec. 7.1.2, class-prior confound); verified against audit/confound_class_prior.json :: statistics
ISSUE: The +0.222 within-class increment's uncertainty is stated as 'A paired-bootstrap 95% CI on the per-fold delta is approximately [+0.0, +0.30] (its lower edge brushes zero precisely because of the fold-1 sign flip).' A standard percentile bootstrap over the five per-fold deltas [-0.0164, 0.280, 0.2737, 0.2955, 0.2784] gives a 95% CI of approximately [+0.10, +0.29]; the lower edge is ~+0.10, not ~0. To reach zero a resample would have to be dominated by the single -0.016 fold (probability <0.03% of an all-fold-1 draw), so the percentile bootstrap lower edge cannot 'brush zero'. The stated interval and its rationale are inconsistent with the cited per-fold-delta resampling.
FIX: Either (a) report the correct percentile-bootstrap-of-fold-means CI [+0.10, +0.29] and replace the 'brushes zero' clause with the honest leave-one-fold-out statement (dropping fold-1 raises the mean to ~+0.28; dropping any non-outlier fold leaves it ~+0.22, i.e., the lower edge is a leave-one-fold sensitivity, not a bootstrap edge); or (b) if a different resampling unit (e.g., per-window paired bootstrap) was intended, name it explicitly and give the actual computed bounds. As written the interval mis-states the bound as weaker than the cited computation.

[A-sub-5] (A-write) decoder_paper_v2.tex line 735 (sec:disc-confound, 2725 chars — longest paragraph in paper) :: structure
ISSUE: The class-prior confound discussion is the single most important analytical passage (it establishes the +22 pp upper bound), but it is delivered as one 2725-character paragraph that simultaneously: reports the three baselines, flags the 4-of-5-fold fragility, explains the file-level-CV null logic, reconciles 0.795 vs 0.8875, gives the paired-bootstrap CI, AND pre-empts the per-file-modal reviewer objection. The reader loses the thread of which claim is being defended.
FIX: Split into: (a) the headline result (decoder 0.795 vs class-prior 0.572, +22 pp); (b) the fold-1 fragility caveat as its own short paragraph; (c) the 'why operation-class modal is the strictest legitimate null' argument; (d) the class-conditional-modal robustness check. The 0.795-vs-0.8875 reconciliation aside (which also appears at line 733) can be a footnote. This is verified-correct content (decoder 0.795±0.121, prior 0.572±0.004, deltas {-0.016,+0.28,+0.27,+0.30,+0.28}) — the problem is purely packaging.

[A-sub-6] (A-write) decoder_paper_v2.tex line 75 (Abstract) :: clarity
ISSUE: The structured abstract is a single 190-word block (at the MDPI ~200-word ceiling) packed with 7+ bolded fragments, 4 em-dash asides, and dense notation ($+22$ pp, $0.213$, $0.78\!\to\!0.21$, $0.59\!\to\!0.10$, $0.026$, $77.7\%$). It reads as a results dump rather than a narrative. A reader cannot extract the one-sentence takeaway without parsing every clause. The bolding is so pervasive (six bold spans) that nothing stands out.
FIX: Restructure into a clear arc of 4-5 sentences: (1) the threat (hash-invisible forgery), (2) what was built and on what platform/TRL, (3) the single load-bearing finding (categorical fields recoverable; +22 pp within-class command increment as an upper bound), (4) what is NOT recoverable (generative reconstruction at floor; coordinates unsupported), (5) the scope/posture caveat (forensic-support audit, single-platform, closed-world). Reduce bolding to at most the two genuinely load-bearing quantities. Move the $0.78\to0.21$ / $0.59\to0.10$ token/digit collapse detail to a single phrase rather than two separate number pairs.

[A-sub-7] (A-write) decoder_paper_v2.tex line 88 (Introduction, 2nd paragraph, 2058 chars) :: structure
ISSUE: This is a single 2058-character paragraph that front-loads the entire scope/threat-model/deployment-posture/out-of-scope enumeration before the reader has seen the task or any result. It chains platform scope, audit posture, 'not a real-time IDS', closed-world binding, LOCO collapse forward-reference, and the cost-envelope framing into one breathless block. The point (what the paper does) is buried under caveats stated pre-emptively.
FIX: Split into two paragraphs: one stating the platform and the intended forensic-support audit application positively, and a separate short paragraph enumerating what is explicitly out of scope (real-time alerting, numeric/feed substitution, novel-class). Defer the LOCO-collapse and closed-world-binding forward references to where they are first needed; the Introduction need only flag them, not fully scope them. This material is re-stated almost verbatim in the Scope statement (line 123) and the Limitations (lines 834-836).

[A-sub-8] (A-write) decoder_paper_v2.tex lines 591-604 ('Decomposing the collapse mechanism' paragraph) vs lines 611-616 (sec:abl-nonumeric) vs lines 760-763 (sec:disc-mode-collapse) :: structure
ISSUE: The no-numeric-retraining collapse decomposition (+8.8 pp AR struct token, paired t=4.02, p<0.02, sequence exact-match unchanged) is stated three times: once as a paragraph inside the positional-metadata ablation (591-604), again in full in sec:abl-nonumeric (611-616), and the mode-collapse mechanism is set up again in sec:disc-mode-collapse (760-763). The reader encounters the same '$5/5$ folds positive, paired $t=4.02$' twice within ~25 lines.
FIX: Delete the 'Decomposing the collapse mechanism' paragraph at 591-604 from the positional-metadata subsection (it is a forward-pointer that duplicates the next subsection). Replace with a one-line pointer: 'The complementary decomposition of what the collapse is made of is in Section X.' Keep the full statement only in sec:abl-nonumeric.

[A-sub-9] (A-stats) decoder_paper_v2.tex lines 599 and 616 (Sec. 6.6 No-Numeric Retraining); tables/nonumeric.tex; verified against audit/design_b_compare_ss0.json and no_numeric_ablation_designA.json :: statistics
ISSUE: The no-numeric AR structural-token improvement is reported (twice, identically) as '+8.8 pp (paired across the five folds, 5/5 positive, paired t=4.02, p<0.02)'. Recomputing the paired t-test on the cited artifact (current_AR vs ss0_AR struct_token_acc, per-fold deltas [0.0748, 0.1195, 0.1674, 0.0447, 0.0342]) gives mean +0.0881 (matches), 5/5 positive (matches), but paired t = 3.56 and p = 0.0236 — NOT t=4.02 and NOT p<0.02 (0.0236 exceeds 0.02). Wilcoxon p=0.0625. The reported t-statistic and p-threshold do not reproduce from the artifact; p<0.02 is false.
FIX: Change both occurrences to 'paired t=3.56, p=0.024 (Wilcoxon p=0.063)'. Since at n=5 the paper already treats p-values as descriptive and rests on effect size + 5/5 sign consistency, soften 'p<0.02' to 'p<0.05 (paired t), Wilcoxon p=0.06' or simply report the +8.8 pp effect with 5/5 directional consistency and drop the threshold claim. Confirm which scoring produced t=4.02 (it is not the cited design_b_compare_ss0.json).

[A-sub-10] (A-write) decoder_paper_v2.tex lines 809, 811, 813, 815 (Threat Model Detectability, four consecutive dense paragraphs) :: structure
ISSUE: The tamper operating-point discussion repeats the same three caveats — measured autocorrelation rho=0.42, ~8 pp sensor-conditioned increment over modal-substitution, open-vocabulary LOCO FPR 0.52 — in line 811 ('none of which closes the gap once...'), again in line 813 ('once the measured autocorrelation...are all accounted for'), and a third time as the enumerated (1)(2)(3) in line 815. This is the same triplet stated three times in four paragraphs.
FIX: State the rho=0.42 / 8 pp / LOCO-0.52 triplet once, in the enumerated form at line 815 (which is the clearest version), and replace the prose recapitulations at 811 and 813 with a pointer ('the three deployment-relevant security numbers below'). This subsection is currently the most repetitive in the Discussion.

[A-sub-11] (A-write) decoder_paper_v2.tex lines 88, 123, 834, 836 (Intro 2nd para, Scope statement, two Limitations paragraphs) :: consistency
ISSUE: The single-desktop-platform / production-VMC contrast (vibration 1-2 orders of magnitude, spectral content, servo-vs-stepper, flood coolant, cross-platform-is-future-work) is stated in near-identical wording at least four times: Intro para 2 (88), Scope statement (123), Dataset 'Platform characterization' (424), and two consecutive Limitations paragraphs (834 'Material variety' and 836 'Single desktop CNC platform'). The latter two are nearly duplicate paragraphs in the same subsection.
FIX: Merge the two consecutive Limitations paragraphs (834 'Material variety' / cross-platform and 836 'Single desktop CNC platform') into one — they make the same point with the same VMC enumeration. State the full VMC contrast once (in Dataset, line 424), and elsewhere reduce to a one-clause cross-reference ('the single-platform scope of Section X'). This recovers roughly half a page and stops the reader feeling lectured.

[A-sub-12] (A-critic) decoder_paper_v2.tex tamper subsection (L780-786); lim-eval Single-run (L862) :: methodology
ISSUE: The entire tamper-detection FPR is, by explicit admission, 'mechanically the recoverability complement (one minus per-field recoverability) on the same population, not an independent honest-run-pair measurement.' This is the right caveat, but it leaves a real and arguably computable analysis missing: the corpus contains multiple files per operation class (16-20 files for clean classes per operation_class_inventory.json). A genuine empirical false-alarm baseline -- comparing the decoder's prediction on one honest run against the controller log of a DIFFERENT honest run of the same operation class -- would give an independent, non-circular FPR estimate that the paper currently asserts is impossible to produce. The 'single-run per condition' limitation (L862) is about cross-trial variance, but it does not actually preclude a same-class run-pair disagreement-rate estimate, which is exactly the missing honest-pair measurement.
FIX: Either (a) compute a same-operation-class honest-run-pair disagreement rate as an independent FPR proxy (decoder prediction on run A vs. controller log of run B, same class) and report it alongside the recoverability-complement FPR, or (b) if genuinely infeasible, state explicitly WHY the multi-file-per-class corpus cannot support even a coarse run-pair FPR estimate, so the reader does not assume the omission is an oversight. This is the single most credibility-relevant missing control for the detector framing.

[A-sub-13] (A-related) decoder_paper_v2.tex, line 823 ('Audit leverage by toolpath entropy' paragraph) :: related-work
ISSUE: Citation mischaracterization. The clause "complementing rather than replacing cryptographic file-hash signing~\cite{chhetri2017confidentiality}" attaches the Chhetri et al. 2017 reference to the concept of *cryptographic file-hash signing*. That paper ('Confidentiality Breach Through Acoustic Side-Channel in Cyber-Physical Additive Manufacturing Systems', ACM TCPS) is an *acoustic side-channel IP/confidentiality attack* demonstration, not a work on file-hash signing or integrity controls. A reviewer who pulls the reference will see an obvious mismatch between the cited content and the sentence it supports. This is the kind of error that erodes trust in the entire bibliography.
FIX: Either (a) move the \cite{chhetri2017confidentiality} to a sentence about acoustic side-channel confidentiality/IP leakage (e.g., the line-90 IP-protection cluster, where it actually belongs), and cite file-hash-signing work (\cite{belikovetsky2017drowned,nist2024fmnet}) for the 'cryptographic file-hash signing' clause; or (b) drop the citation from line 823 entirely, since the entropy-axis corollary is already attributed to the companion paper \cite{paperB_entropy}.

[A-sub-14] (A-claims) decoder_paper_v2.tex:75 (Abstract) vs :733 (disc-confound) :: consistency
ISSUE: The abstract presents the +22pp increment more confidently than the body supports. The abstract says it is 'robust to conditioning the null on operation class' (true) but omits the body's central caveat that it is 'a 4-of-5-fold effect that is zero on one fold' and that 'the mean increment ... would not survive removal of more than one fold' (line 733). A reader of the abstract alone would take the +22pp as a uniform 5-fold result. The abstract does later parenthesize the fold-1 caveat in the headline-findings paragraph (line 106: 'a 4-of-5-fold effect that is zero on one fold'), so the abstract proper is the only place the fold-1 fragility is dropped.
FIX: Add the fold-dependence to the abstract sentence, e.g. '...a +22 pp within-class command increment (a 4-of-5-fold effect, near-zero on the one covariate-shift outlier fold) that we report as an upper bound on sensor-driven recovery, robust to conditioning the null on operation class.'

[A-sub-15] (A-claims) decoder_paper_v2.tex:909 (Conclusion item (1)) :: claim/rigor
ISSUE: The conclusion states the class-prior-controlled within-class increment '...is the figure that survives both the prior and an unseen-class holdout.' This conflates two distinct results in a way that overclaims. The +22pp increment is measured in the IN-DISTRIBUTION closed-vocabulary regime; it does NOT survive the unseen-class (LOCO) holdout. Under LOCO, command accuracy collapses to 0.213 (verified, loco_significance.json / loco_per_class_9holdouts.json), which is BELOW the 0.572 class prior, i.e. the model does worse than the sensor-independent null on novel classes. What 'survives' the holdout is nothing positive about the increment; the increment is precisely what collapses. The body (Sec 4.21/disc-confound) is careful that the increment is closed-vocabulary and that LOCO is the open-vocabulary floor, but this sentence merges them into a single false 'survives both' claim.
FIX: Rewrite to separate the two: e.g. '...the class-prior-controlled within-class increment over the operation-class modal predictor (Section disc-confound) is the figure that survives conditioning the null on operation class; on an unseen operation class (LOCO, Section loco) this increment does not transfer and command recovery collapses below the class prior (0.213), the open-vocabulary floor.'

[A-sub-16] (A-critic) per_axis_recoverability.tex (Table 7 / tab:per_axis); decoder_paper_v2.tex per-axis section (L505-525) :: claim/rigor
ISSUE: Per-axis numeric value MAE is reported in inches (X 0.248, Y 0.205, Z 0.009, R 0.021) but is never contextualized against the physical scale that makes those numbers interpretable: the machine work envelope, typical feature dimensions, or the within-window coordinate range. A reader cannot tell whether a 0.25-inch X error is catastrophic (larger than many features) or negligible. The encoder-probe section mentions a '~3.0 normalized-unit within-window range' (L382) but this never connects to the inch-scale MAE table, and there is no statement of the workpiece footprint or coordinate span. Given that 'numeric precision is the limiting tier' is a central conclusion, the magnitude of the limitation is left unquantified in interpretable terms.
FIX: Add the coordinate-span context to the per-axis MAE discussion and table caption: state the X/Y/Z work-envelope extent (or the empirical per-axis coordinate range over the corpus) so the 0.21-0.25 inch X/Y MAE can be read as a fraction of feature/part scale (e.g., 'X MAE 0.248 in against a ~X.X in X-axis span = ~Y% of range'). This converts the numeric-tier limitation from an uninterpretable absolute number into a deployment-meaningful relative one and directly supports the 'unsupported for coordinate reconstruction' claim.

[A-sub-17] (A-figtab) supplementary_materials.tex line 1005 (Figure fig:supp-seq2seq-baseline caption/intro) vs decoder_paper_v2.tex lines 706 + Table tab:seq2seq_baseline :: consistency
ISSUE: The supplement figure S (fig:supp-seq2seq-baseline) describes the seq2seq baseline as one that 'shares the encoder backbone ... the only change is the decoder head, which is replaced by a flat token-sequence decoder' — i.e. it KEEPS the frozen MM-DTAE encoder. But the main paper's Section 7.10 (line 706) and Table 11 explicitly define the baseline as a 'from-scratch Transformer encoder--decoder (8.64 M params, 4-layer encoder + 4-layer decoder) ... No frozen encoder.' These are two materially different baselines (one with, one without the frozen encoder), yet the figure is presented as the visual companion to the same Table 11 result. A reader comparing the two will conclude the architectures described do not match.
FIX: Rewrite the S31 figure intro (line 1005) and the figure caption (line 1010) to match Section 7.10 exactly: a from-scratch Transformer encoder-decoder with NO frozen encoder, no grammar mask, no structured heads. Remove 'shares the encoder backbone ... the only change is the decoder head.' Confirm the bars in seq2seq_baseline_tf_vs_ar_bars.pdf were generated from the from-scratch run, not an encoder-sharing variant.

============================================================
## MODERATE (47)
============================================================

[A-mod-1] (A-consistency) Main paper Sec 3.2 (line 212) vocabulary "1{,}048 NUM_X buckets, 1{,}086 NUM_Y buckets, 231 NUM_Z buckets ... Total vocabulary size: 2{,}418" :: claim/rigor
ISSUE: The component bucket counts and the 2,418 total are stated but not cross-checked here against an artifact, and they must be internally consistent with the 'reduces ... from |V|=2,418 to 31.8 tokens' (line 324), the K-spectrum K=2418 (line 412, 621), and the legacy-token head '2,418 classes' (line 309). 1048+1086+231 = 2365 for X/Y/Z alone, leaving only 53 for SPECIAL+COMMAND+PARAM+all other NUM axes (R,F,I,J,K,S,P) -- plausible but tight; no artifact in audit/ was found that enumerates the full 2,418 breakdown. Given 2,418 recurs in 4+ places it is load-bearing and should be tied to a released vocab file.
FIX: Cite the vocabulary artifact (e.g. the tokenizer/vocab JSON) that confirms 1048+1086+231 plus the remaining axes/specials sum to exactly 2,418, and verify the 31.8 mean-valid-next-token figure (line 324) is computed against that same |V|=2418.

[A-mod-2] (A-consistency) Main paper Sec 6.10 (Discussion, threat model), line 815: "the empirically measured honest-trace autocorrelation $\rho!=!0.42 \pm 0.24$" :: statistics
ISSUE: The stated standard deviation on the measured lag-1 autocorrelation is 0.24, but the cited artifact audit/tamper_alert_autocorrelation.json reports aggregate rho_honest_lag1 std = 0.21811854 (i.e. 0.22, not 0.24). The mean 0.4210 correctly rounds to 0.42, but the dispersion is overstated by ~2 pp. This is the only place a \pm is attached to \rho; the conclusion (line 912) and Supplementary S31 repeat \rho=0.42 without a \pm, so the body is internally inconsistent on whether/which dispersion is reported.
FIX: Change "$\rho\!=\!0.42 \pm 0.24$" to "$\rho\!=\!0.42 \pm 0.22$" to match audit/tamper_alert_autocorrelation.json (std 0.2181), or drop the \pm for consistency with line 912 and the supplement.

[A-mod-3] (A-consistency) Main paper Sec 7.3 Statistical Power (line 858) vs Sec 5.1 headline (line 501) and Table 6 (per_class_command.tex) :: consistency
ISSUE: Two different command-class support bases are presented in adjacent sections without reconciling their denominators, inviting reader confusion. Sec 5.1 / Table per_class_command report PER-FOLD full-window supports (G0 ~65, G1 ~830, G2 ~400, G3 ~315). The power analysis (line 858, from audit/extended_rigor_audits.json rec6) reports much larger supports (G0 n=86, G1 n=1095, G2 n=475, G3 n=395, none n=5886) that are on a different basis (fold-1 per_row positions). A careful reader sees "G1 ~830 per fold" then "G1 (n=1095)" pages later with no explanation of the change of basis.
FIX: Add one clause at line 858 stating the basis explicitly, e.g. "Per-class supports below are fold-1 per-row COMMAND-position counts (cf. the per-fold full-window supports in Table~\ref{tab:per_class_command})." so the two support columns are not mistaken for a discrepancy.

[A-mod-4] (A-figtab) Overall supplement figure budget: 34 figures + 5 input-tables + several inline tables in supplementary_materials.tex vs 8 figures + 2 input-tables in main body :: structure
ISSUE: The supplement contains 34 figures and ~10 tables versus 8 figures / ~11 tables in the main body. Several supplement figures are explicitly self-described as 'corroborative', 'illustrative', 'visual companion', or 'duplicate inclusion' (k_spectrum twice, two confusion-matrix sets, sensor_overlay, learning_curves, t-SNE, length_vs_accuracy 'corroborating not load-bearing', class_conditional_modal_freq). MDPI Sensors supplements are permissive, but a 34-figure supplement risks the perception of padding and makes it harder for a reviewer to find the load-bearing corroboration (e.g. the mode_collapse_heatmap, the reliability_diagram, the LOSO/nested heatmaps).
FIX: Do one consolidation pass on the supplement targeting the explicitly-redundant items flagged above (cut duplicate k_spectrum, cut raw-count confusion matrices, merge outpos table+figure, consider merging slot-5 trio). This would remove ~6-8 figures with zero information loss and sharpen the remaining corroborative set.

[A-mod-5] (A-consistency) Supplement Sec S1 (line 107) and Main paper Sec 5.6 (line 549): per-operation-class AR "face ... token 0.56 / numeric 0.40 ... pocket150025 ... 0.09 / 0.01" :: claim/rigor
ISSUE: The AR per-op-class face=0.56/0.40 and pocket150025=0.09/0.01 figures are not traceable to a clean cited artifact in audit/. audit/per_op_class_v8.json is broken (op_names length-mismatch errors, empty aggregate); audit/per_op_class_v8_fixed.json holds TF values (face token mean 0.9696, not 0.56); audit/per_op_class_v8_ss05.json holds per-row SS=0.5 token values (face 0.733), none of which is the AR full-window 0.56/0.40 cited. The face n=144 windows checks out against per_op_class_v8_fixed.json, but the AR accuracy values themselves lack a verifiable source file. (This is the per_op_class alignment issue flagged in project memory.)
FIX: Cite the specific JSON that contains the AR full-window per-op-class token/numeric values (e.g. the AR breakdown that yields face 0.56 / 0.40), and confirm per_op_class_v8.json is regenerated without the op_names alignment error before release, since it is the natural artifact a reviewer/reproducer will open. If the values come from failure_cases_fullwindow.json or an unsaved recompute, persist a clean per_op_class AR JSON.

[A-mod-6] (A-critic) decoder_paper_v2.tex L209 (per-row section) vs L382 (encoder probe) :: consistency
ISSUE: Two different summary statistics for rows-per-window are stated and never reconciled. Section per-row (L209) says a window 'contains an average of 60 distinct G-code rows (median 29, max 237).' The encoder-probe section (L382) says 'a window holds a median of 29 rows (max 237)' and elsewhere '~60 candidate rows.' The mean of 60 with a median of 29 implies a very heavy right tail, which is plausible, but the paper alternates between '~60' and '29' as the operative number for the row-disambiguation argument (e.g., 'disambiguate which of ~60 candidate rows' at L626) without stating which statistic is load-bearing. This is a minor internal inconsistency that a careful reader will flag, and I could not locate the source artifact (operation_class_inventory has n_gcode_rows per class but not a per-window distribution).
FIX: State both statistics once with the source artifact, and use the median (29) consistently as the operative row-count for the disambiguation argument (it is the more representative number given the skew), or explicitly justify using the mean (60) where it appears. Cite the artifact that produces the 60/29/237 distribution so it is verifiable.

[A-mod-7] (A-critic) decoder_paper_v2.tex L668-672 (orthogonal probes), Conclusion item 3 (L911) :: claim/rigor
ISSUE: The deployment recommendation 'drop y_bed__4, retain spindle2' (L740) rests on three nested null results (LOMO p=0.499, LOSO p=0.415, nested 6x6 p=0.896), all of which the paper itself flags as underpowered at n=5 ('failure-to-reject is not positive evidence for equivalence'). Yet the redundancy/drop-a-board recommendation is stated as an actionable deployment takeaway in bold. The completeness gap: a TOST equivalence test is applied to the frozen-encoder modality means (L657) but NOT to the LOSO per-board retrained results that actually underwrite the 'drop y_bed__4' recommendation. The recommendation to physically remove a sensor board is the kind of concrete actionable claim that most needs an equivalence-style bound (e.g., 'the cost of dropping y_bed__4 is <= X pp at 90% confidence'), not just a non-significant ANOVA.
FIX: Apply the same TOST + BF01 equivalence machinery used for the frozen-encoder modalities to the LOSO per-board retrained deltas, and report an upper equivalence bound for the y_bed__4 drop (e.g., 'TOST rejects a cost >2.6 pp at the +/-X margin'). If TOST is inconclusive at n=5 (likely), temper the bold 'drop y_bed__4' recommendation to 'y_bed__4 is the leading candidate for cost reduction, pending an equivalence confirmation at larger support.'

[A-mod-8] (A-critic) decoder_paper_v2.tex L890-895 (Conclusion, exposure-bias remedies) :: related-work
ISSUE: The Conclusion lists 'standard exposure-bias remedies [he2019exposure, ott2018uncertainty, ranzato2016sequence, holtzman2020curious, welleck2020neural]' as a future direction to close the AR mode-collapse gap, but the paper never actually evaluates ANY of them beyond scheduled sampling (p_max=0.5, already in headline) and beam search (mentioned as not closing the gap, S10.4). For a paper whose central negative result is AR mode-collapse, the absence of even one tested exposure-bias mitigation (e.g., a quick scheduled-sampling sweep beyond 0.5, or a single run with sequence-level training / unlikelihood) is a completeness gap: the mode-collapse mechanism is diagnosed thoroughly but no remedy is empirically probed, leaving open whether the collapse is fundamental or merely untreated.
FIX: Either add a short experiment (even single-fold, clearly labeled as a pilot) applying one exposure-bias remedy beyond scheduled sampling/beam, OR add an explicit sentence in the mode-collapse discussion stating that no exposure-bias remedy beyond scheduled sampling and beam search was tested and that whether the collapse is treatable is therefore an open question -- so the citation list reads as 'untested directions' rather than implying coverage.

[A-mod-9] (A-figtab) decoder_paper_v2.tex Table tab:ablations (ablations_summary.tex) row '+ sequence classifier (pattern-aware, fold-1)' line 28-34 :: figure/table
ISSUE: Table 5 (ablation matrix) mixes a single-fold pilot row (pattern-aware sequence classifier, fold-1 only, no +-std) into a table whose other rows are 5-fold mean+-std. The fold-1 row reports bare point values (0.694 / 0.536 / etc.) with no uncertainty alongside 5-fold means with std, which invites a reader to compare a fold-1 number directly against a 5-fold mean. The caption flags it as a fold-1 pilot, but placing it in the same numeric column as 5-fold means is visually misleading.
FIX: Either move the pattern-aware fold-1 pilot row out of Table 5 into the single-fold-pilots supplement section (S8) where the other single-fold probes already live, or visually set it apart (e.g., a midrule + an explicit 'single-fold pilot (not comparable)' subheader and dashes in the std). The cleanest fix is to remove it from the headline ablation table since S8 already reports it.

[A-mod-10] (A-figtab) decoder_paper_v2.tex Table tab:per_axis (per_axis_recoverability.tex) line 17, F-row Value MAE 1.115; and caption :: figure/table
ISSUE: The F (feed-rate) row in Table 6 reports a Value MAE of 1.115 +- 0.350 'inch' while every other axis MAE is in inches at the 0.01-0.25 scale; the footnote explains F values are larger (~10-100 inch/min) so the MAE is 'on a different scale'. Putting a 1.115 value in the same 'Value MAE' column with the same nominal unit as Z's 0.009 invites a reader to conclude F is recovered ~100x worse, when in fact it is a different physical quantity (inch/min vs inch) and is also flagged elsewhere as not meaningfully evaluable due to thin support. Mixing a feed-rate MAE (inch/min) into a coordinate-MAE column (inch) is a units inconsistency within one column.
FIX: Annotate the F-row MAE with its actual unit inline (e.g. '1.115 (inch/min)') rather than relying on the footnote, or move the F row's MAE to a separate sub-table/footnote so the 'Value MAE (inch)' column is unit-homogeneous. Given F is already labeled thin-support and not in the headline claim, the cleanest option is to dash the F Value-MAE cell and report it only in prose.

[A-mod-11] (A-figtab) decoder_paper_v2.tex Table tab:tamper_auc (lines 784-808), LOCO block rows :: figure/table
ISSUE: Table 8's open-vocabulary LOCO block reports Command-swap and Sign-flip rows but the Feed-rate LOCO row is flagged '(degenerate, n=45)' with BA 0.50 and TPR/FPR both 0.000. A degenerate n=45 cell sitting in the same table as the load-bearing operating points may read as a real measurement to a quick scanner. Also the in-distribution block reports AUC for command-swap but BA for sign-flip/feed-rate (two different discrimination metrics in one 'AUC or BA' column), which is correct but easy to misread as comparable numbers down the column.
FIX: (a) Either drop the degenerate Feed-rate LOCO row or move it to a footnote, since it carries no information (n=45, all-zero). (b) Add a light visual cue (e.g. an italic '[AUC]' / '[BA]' tag is already present per-row, but consider shading or a vertical rule) to reinforce that AUC and BA rows are not on the same scale, and state in the caption that down-column comparison of AUC vs BA values is not meaningful.

[A-mod-12] (A-write) decoder_paper_v2.tex line 106 ('Headline findings') and line 120 (Contributions item 6) and line 486 (sec:results-headline) :: consistency
ISSUE: The 'we reserve recovery for categorical fields; generative reconstruction is at floor (AR exact-match 0.026)' framing is stated in the abstract (75), Headline findings (106), the results body (486), and the Conclusion item 2 (910). The terminological-reservation sentence ('we reserve the word recovery...') appears nearly verbatim in 106 and 486.
FIX: State the terminology reservation once, prominently, in the Introduction's Headline-findings paragraph, and in the results body (486) shorten to 'as defined in Section 1, recovery refers only to the categorical heads.' Avoid re-deriving the 0.026 exact-match claim in all four locations; cite the table.

[A-mod-13] (A-write) decoder_paper_v2.tex line 462-463 (sec:stats, 'Inferential scope and a caution on n=5') :: clarity
ISSUE: Line 461 (reporting conventions) and 463 (n=5 caution) are two consecutive very dense statistical paragraphs. Line 461 explains the NumPy-population-vs-ddof=1 convention difference in a 1200+ char sentence; line 463 chains five inferential-policy clauses (treat p as descriptive / read CIs as smoothed / nonparametric primary / rest on effect sizes / single-fold pilots labeled). A reader wanting to know 'how should I read the statistics' must parse two heavy blocks.
FIX: Convert line 463's five-clause policy sentence into a short bulleted list ('Our statistical reporting policy:') — this is exactly the content readers want at a glance. Compress line 461's s.d.-convention explanation to one sentence and move the per-table convention mapping to a footnote or Supplementary S19.7, since it 'does not change any significance call'.

[A-mod-14] (A-write) decoder_paper_v2.tex line 484 (sec:results-headline, 1731 chars) — the sequence-TF-not-well-defined explanation :: clarity
ISSUE: The headline results paragraph embeds a ~4-line explanation of why a sequence-level TF score is not well-defined ('the metric requires generating a full output, and under teacher forcing the model is conditioned on the true prefix...collapsed to an all-or-nothing test'). This methodological aside interrupts the flow of reporting the headline numbers and partly duplicates the TF-vs-AR explanation already given in sec:metrics (lines 247-253).
FIX: Move the 'why sequence-TF is undefined' explanation to sec:metrics (where TF/AR is defined) or to a footnote, leaving the results paragraph to report the numbers and cite the regime definition. This tightens the single most-read paragraph of the Results.

[A-mod-15] (A-stats) decoder_paper_v2.tex line 484 / Sec. 6.2; line 683 / Sec. 6.9 (LOCO); audit/loco_bootstrap_ci.json :: statistics
ISSUE: The LOCO command floor is reported as 0.213 ± 0.191 with the finite-population justification (nine classes are the population, so no inferential CI). The artifact loco_bootstrap_ci.json nonetheless contains a bootstrap CI over the nine holdouts (cmd ci_lo 0.102, ci_hi 0.353) that is never surfaced. The finite-population framing is defensible, but the ±0.191 std is then doing inferential-looking work (it is a between-class spread, with one extreme outlier damagepocket=0.678 inflating it). Reporting a single ± with a heavily skewed 9-point distribution (eight in [0,0.27], one at 0.68) can mislead; the median 0.21 is given but the mean±std is the one quoted in the abstract-adjacent 'floor of 0.213'.
FIX: When quoting 0.213±0.191, add the distributional shape inline (median 0.21; range [0,0.68]; 8/9 in [0,0.27]) at first use (it is given later in Sec. 6.9 but not at the headline/line 484 use). Consider leading with the median as the 'floor' descriptor since the mean is pulled up by the damagepocket outlier, and note the ±0.191 is between-class heterogeneity not sampling error (already stated at line 683 but not at line 484).

[A-mod-16] (A-stats) decoder_paper_v2.tex line 497 (Fig. five_fold_spread caption) and line 871 (Sec. 8.3.1 covariate-shift audit); cross-check audit/seed_robustness_full_window.json :: statistics
ISSUE: The fold-1 command-head outlier (0.776 vs folds 2-5 at 0.91-0.92) is attributed exclusively to a -4.4 pp Z-coverage covariate shift. But the seed-robustness artifact shows fold-1 command accuracy is partly seed-dependent: seed 42 (reported headline) gives fold-1 = 0.7762, while seeds 456 and 2024 give fold-1 = 0.8301 and 0.8181 — a 4-5 pp seed-driven swing on the very fold whose depression drives the +0.222 increment's 4/5-fold pattern and the calibration outlier. The covariate-shift narrative is presented as the sole cause; it is at most a partial cause, since fold-1 is materially higher on other seeds with identical splits/Z-coverage.
FIX: Add one clause where the fold-1 outlier is first introduced (caption of Fig. five_fold_spread and/or Sec. covariate-shift) noting that the fold-1 depression is partly seed-specific (fold-1 command ranges 0.776-0.830 across the four seeds in seed_robustness_full_window.json at fixed splits), so the Z-coverage covariate shift is a contributing, not exclusive, explanation. This also slightly strengthens the +0.222 increment: on higher-fold-1 seeds the fold-1 sign flip in the confound control may not occur.

[A-mod-17] (A-stats) decoder_paper_v2.tex line 621 (Sec. 6.6, K-spectrum control); tables/k_spectrum.tex caption; audit/k_spectrum_stats.json :: statistics
ISSUE: The body says T3.1 (K=2418, matched schedule) is 'the strict winner on every metric (AR struct token 0.470 vs headline 0.317; Holm-adjusted p=0.071 on the paired contrast)'. The cited Holm p=0.071 is for the AR-token contrast and is NOT significant at alpha=0.05 (verified: Holm p=0.0707, paired t=3.26). Calling it the 'strict winner' while the load-bearing AR-token contrast fails Holm correction overstates the result. The k_spectrum.tex caption instead emphasizes the AR-sequence contrast (Holm p=0.006), creating an asymmetry between body and table about which metric carries the claim.
FIX: Reword to 'highest-performing variant on every metric (5/5 folds positive on AR token, +15.2 pp; the paired AR-token contrast is Holm p=0.071, not significant at n=5; the AR-sequence contrast is Holm p=0.006)'. Replace 'strict winner' with 'directionally strongest (5/5 folds), significant on AR sequence but not AR token after Holm', consistent with the paper's stated descriptive-p stance and with the supplement (line 643, t(4)=3.26).

[A-mod-18] (A-stats) decoder_paper_v2.tex line 628 (Sec. 6.7.3 Target-Mode Ablation); tables/ablations_summary.tex :: statistics
ISSUE: The headline target-mode result 'command accuracy moves from per-row 0.499 to full-window 0.888, a +38.9 pp gain' reports both endpoints as bare point estimates with no cross-fold std or CI on the per-row 0.499 figure, and no test of the difference. The full-window 0.888 carries ±0.056 elsewhere but the per-row 0.499 appears without dispersion anywhere in the body, so the load-bearing +38.9 pp lift has no uncertainty quantification despite being a central RQ2 claim and the command head being TF/AR-invariant (so a paired-by-fold test is feasible).
FIX: Report the per-row command accuracy with its 5-fold std and give a paired-by-fold effect size for the +38.9 pp lift (e.g., per-fold deltas, Cohen's d_z, or at least the fold range), matching the rigor applied to the positional-metadata and LOMO contrasts. If only fold-1 per-row is available, label it as such.

[A-mod-19] (A-write) decoder_paper_v2.tex line 636 (sec:abl-sensors, 2321 chars) and line 634 ('Audio-RMS fold count' digression) :: clarity
ISSUE: The audio-RMS n=4-vs-n=5 fold-count caveat is explained in laborious detail twice within the same subsection (line 634 'unambiguous statement' and again at the end of line 636 in parentheses) and a third time in the figure caption logic. The TOST+BF01 inconclusive-verdict aside in line 636 is a dense statistical digression in the middle of stating the main finding (uniform collapse).
FIX: State the audio-RMS retrain note once, in a single sentence ('all seven modalities are evaluated at n=5; the table caption's n=4 is the pre-retrain support, retained for audit consistency'). Move the TOST/BF01 inconclusive-verdict detail to the supplement and replace with one clause ('at n=5 the seven modalities cannot be ranked against each other; see Supplementary S5'). The finding — uniform 40-44 pp collapse, not modality differentiation — should lead the subsection unencumbered.

[A-mod-20] (A-write) decoder_paper_v2.tex line 813 (Operating-point recommendation, 2587 chars) :: clarity
ISSUE: This single paragraph delivers the central deployment recommendation but is 2587 characters and mixes the alert-budget rationale, the two recommended operating points (1)(2), the LOCO caveat, AND a restatement of the TRL-4 / not-a-SOC-monitor framing 'for the deployment-table reader'. The two concrete operating points — the actual recommendation — are hard to find inside the surrounding qualification.
FIX: Lift the two operating points into a short labeled list or two-sentence paragraph at the top of the recommendation, then give the qualifications. The 'restated for the deployment-table reader' closing sentence duplicates the Positioning paragraph (line 750) and the Conclusion item 4 (line 912) and can be cut.

[A-mod-21] (A-write) decoder_paper_v2.tex line 915 (Conclusion, final paragraph, 1858 chars) and line 912 (Conclusion item 4) :: structure
ISSUE: The Conclusion's closing paragraph (915) re-states, in one long block, the seq2seq scoping, the 'closing the gap is a sequence-head problem first', the encoder-co-responsibility caveat, the exposure-bias remedy citations, the TRL-5-6 target, the cross-platform precondition, AND a final 'none of these upgrades the posture to real-time SOC' sentence — most of which already appeared in the Discussion and in Conclusion items (2),(3),(4). The conclusion recapitulates rather than concludes.
FIX: Trim the closing paragraph to: the architectural-contribution scoping (one sentence), the principal open direction (encoder retraining + cross-platform, one sentence), and a single forward-looking close. Delete the re-listed exposure-bias citation pile (it is in Future Work) and the redundant 'not real-time SOC' sentence (stated in item 4 and three times in the Discussion).

[A-mod-22] (A-write) decoder_paper_v2.tex line 96 (Three challenges) and lines 100-104 (RQ list) and lines 109-121 (Contributions) :: structure
ISSUE: The Introduction has four consecutive enumerated/structured blocks back-to-back: 'Three challenges' (i)(ii)(iii), 'Research questions' RQ1-4, 'Headline findings', and 'Contributions' (1)-(6). The reader hits four lists in a row before the Scope statement. The Contributions list (6 items) and the RQ list (4 items) overlap heavily in content (e.g., Contribution 5 = RQ4, Contribution 3 = RQ1's per-axis decomposition).
FIX: Consider merging or cross-indexing the RQ and Contributions lists, or convert 'Three challenges' to prose (it is short). At minimum, add a transition sentence between the lists so the Introduction does not read as four bullet blocks in sequence. Contribution 6 (line 120) also smuggles in the companion-paper relationship, which belongs in the Positioning/Companion appendix.

[A-mod-23] (A-write) decoder_paper_v2.tex lines 640, 645-646, 652, 668-672 (sec:abl-sensors Interpretation, fig caption, sec:abl-lomo Interpretation) :: consistency
ISSUE: The 'frozen-encoder zeroing measures joint necessity / gating artifact; encoder retraining gives the genuine ranking; no single modality load-bearing' narrative is repeated four times: sec:abl-sensors Interpretation (640), the Figure sensor_ablation caption (645-646), the sec:abl-lomo finding+interpretation (652, 668-672), and again in sec:disc-modality (742). The frozen-vs-retrained contrast and the 'gating artifact' phrasing recur near-verbatim.
FIX: The two figure captions (sensor_ablation and lomo_modality) both re-explain the frozen-vs-retrained distinction at length; trim them to describe what the figure shows and cross-reference. State the gating-artifact interpretation once (sec:abl-lomo Interpretation, 672) and have sec:disc-modality (742) point to it rather than re-running the three-step argument.

[A-mod-24] (A-stats) decoder_paper_v2.tex lines 795-808 (Table tamper_auc) and line 782 :: statistics
ISSUE: The illustrative tamper table mixes a swept-threshold ROC-AUC with Hanley-McNeil CI (command-swap) and a single-threshold balanced-accuracy (BA) estimator (sign-flip, feed-rate), with CIs given only for the command-swap AUC rows. The sign-flip/feed-rate BA rows and the LOCO rows carry no uncertainty at all, and the FPR is explicitly the recoverability complement on the same population (not an independent honest-run measurement). Given the prominent 'illustrative, not a validated detector' framing this is honestly disclosed, but presenting point BA values (0.61-0.78) and LOCO BA (0.20, 0.50, 0.66) with no interval invites over-precise reading, especially feed-rate LOCO BA 0.50 flagged 'degenerate n=45'.
FIX: Add at least bootstrap or Wilson-style intervals (or an explicit 'n too small for CI' marker) to the BA rows, and state in the caption that BA point values without CIs at these n (n=111 feed-edit, n=45 LOCO feed) are not separable from chance. This matches the rigor of the command-swap AUC rows and reinforces the illustrative framing.

[A-mod-25] (A-write) decoder_paper_v2.tex lines 917-962 (Appendices that are entirely pointers to Supplementary Materials) :: structure
ISSUE: There are five consecutive appendix sections (app:spec-addenda, app:companion, app:replicability, app:diagnostics, app:extended-ablations, app:baselines) that are almost entirely 'this content has been relocated to Supplementary Materials Section Sxx' pointers. The reader who reaches the appendices finds a chain of redirects rather than content, which is disorienting and inflates the apparent length.
FIX: Consolidate the pure-pointer appendices into a single short 'Supplementary Materials Guide' appendix with a table mapping topic -> Supplementary section, rather than six separate \section pointers. Keep app:companion (the four-contribution division) and the Data Availability as real content; collapse the rest. This is a structural cleanup that will noticeably reduce reader friction at the back of the paper.

[A-mod-26] (A-critic) decoder_paper_v2.tex when-to-use (L744), disc-threat-model (L815), Conclusion item 4 (L912) :: scope/framing
ISSUE: The primary deployment posture is 'forensic-support post-hoc audit' in which 'command/axis-letter disagreements flag specific rows for offline analyst review.' The paper quantifies per-row FPR (e.g., command-swap baseline FPR 0.299, K=10 aggregated FPR ~0.10-0.18) but never translates this into the analyst-burden quantity that would make the forensic posture concrete: at the measured per-row/per-window disagreement rate, how many false-flag rows does an analyst face per audited program (programs have ~hundreds-to-thousands of rows)? An FPR of 0.10 over a 1000-row program is 100 flagged rows -- whether that is a usable triage aid or an unusable flood is unaddressed. The deployment claim's viability hinges on this and it is a completeness gap.
FIX: Add a short analyst-burden calculation to the 'when to use' or threat-model section: at the K=10 aggregated FPR (~0.10) and typical per-fold distinct-line counts, state the expected number of false-flag rows per audited program and argue why that volume is (or is not) tractable for human triage. This grounds the 'forensic-support' framing in an operational number rather than leaving it qualitative.

[A-mod-27] (A-write) decoder_paper_v2.tex — global (121 em-dashes, 369 semicolons across 1038 lines) :: presentation
ISSUE: Em-dash and semicolon density is very high (a '---' roughly every 8.5 lines; a semicolon every ~3 lines), producing many sentences with stacked subordinate clauses. The git log shows a prior 'em-dash stacks' cleanup (commit b7c68b7) but the density remains a readability drag, especially in the Discussion. Reviewers of a Sensors paper will read this as overworked prose.
FIX: Do a targeted pass converting the heaviest em-dash/semicolon chains (lines 88, 534, 636, 735, 813, 928 are the worst) into separate sentences. Aim to halve the em-dash count. A useful heuristic: any sentence with two or more '---' or three or more ';' should be split.

[A-mod-28] (A-write) decoder_paper_v2.tex — global (18 occurrences of 'load-bearing', 17 of 'in-distribution', 14 of 'forensic-support post-hoc audit', 13 of 'cross-platform', 12 of 'closed-vocabulary') :: presentation
ISSUE: Several phrases are repeated to the point of becoming verbal tics. 'load-bearing' (18x), 'forensic-support post-hoc audit' spelled out in full (14x), 'closed-vocabulary' (12x). The repeated full spelling of 'forensic-support post-hoc audit' is especially heavy; once established it could be 'the audit posture'. 'load-bearing' is used as a hedge-emphasis marker so often it no longer signals importance.
FIX: Define 'forensic-support post-hoc audit' once (Intro) and thereafter use 'the audit posture' or 'post-hoc audit'. Cut 'load-bearing' to ~5 genuinely load-bearing uses; elsewhere delete it or replace with the specific noun. This is the residue of the prior voice-cleanup commits and is still over-threshold.

[A-mod-29] (A-related) decoder_paper_v2.tex, Table 1 (tab:lit_comparison, lines 163-189) :: related-work
ISSUE: The paper repeatedly stakes its core differentiation from the AM side-channel literature on *sensor sampling rate / bandwidth* (Section 4.3 l.150: 'raw acoustic emission at kHz-MHz' vs '4 Hz audio-RMS envelope'; Discussion l.746; abstract). Yet the positioning comparison table — the artifact a reviewer scans to grasp novelty at a glance — has no column for sensor rate/bandwidth. The table differentiates only on Domain/Task/Output/Validation/Field-eval/LOCO, none of which surfaces the deliberately-low-rate stance that the prose treats as load-bearing. The single most distinctive axis of the contribution is therefore invisible in the one table built to position it.
FIX: Add a 'Sensor rate / band' column (e.g., Al Faruque/Song/Chhetri/Chattopadhyay = 'raw acoustic, kHz-MHz' or 'optical video'; Kimmell = 'energy/current'; This work = '4 Hz envelope, 7 modalities'). This makes the deliberately-conservative low-rate posture, and the resulting 'categorical-not-geometric' fidelity tradeoff, legible directly from the positioning table.

[A-mod-30] (A-related) decoder_paper_v2.tex, line 138 (Section 4.1, digital-twin/digital-shadow sentence) and Table 1 :: related-work
ISSUE: The digital-twin / digital-shadow process-verification line (bergs2021digital) is introduced in prose as 'complementary to, and orthogonal in trust assumptions from, the present work' — a genuinely important contrast (model-based reference comparison vs. reference-free sensor reconstruction). But this distinct *defense family* is not represented in Table 1's positioning, which lists only sensor-monitoring studies and the two orthogonal non-sensor defenses (signed file, attestation). Digital-twin verification is arguably the closest competing *process-verification* approach and its omission from the comparison table leaves the most natural reviewer question — 'why not just compare observed state against a digital-twin simulation of the intended program?' — answered only in a single prose clause.
FIX: Add a digital-twin/digital-shadow verification row to the 'orthogonal defenses' block of Table 1 (Domain: CNC/AM; Task: model-based process verification; Output: deviation from simulated reference; key distinction: requires a trusted reference model of the *intended* program, whereas this work is reference-free). This pre-empts the 'why not a digital twin?' question at the table a reviewer scans first.

[A-mod-31] (A-related) decoder_paper_v2.tex, line 152 ('To the best of our knowledge, no prior work reports per-row G-code recovery from CNC sensor data with structured-output evaluation') and line 161 ('No prior study combines (a) per-row... (b) structured-output... (c) file-level CV with LOCO') :: claim/rigor
ISSUE: The load-bearing novelty claim is a triple-conjunction ('per-row targets AND structured-output eval AND file-level CV+LOCO'). Conjunctive novelty claims are weaker and easier for a reviewer to puncture than a single sharp claim, because a reviewer need only find one prior work hitting two of three to make the contribution feel incremental. The genuinely distinctive element — *per-row/per-field categorical recovery of CNC milling instructions from sensors* (vs. session/class-level or AM-geometry targets) — is the crisp novelty; the CV/LOCO methodology is good practice but not itself novel and slightly dilutes the claim by being bundled into the headline differentiator.
FIX: Lead the novelty statement with the single sharpest claim ('the first per-instruction, per-field categorical recovery of CNC *milling* G-code from a multi-modal sensor stream, as opposed to the session/operation-class granularity of prior CNC monitoring and the geometric-toolpath granularity of AM side-channels') and present the file-level-CV+LOCO protocol as rigorous *evaluation methodology* supporting that claim, not as a co-equal leg of the novelty conjunction.

[A-mod-32] (A-related) decoder_paper_v2.tex, line 90 ('largely absent from the CNC literature') / line 152 ('To the best of our knowledge') :: claim/rigor
ISSUE: The novelty rests on a literature-absence claim ('largely absent', 'to the best of our knowledge no prior work'). The cited CNC set (Section 4.1) leans on a 2022 review (tsanousa2022multisensor) to establish that the field is 'dominated by tool-condition and operation-classification tasks.' For a 2026 submission, anchoring the absence-of-prior-work claim on a 2022 review leaves a ~4-year window in which instruction-level CNC reconstruction could have appeared, which a reviewer may probe. The claim is appropriately hedged ('largely', 'to the best of our knowledge'), so this is about defensibility, not correctness.
FIX: Add a sentence noting the recency of the literature check (e.g., 'A search of CNC-monitoring and manufacturing-security venues through 2025 surfaced no per-instruction G-code recovery work; the closest CNC-security efforts remain binary attack/anomaly detection [Kimmell, Coelho].') and, if possible, cite one 2023-2025 CNC-monitoring survey or representative recent paper to demonstrate the absence claim was checked against current literature, not a 2022 snapshot.

[A-mod-33] (A-related) decoder_paper_v2.tex, line 90 (intro) and line 746 (Discussion) :: scope/framing
ISSUE: Citation-cluster scope drift on the IP-protection framing. Line 90 lists 'intellectual-property protection~\cite{alfaruque2016acoustic,song2016my,chhetri2016kcad,jamarani2024screg,chattopadhyay2025video}' as a motivation, and jamarani2024screg/chattopadhyay2025video are explicitly IP-theft papers ('Decoding Intellectual Property...', 'One Video to Steal Them All... IP Theft'). But the paper's threat model (Section 3.5) and entire deployment posture are *integrity/tamper-detection* (forensic-support audit of executed instructions), never *confidentiality/IP-exfiltration*. The IP-protection motivation is raised once and never developed, and the system as built (a defender-side monitor reconstructing one's own executed instructions) is not an IP-protection mechanism at all. A reviewer may read this as motivational over-reach borrowing the AM IP-theft literature's appeal without delivering on it.
FIX: Either (a) drop the 'intellectual-property protection' motivation and keep these citations purely as 'side-channel reconstruction of manufacturing instructions' precedents (their genuine relevance), or (b) add one sentence in the threat model acknowledging the dual-use angle — that the same sensor->instruction channel an *adversary* could exploit for IP exfiltration is the channel the *defender* repurposes for integrity audit — which would honestly connect the IP-theft citations to the integrity framing rather than leaving the motivation dangling.

[A-mod-34] (A-related) decoder_paper_v2.tex, line 928 (Relationship to Companion Contributions: 'standalone methodological novelty is modest') and line 704 ('None of these components is individually novel') :: scope/framing
ISSUE: The paper twice volunteers that its methodological novelty is 'modest' / that no component 'is individually novel' (l.704, l.928). This candor is admirable and correctly relocates novelty to 'the characterization itself,' but stated this bluntly and twice it risks under-selling to a reviewer or editor who skims those lines out of context — particularly since the actually-novel framing (per-field *categorical recoverability* as a distinct, sensor-recoverable quantity, separated from the unrecoverable numeric/geometric tier, with an upper-bound/floor framing) is genuinely new and is the contribution. The self-deprecation is currently stronger and more memorable than the positive novelty statement that should accompany it.
FIX: Keep the honest 'no individual component is novel' framing but immediately pair each instance with the positive claim in the same breath: '...the novelty is not architectural but *epistemic* — the first per-field characterization establishing which G-code fields are sensor-recoverable (categorical: command/axis/sign) and which are provably not (numeric coordinate/feed), with bound-style rather than point-accuracy claims.' This converts a concession into a sharpened contribution statement.

[A-mod-35] (A-claims) decoder_paper_v2.tex:106, 484, 486 (headline framing) :: claim/rigor
ISSUE: The paper repeatedly anchors the headline on the '~0.89 command' categorical recovery as TF/AR-invariant and sensor-driven, but the single most important honesty fact (that ~0.57 of the 0.79 command accuracy is operation-class prior, so only ~0.22 is sensor-driven within-class signal) lives only in the Discussion (disc-confound, line 733). RQ1/headline (Sec results-headline) reports command 0.8875 prominently and only flags the class-prior caveat as the third of three caveats by cross-reference. Given that the abstract's load-bearing result is explicitly the +22pp increment 'not the closed-vocabulary ~0.89', the headline results subsection under-weights the decomposition relative to the abstract's own framing.
FIX: In Section results-headline, when first stating command 0.8875, add a one-clause forward pointer that the majority (~0.57) of this is operation-class prior and the sensor-driven within-class increment is +22pp (Section disc-confound), so the abstract's framing (increment, not raw 0.89) is established at first mention rather than only in Discussion.

[A-mod-36] (A-claims) decoder_paper_v2.tex:488 (numeric-target patch robustness) :: statistics
ISSUE: The robustness-to-patch paragraph reports the released vs patched headline drift as 'all drift <=1.5 pp' and lists command 0.891/0.888, numeric 0.570/0.585. But patched_numeric_analysis.json shows the per-slot bathtub slot-5 changed by +0.2242 (0.7689 released -> 0.9931 patched), a 22 pp change at the finest slot, which is far larger than 1.5 pp. The paper does explain this elsewhere (slot-5 is the float-epsilon artifact, Section numeric-decomposition), and the <=1.5pp claim is specifically about the HEADLINE/categorical metrics not the per-slot numeric tier. But as written the juxtaposition of 'numeric 0.570/0.585' (pooled) with 'all drift <=1.5 pp' could mislead, since the patch's whole purpose was a >20pp slot-5 correction. The pooled numeric being robust while one slot moves 22pp is a real subtlety that deserves an explicit one-clause note.
FIX: Qualify: 'all POOLED/headline metrics drift <=1.5 pp; the patch's effect is concentrated at the finest digit slot (slot-5 numeric accuracy 0.77->0.99, an encoding-artifact correction, Section numeric-decomposition), which is why we use the patched retrain for the per-digit and per-axis numeric tier.'

[A-mod-37] (A-claims) decoder_paper_v2.tex:549 (Per-Operation-Class Recoverability) :: claim/rigor
ISSUE: The per-op-class section cites AR token/numeric for face (0.56/0.40) and pocket150025 (0.09/0.01) against pooled AR baseline 0.215/0.104, but does not cite the source artifact for these specific AR-per-class numbers (the verified per_op_class_v8_fixed.json is teacher-forced: adaptive token 0.82, not AR). The body claim that face's high score is 'coincidental agreement with the modal trajectory' is well-supported by ar_output_diversity.json (modal predicted string is a facing pattern, 9.5% over-representation). But the specific AR per-class numbers (0.56/0.40, 0.09/0.01) should be traceable to a named artifact for audit parity with the rest of the paper, which is otherwise scrupulous about \texttt{audit/...} citations.
FIX: Add the \texttt{audit/...json} source for the AR per-operation-class numbers (face 0.56/0.40, pocket150025 0.09/0.01) in Section per-op-class or its caption, matching the citation discipline used everywhere else.

[A-mod-38] (A-claims) decoder_paper_v2.tex:735 :: claim/rigor
ISSUE: The class-conditional-modal robustness argument is sound but rests on a subtle estimand mismatch that is easy to misread. The text reports the all-command-position class-conditional modal at 0.576 (verified, class_conditional_modal_5fold.json = 0.5762) and notes a first-command-position-only version reaches ~0.81 (verified, class_conditional_modal.json = 0.8108). The honest move is to compare like-for-like (all-position), which the paper does. But a skeptical reader will note that the decoder's 0.795 is also evaluated at all-command positions, and on a per-WINDOW basis (the unit that actually matters for a tamper alert on a block) the relevant null might be closer to the 0.81 first-position figure. The paper asserts 0.81 'is not a like-for-like null' but does not fully justify why the all-position aggregation is the deployment-relevant one rather than a per-block-first-command basis.
FIX: Add one sentence justifying why the all-command-position aggregation (not first-command-per-window) is the correct null for the decoder's all-position accuracy, e.g. tie it to the fact that the decoder emits a command token at every command position within the full-window target and the tamper detector operates per-row, so the all-position basis is the matched one; the first-position 0.81 is the right null only for a one-command-per-window summarizer, which the decoder is not.

[A-mod-39] (A-claims) decoder_paper_v2.tex:735 'A paired-bootstrap 95% CI on the per-fold delta is approximately [+0.0, +0.30]' :: statistics
ISSUE: The paired-bootstrap CI on the +22pp increment is reported as 'approximately [+0.0, +0.30]', with 'its lower edge brushes zero.' A CI whose lower bound is 0.0 means the increment is not distinguishable from zero at 95% — i.e. the headline upper-bound increment is, by the paper's own CI, not significantly positive. The text leans on effect size and 4/5-fold sign consistency instead, which is defensible at n=5, but the bare statement that the CI lower edge is at zero deserves an explicit acknowledgement that the increment is therefore not a confirmed positive effect, only an effect-size-plus-sign argument. Currently the 'upper bound' framing partly obscures that the lower bound is also unconfirmed-positive.
FIX: State plainly: 'Because the paired-bootstrap lower bound touches zero (driven by the fold-1 sign flip), we do not claim the increment is significantly positive at n=5; the +22pp is an effect-size-and-sign argument (4/5 folds positive) reported as an upper bound, not a confirmed lower-bounded effect.'

[A-mod-40] (A-claims) decoder_paper_v2.tex:735 and Supplement S21 (line 1070) :: claim/rigor
ISSUE: The 'leakage-immune' / 'worst-case' framing for the +22pp increment is asserted as if it holds generally, but by the paper's own per-fold data the worst-case argument fails on fold 1: on fold 1 the decoder (0.552) is at or below its own class prior (0.568), so on that fold the increment is negative and the 'leakage-immune within-class increment' is not present. The supplement (line 1070) does acknowledge it is a '4/5-fold effect, zero on the fold-1 outlier ... survives, on the four non-outlier folds.' But the main-text sentence at 735 ('This increment is an UPPER bound ... but it is robust to conditioning the null on operation class') and the appendix abstract addendum (line 922: 'so the +22 pp ... increment is leakage-immune') state leakage-immunity without the per-fold qualifier in the immediate sentence. The qualifier should travel with the claim every time it is asserted as load-bearing.
FIX: Wherever 'leakage-immune' is asserted (line 735, line 922 appendix addendum), append the fold restriction inline: 'leakage-immune on the four non-outlier folds (zero/negative on the fold-1 covariate-shift outlier).' This is what the data support.

[A-mod-41] (A-claims) decoder_paper_v2.tex:75 (Abstract) and :128-159 (Related Work) :: scope/framing
ISSUE: The abstract and intro frame the motivating threat as 'a hash-invisible forgery' / 'cyber-physical attack ... without altering the loaded file' and the application as forensic audit of contested runs. But the paper's own adaptive-adversary analysis (line 819) concludes 'the residual security value against a non-naive adversary is small: the monitor meaningfully constrains only command/axis/sign substitutions that also stay within the trained toolpath envelope.' The abstract's tamper sentence ('illustrative, prior-dominated, not a validated detector') is honest but does not convey the stronger structural-bypass conclusion that the recoverable fields are exactly the low-value ones for a knowledgeable adversary. The framing gap is between the motivating threat (high-value coordinate/feed tampering) and what is actually defendable (command/axis/sign within-envelope).
FIX: Add a half-sentence to the abstract's tamper clause acknowledging the structural bypass, e.g. '...not a validated detector; an adversary who corrupts only coordinates or feed rate is invisible by construction, so the monitor constrains only in-envelope command/axis/sign substitutions.' This makes the abstract carry the paper's actual security ceiling.

[A-mod-42] (A-claims) decoder_paper_v2.tex:801-808 (Table tamper_auc, in-distribution AUC/BA) :: figure/table
ISSUE: Table tamper_auc reports command-swap AUC 0.80/0.86 with Hanley-McNeil CIs and labels the regime 'In-distribution closed-vocabulary (5-fold pooled).' The FPR (0.299) and TPR (0.921) match threat_model_tamper_AR_5fold_FSM.json exactly (verified). However, the table footnote correctly states FPR is 'mechanically (1 minus per-field recoverability), the recoverability complement re-labelled as an alert rate, not an independent honest-run-pair measurement.' Given this, the swept ROC-AUC of 0.80-0.86 for command-swap is built on a synthetic relabelling of honest predictions, so the AUC is not an AUC of a real detector against real honest/tampered pairs. The 'illustrative, not a validated detector' caveat (line 777-780) covers this in prose, but the table itself presents AUC with formal CIs, which lends an unwarranted air of detector validation. Consider flagging the AUC column header itself as illustrative.
FIX: Add '(illustrative; synthetic relabelling)' to the Table tamper_auc caption title or to the AUC/BA column header, so the formal Hanley-McNeil CI is not read as validating a deployable detector independently of the prose caveat.

[A-mod-43] (A-critic) supplementary_materials.tex S21 (L1065+); decoder_paper_v2.tex L490, L731-735 :: claim/rigor
ISSUE: The representation-reuse concern is dispatched with an analytic worst-case bound (leakage capped at 0.572 = operation-class modal) plus the LOMO-baseline empirical check (retrained-encoder command 0.904 vs headline 0.888, |Delta|<=0.016). But the LOMO-baseline 'leave-decoder-test-files-out' check only removes the test files of the CURRENT fold from encoder training; it does not establish a fully partitioned encoder/decoder split because the LOMO encoder is still trained on the same operation classes and same file pool minus held-out test files. The paper presents 0.904 as evidence the headline is not inflated by reuse, but 0.904 > 0.888 could equally reflect that the LOMO recipe (different optimizer/schedule for the retrained encoder, possibly more train data per the channel-identity gate) is simply a stronger configuration. The two estimates are not a clean A/B on representation reuse alone, yet they are presented as nearly settling the concern.
FIX: Clarify in S21 that the LOMO-baseline is not a clean isolation of representation reuse (it co-varies the encoder training recipe and possibly the train-set size), so the 0.904-vs-0.888 comparison bounds reuse only under the assumption that those co-variates are negligible. Keep the analytic 0.572 worst-case bound as the primary argument and downgrade the LOMO-baseline to corroborating-with-caveats, or explicitly enumerate which factors are held identical between the headline and LOMO-baseline encoders.

[A-mod-44] (A-figtab) supplementary_materials.tex Sections S 'Confusion Matrices (Counts)' (lines 933-967) and S 'Per-Class Head Diagnostics' (lines 1160-1176); figures confusion_matrix_*_counts.pdf and confusion_matrix_*_normalized.pdf :: figure/table
ISSUE: The supplement carries TWO full sets of confusion matrices for the same heads: row-normalized (command/type/param-type in Fig fig:confusion at 1160; sign/digit in Fig fig:supp_confusion_sign_digit at 906) AND raw-count versions for all five heads (Figs at 938-967). That is up to ten confusion-matrix panels for five heads. The supplement itself concedes at line 936 the count matrices 'are otherwise redundant with the normalized versions.' Ten near-duplicate panels is excessive even for a supplement and dilutes the load-bearing figures.
FIX: Drop the five raw-count confusion-matrix figures (Section S 'Confusion Matrices (Counts)', lines 933-967) and instead state once that raw per-cell counts are available in the released audit artifacts (the section already says this at line 936). Keep only the row-normalized set. If raw counts are genuinely needed for chi-squared recomputation, consolidate all five into a single multi-panel figure rather than five standalone ones.

[A-mod-45] (A-figtab) supplementary_materials.tex line 978 (Figure fig:supp-tamper-roc caption) :: consistency
ISSUE: The tamper-detection ROC figure caption names the three attack classes as 'command-swap, parameter-substitution, magnitude-perturbation', but the main-paper Table 8 (tab:tamper_auc), Section 8.4, and figure tamper_aggregation_curves all use the consistent triple 'command-swap, sign-flip, feed-rate (+-10x)'. 'parameter-substitution' and 'magnitude-perturbation' are non-matching labels for what are elsewhere called sign-flip and feed-rate edits.
FIX: Change the S figure caption to 'command-swap, sign-flip, feed-rate (+-10x)' to match Table 8 and the rest of the threat-model section, or add an explicit one-line gloss mapping parameter-substitution->sign-flip and magnitude-perturbation->feed-rate if the figure axes truly carry the older labels (in which case regenerate the figure).

[A-mod-46] (A-figtab) supplementary_materials.tex lines 687-692 (Figure S? fig:k_spectrum) and 994-999 (Figure fig:supp-kspectrim); also tables/k_spectrum.tex Table 10 (main) and figures/k_spectrum.pdf :: figure/table
ISSUE: The identical PDF file k_spectrum.pdf is included twice in the Supplementary Materials as two separately-numbered figures (fig:k_spectrum at line 689 with width \linewidth and fig:supp-kspectrum at line 996 with width 0.6\linewidth), with overlapping captions. The supplement text at line 992 openly admits this: 'the duplicate inclusion below is a standalone presentation.' The same data is also already in Table k_spectrum (main body). A duplicated figure inflates the figure count and is the kind of thing a Sensors copy-editor will flag.
FIX: Cut the second inclusion (fig:supp-kspectrum, Section S 'Vocabulary K-Spectrum Curve', lines 988-999) entirely and replace any cross-reference to it with a \ref to fig:k_spectrum. One k_spectrum figure plus the main-body Table 10 is sufficient.

[A-mod-47] (A-stats) tables/per_axis_recoverability.tex (Table per_axis, Value MAE column); audit/per_axis_bootstrap_ci.json vs audit/patched_numeric_analysis.json :: statistics
ISSUE: The Value-MAE column reports patched-retrain point estimates with a cross-fold std (X 0.248±0.140, Y 0.205±0.080, Z 0.009±0.006, R 0.021±0.011) but NO confidence interval, even though a 10,000-resample bootstrap CI exists in audit/per_axis_bootstrap_ci.json. Crucially, that bootstrap-CI artifact was computed on the RELEASED checkpoint with materially different point estimates (X 0.277 [0.177,0.432], Y 0.179 [0.135,0.252], Z 0.042 [0.020,0.063], R 0.018 [0.010,0.026]) — i.e., the only available CIs do not correspond to the patched values now in the table (Z differs 0.009 vs 0.042, X 0.248 vs 0.277). The numeric tier thus has point estimates from one checkpoint and the only bootstrap CIs from another, and the table presents only fold-std without intervals. The X MAE fold std (±0.140) is more than half its mean, so an interval is especially warranted here.
FIX: Recompute the per-axis MAE bootstrap CIs on the PATCHED checkpoint and add them to the table (or add a footnote that the released-checkpoint bootstrap CIs in per_axis_bootstrap_ci.json are stale and patched-checkpoint CIs are pending). At minimum, note that X-MAE has a fold std (±0.14) comparable to its mean and is heavy-tailed/bimodal (already stated in line 522), so the single MAE number is a midpoint between regimes — reinforce this caveat in the table caption.

============================================================
## MINOR (61)
============================================================

[A-min-1] (A-consistency) Main paper Sec 4.2 (line 292) decoder param count vs Sec 9.2 seq2seq (line 706) and Conclusion :: consistency
ISSUE: The decoder trainable-parameter count is given as 31.28 M at line 292 ("$31.28$~M trainable parameters ... the Transformer stack is $18.9$~M") and total system ~39.9 M, but Sec 9.2 / Table seq2seq_baseline.tex describe the headline as "$\approx\!30$~M decoder params" and the conclusion/abstract say nothing. 31.28 M vs "~30 M" is fine as rounding, but line 292 also says the Transformer stack is 18.9 M and "the remainder is the grammar-mask + sensor-prior machinery and the six output heads" -- 31.28 - 18.9 = 12.4 M attributed to heads+grammar+embeddings, which is large relative to an 18.9 M transformer and may surprise a reader; verify this breakdown is correct and that S19.8 supports it.
FIX: Confirm the 31.28 M / 18.9 M split against the component breakdown in Supplementary S19.8 and ensure "~30 M" (Table seq2seq) and "31.28 M" (line 292) are explicitly the same quantity; if the 12.4 M non-transformer remainder is dominated by the 2418-way token embedding/output, say so to preempt the "why is the head bigger than the backbone" question.

[A-min-2] (A-consistency) Main paper Sec 4.7 (line 382) "a window holds a median of 29 rows (max 237)" vs Sec 3.3 (line 220) "average of 60 distinct G-code rows (median 29, max 237)" vs Sec 4.2 system caption (line 278) implicit window :: consistency
ISSUE: The per-window row-count statistics (mean 60 / median 29 / max 237) are stated in three places but only fully spelled out in one (line 220). Line 382 and line 628 ("$\sim$60 candidate rows") use the mean=60 and median=29 inconsistently as the headline number (line 382 leads with median 29; line 628 leads with ~60). Not a numeric error, but the reader sees "29" and "60" used interchangeably for "how many rows per window" without a reminder that one is median and one is mean.
FIX: At line 628 change "$\sim$60 candidate rows" to "$\sim$60 candidate rows (median 29)" or standardize on one statistic, so the per-window row count reads consistently across Sec 3.3, 4.7, and 5.x.

[A-min-3] (A-consistency) Main paper Sec 4.7 Encoder probe, line 382: "axis presence $0.87$--$0.93$ (X/Y/Z)" :: consistency
ISSUE: The cited range lower bound 0.87 does not bracket the actual probe values. audit/encoder_probe_v8.json gives probe_has_X=0.8853, probe_has_Y=0.8663, probe_has_Z=0.9269. The minimum across X/Y/Z is Y=0.866, which is below 0.87. The range should be 0.866--0.927, i.e. "$0.87$--$0.93$" understates the spread at the low end (Y rounds to 0.87 only by convention).
FIX: Either widen to "$0.87$--$0.93$" -> "$0.866$--$0.927$" (exact), or restate as "$\approx 0.87$--$0.93$" with an explicit note that Y is the floor at 0.866; the cleanest fix is "$0.87$--$0.93$" -> "$\sim$$0.87$--$0.93$".

[A-min-4] (A-consistency) Main paper Sec 5.1 (line 488) patched headline "command $0.891$/$0.888$ ... numeric $0.570$/$0.585$" :: consistency
ISSUE: The patched-vs-released headline pairs are command 0.891/0.888, token 0.774/0.781, numeric 0.570/0.585, type 0.983/0.984, param 0.993/0.993, AR seq 0.029/0.026. The released values (0.888, 0.781, 0.585, 0.984, 0.993, 0.026) all match Table 4. The patched numeric 0.570 is consistent with patched per-slot recompute. The patched command 0.891 and token 0.774 are asserted but not in the inspected audit JSONs (patched_numeric_analysis.json only carries per-axis MAE and per-slot, not headline command/token). The "all drift <=1.5 pp" claim depends on the 0.891 command being real.
FIX: Cite the artifact that contains the patched 5-fold headline command (0.891) and token (0.774) -- e.g. a metrics.json under checkpoints/full_window_5fold_patched/ -- so the patched/released drift claim is independently checkable, not only the numeric tier.

[A-min-5] (A-consistency) Main paper Sec 5.10 (line 685) "per-fold deltas ... folds 2--5 sit $+0.27$ to $+0.30$" / Sec 6.1 (line 733) :: statistics
ISSUE: Verified against audit/confound_class_prior.json per-fold: deltas = {-0.0163, +0.2800, +0.2737, +0.2955, +0.2784}. The body (line 733) reports per-fold deltas {-0.02, +0.28, +0.27, +0.30, +0.28} and "folds 2--5 sit +0.27 to +0.30." Correct: fold3=+0.274 rounds to +0.27, fold4=+0.296 to +0.30. The stated range +0.27 to +0.30 brackets all four. Consistent. Minor: the bracket lower bound +0.27 is fold-3's +0.2737 which rounds to +0.27, fine; nothing to fix beyond confirming.
FIX: No change required; the per-fold deltas and the +0.27 to +0.30 range are consistent with the artifact. Listed for completeness of the consistency audit.

[A-min-6] (A-consistency) Main paper Sec 5.11 (line 706) "$8.64$~M parameters" seq2seq baseline :: consistency
ISSUE: Verified: seq2seq_baseline/fold_1/results/metrics.json params_millions=8.638578 -> 8.64 M. Consistent. Note the frozen encoder is also stated as 8.6 M (line 287/292); the seq2seq baseline at 8.64 M is coincidentally nearly the same size as the encoder alone, which is the rhetorical point (a same-budget vanilla model). This is fine but the near-identical 8.6 vs 8.64 could read as a typo to a skimming reviewer.
FIX: Consider a half-sentence noting the deliberate budget match, e.g. "(8.64 M, comparable to the frozen encoder's 8.6 M, a same-order-of-magnitude vanilla null)" so the two 8.6x numbers are clearly distinct entities.

[A-min-7] (A-consistency) Main paper Sec 5.4 (line 534) per-digit slot accuracies vs Conclusion (line 910) "slot accuracy 0.42" :: consistency
ISSUE: Slot-4 (thousandths) accuracy is reported as 0.4165 (line 534, matching audit/patched_numeric_analysis.json patched_mean[4]=0.4165), abbreviated to 0.42 in the figure caption (line 541), conclusion (line 910), and threat-model (line 775). All consistent. Slot-5 returns to 0.9931 (line 534) abbreviated 0.99. Consistent. Minor: line 534 writes slot-0 as "$1.0000$" while the artifact value is 0.9999 (patched_mean[0]=0.9999); the table/figure rounds up to 1.00 which is fine but 1.0000 (4 dp) implies exactness the artifact does not have.
FIX: Change slot-0 "reaches $1.0000$" to "reaches $0.9999$" (or "$\approx\!1.00$") to match audit/patched_numeric_analysis.json, since 1.0000 to four decimals asserts a precision the artifact (0.9999) does not support.

[A-min-8] (A-consistency) Main paper Sec 5.9 (line 564) position-1 recovery numbers vs Sec 12 pointer (line 941) :: consistency
ISSUE: The output-position failure numbers are stated twice with slightly different precision: line 564 gives per-row position-1 = 0.240 lifting to full-window 0.622 ± 0.027 (a "+38 pp gain"), while line 941 (diagnostics pointer) gives "position-1 0.24 per-row -> 0.62 full-window". 0.622-0.240 = 0.382 = +38.2 pp, consistent with "+38 pp". Fine, but note the target-mode command gain at line 628 is "+38.9 pp" (0.499->0.888) -- two different "+38/+38.9 pp" effects (position-1 token vs command accuracy) sit close together and could be conflated by a reader.
FIX: Disambiguate the two +38-ish pp effects: at line 564 specify "+38 pp on position-1 token recovery" and at line 628 keep "+38.9 pp on command accuracy," so the near-identical magnitudes are not read as the same statistic.

[A-min-9] (A-consistency) Main paper Sec 6.1 (line 735) class-conditional modal "$0.576 \pm 0.003$" and first-position "$\sim\!0.81$" :: statistics
ISSUE: Verified: audit/class_conditional_modal_5fold.json mean 0.5762 std 0.0028 -> "0.576 ± 0.003" correct; audit/class_conditional_modal.json mean 0.8108 -> "~0.81" correct. The unconditional operation-class modal is 0.5724 ("0.572"). All consistent. The claim "conditioning the null on operation class does not raise it (0.576 ~ 0.572)" is supported (0.576 vs 0.572, a 0.4 pp difference within the 0.3 pp std). Listed to confirm the +22 pp robustness claim is artifact-backed.
FIX: No change required; the class-conditional (0.576), unconditional (0.572), and first-position (0.81) modal nulls all match their artifacts and support the +22 pp upper-bound claim. Included for audit completeness.

[A-min-10] (A-consistency) Main paper Sec 6.4 (line 763): AR output-diversity "407 distinct strings (vs. 459 ... ) ... modal predicted string appears in $9.5\%$ ... vs $2.9\%$ ... a $3.3\times$" :: claim/rigor
ISSUE: Verified against audit/ar_output_diversity.json: sum n_distinct_pred=407, sum n_distinct_true=459, sum n_test=549, mean_modal_pred_frac=0.09498 (9.5%), mean_modal_true_frac=0.02918 (2.9%), ratio 3.25x ~ 3.3x. All correct. One subtlety: the "407 distinct vs 459" are SUMS of per-fold distinct counts (folds are disjoint files), while "9.5% vs 2.9%" are MEANS of per-fold modal fractions -- two different aggregation modes in one sentence. Not wrong, but the basis switch (sum-of-distinct vs mean-of-fraction) is implicit.
FIX: Optionally note the basis: "...produces 407 distinct strings (summed over the five disjoint folds; vs 459 ground truth) and the modal predicted string occupies a fold-mean $9.5\%$ of windows vs $2.9\%$..." to make the two aggregation bases explicit.

[A-min-11] (A-consistency) Main paper Sec 6.9 / Conclusion: tamper "$\sim\!8$~pp sensor-conditioned increment over the modal-substitution control" :: statistics
ISSUE: Verified against audit/tamper_alert_autocorrelation.json: real_tpr mean 0.91999, modal_tpr mean 0.84077, difference 0.0792 = ~8 pp. Correct. However the claim is a TPR increment, and elsewhere the increment is described in command-accuracy terms (+22 pp at line 733) and in a percentage of windows -- the "~8 pp" tamper TPR lift and the "+22 pp" command increment are different estimands measured on different populations, stated within a few paragraphs; a reader could conflate "8 pp" (tamper TPR over modal) with the calibration "12 pp overconfidence" or the "+22 pp" command increment.
FIX: At each occurrence of "~8 pp" make explicit it is a TPR increment over the modal-substitution detector (not a command-accuracy increment), e.g. "$\sim\!8$~pp of detection TPR," to keep it distinct from the +22 pp command-accuracy increment and the 12 pp calibration gap.

[A-min-12] (A-consistency) Main paper Sec 7.3 (line 871) covariate-shift "per-row fold-1 Z $-4.4$~pp" and Sec 5.1 figure caption (line 497) "the $-4.4$~pp Z-coverage covariate shift" :: consistency
ISSUE: The fold-1 Z covariate shift is cited as -4.4 pp in two places. Table data_coverage.tex gives per_row train Z=35.8%, per_row test Z=31.4%, difference = -4.4 pp. Consistent and verifiable. However, the headline command head is on the FULL-WINDOW configuration (line 484), and full_window Z coverage is train 29.7% / test 32.6% = +2.9 pp (a shift of opposite sign). The figure caption (line 497) attributes the fold-1 command outlier (0.776) to "the $-4.4$~pp Z-coverage covariate shift" but -4.4 pp is the PER-ROW Z shift, while the headline/figure is full-window; the relevant full-window Z shift is +2.9 pp, not -4.4 pp.
FIX: Reconcile which regime's Z covariate shift drives the full-window fold-1 command outlier. If the -4.4 pp per-row figure is being used to explain a full-window result, clarify the cross-regime link or cite the full-window covariate-shift number (audit/extended_rigor_audits.json rec7_covariate_shift_full_window) instead.

[A-min-13] (A-consistency) Main paper abstract (line 75) "$\sim\!0.89$ command" and "floor of $0.213$" vs body 0.8875/0.888 :: consistency
ISSUE: The abstract rounds command accuracy to ~0.89, while the body and tables use 0.8875, 0.888, and (confound) 0.795, and the patched value 0.891. Within the body the same quantity appears as 0.8875 (Table 4, line 484), 0.888 (line 490, 636, 742, etc.), and 0.891 (patched, line 488). This is defensible rounding, but the coexistence of 0.8875 / 0.888 / 0.891 / 0.795 for "command accuracy" across the paper is a known reader trap; the 0.795 (COMMAND-position-only) is reconciled at line 734 but 0.888 vs 0.891 (released vs patched) is only reconciled at line 488.
FIX: No numeric change required, but consider a single sentence early (e.g. end of Sec 5.1) defining the canonical headline as 0.888 (released, all-position) and noting 0.795 is the COMMAND-position-only basis and 0.891 the patched recompute, so the three values are never read as conflicting.

[A-min-14] (A-consistency) Supplement Sec S2 (line 124) "$3.0$~pp ECE reduction" and main paper Sec 5.7 (line 559) :: figure/table
ISSUE: The cross-fold command-head ECE reduction is stated as 0.050 -> 0.020 (a 3.0 pp reduction). audit/calibration_cross_fold.json gives ece_pre_mean=0.05005, ece_post_mean=0.019741, ece_reduction_mean_pp=3.0309. The reported "0.020" is the rounded post value (0.0197) and "3.0 pp" matches. Consistent. However, note that 0.05005-0.019741 = 0.03031 = 3.03 pp, whereas 0.050-0.020 = 3.0 pp -- the rounded endpoints (0.050, 0.020) give exactly 3.0 pp, so the rounding is self-consistent. No error; flagging only that the supplement Table S1 row (line 156) writes "ECE $0.050 \to 0.020$" which is correct.
FIX: No change required; this entry confirms consistency. If desired, the supplement could note the exact reduction is 3.03 pp to avoid a reader recomputing 0.050-0.020=3.0 and finding a 0.03 pp rounding gap.

[A-min-15] (A-figtab) decoder_paper_v2.tex Figure fig:encoder_memory_pca (375-380) and Supplement Figure fig:supp_encoder_tsne (894-898) :: figure/table
ISSUE: The PCA (main Fig 2) and t-SNE (supplement) of the same 549-window frozen-encoder memory are a reasonable linear/nonlinear pair, and the split (load-bearing PCA in body, corroborative t-SNE in supplement) is handled correctly. Minor: both captions assert linear separability / tight clusters by op-class; the main PCA caption is long (8 lines) and re-states the low-rank PC counts (9/24/40 -> 80/95/99%) that are also in the body text at line 372-373.
FIX: Trim the Fig 2 caption: the 9/24/40-PC variance numbers appear in both the caption and the body sentence at line 372; keep them in one place (caption is fine) and shorten the body to a pointer, or vice versa. No structural change needed.

[A-min-16] (A-figtab) decoder_paper_v2.tex Figure fig:five_fold_spread (line 494-499) and Table tab:headline (headline_5fold.tex) :: figure/table
ISSUE: Figure 4 (five_fold_spread) is single-regime (TF for token/numeric/type/command/param-type, AR only for sequence) and its caption spends three sentences explaining what it does NOT show ('The full teacher-forced-vs-autoregressive contrast ... is reported in Table 4, not in this single-regime spread'). The figure is largely redundant with Table 4, which already gives per-head mean+-std for both regimes; the only added value is the per-fold scatter points. A reader gets the fold-1 command outlier from the figure, but that single fact is also stated verbatim in the caption and text.
FIX: Consider whether Figure 4 earns its place given Table 4. If kept, tighten the caption to state the one thing the figure adds over the table (per-fold scatter showing the fold-1 command outlier and the categorical-head tightness) and drop the defensive 'not shown here' sentence. If page budget is tight, this is a candidate to relocate to the supplement.

[A-min-17] (A-figtab) decoder_paper_v2.tex Figure fig:per_axis (line 524) and Table tab:per_axis :: figure/table
ISSUE: Figure 5 (per_axis_recoverability) plots has-axis F1, sign accuracy, and value MAE 'side by side' for the per-axis recoverability story, but Table 6 reports the same three quantities plus has-axis Acc and Presence Recall. The figure and table are highly overlapping; the figure adds the visual categorical-vs-numeric split but no new numbers. Caption mentions Y~1.00 vs Z~0.94 sign which is also in the table and twice in the text.
FIX: Keep the figure (the visual three-panel split is genuinely clearer than the table for the categorical-vs-numeric point) but trim the caption to avoid re-stating the Y/Z sign numbers that appear in the table and twice in body prose; let the figure carry the qualitative split and the table carry the numbers.

[A-min-18] (A-figtab) decoder_paper_v2.tex Figure fig:sensor_ablation (642-647) vs Figure fig:lomo_modality (658-670) :: figure/table
ISSUE: Figures 6 (sensor_ablation_bars) and 7 (lomo_modality_bars) are a deliberate contrast pair (frozen-encoder zeroing collapse vs encoder-retraining null), which is good, but each has a ~4-line caption that re-derives the other's result ('Contrast with Figure 6, where ...' / 'A genuine modality-importance ranking requires ... Section 4.10'). The two captions together repeat the joint-necessity-vs-information-content argument that is also made in full in Section 7.5 body text and again in Section 8.2. The figures are necessary; the caption redundancy is high.
FIX: Trim both captions to state only what each figure shows; move the cross-figure interpretive contrast ('the gap is the encoder's re-allocation capacity') to a single sentence in the body where the two figures are introduced, rather than repeating it in both captions and twice in prose.

[A-min-19] (A-critic) decoder_paper_v2.tex L382 (encoder probe), Figure encoder_memory_pca :: methodology
ISSUE: The encoder information-content probe (the upper bound on everything the decoder can recover) is reported only as point accuracies (operation type 0.846, axis presence 0.87-0.93, command 0.77) with no uncertainty/cross-fold spread, and the PCA in Figure 4 is on a pooled 549-window matrix across all 5 folds. Because the probe defines the representational ceiling that the whole 'numeric precision is lost' / 'per-row disambiguation fails' argument leans on, presenting it without per-fold variance or a confidence interval is a small rigor gap relative to the care taken everywhere else in the paper (which is meticulous about n=5 caveats).
FIX: Report the probe accuracies with cross-fold mean +/- std (or note they are single-fold and label them as such, consistent with the paper's single-fold-pilot convention), so the representational-ceiling numbers carry the same uncertainty discipline as the rest of the paper.

[A-min-20] (A-critic) decoder_paper_v2.tex L382 RMSE values; per_axis MAE table :: consistency
ISSUE: The encoder-probe numeric section reports X-value RMSE 0.97, Y 0.80, Z 0.10 in 'normalized units', while the per-axis recoverability table reports decoder value MAE X 0.248, Y 0.205, Z 0.009 in inches. The two numeric-precision diagnostics use different metrics (RMSE vs MAE) and different units (normalized vs inches) and are never put on a common footing, even though they are used to make the same point (numeric precision is the limiting tier). A reader trying to connect the encoder ceiling to the decoder output cannot, because the scales differ. Notably Z is ~0.10 normalized RMSE at the encoder but ~0.009 inch MAE at the decoder output -- whether these are consistent is impossible to check as presented.
FIX: Add a one-line bridge converting the encoder-probe RMSE to the same inch scale (or the decoder MAE to normalized units) so the encoder ceiling and decoder output are directly comparable, and confirm the Z numbers are mutually consistent. This makes the 'encoder retains a window-level mean, not per-row precision' claim verifiable end-to-end.

[A-min-21] (A-critic) decoder_paper_v2.tex L774-776 (audit-leverage-by-entropy paragraph) :: scope/framing
ISSUE: The 'Audit leverage by toolpath entropy' paragraph asserts a defense-in-depth corollary -- 'sensor-side audit is most effective on low-entropy operation classes... and information-theoretically blind on high-entropy adaptive optimizer-generated toolpaths' -- but defers the entire substantiation to the companion limit paper [paperB_entropy]. As a standalone Sensors submission, this is an unsupported forward-reference asserting a result the present paper does not demonstrate. The per-operation-class section (L639) shows face is the only class above floor due to mode-collapse coincidence, which is a DIFFERENT mechanism than 'low source entropy = better audit'; the two could be conflated by a reader.
FIX: Either back the entropy-leverage claim with a minimal in-paper figure/number (e.g., per-class command accuracy vs a crude per-class command-entropy proxy computable from the released corpus), or downgrade the sentence to a clearly-flagged hypothesis developed in the companion paper, and explicitly distinguish it from the mode-collapse-coincidence mechanism of Section per-op-class so the two are not conflated.

[A-min-22] (A-figtab) decoder_paper_v2.tex Table tab:headline (headline_5fold.tex) Sequence row line 16, and the parallel claim in Table tab:ablations 'Sequence (TF)' column :: consistency
ISSUE: Table 4 (headline) reports Sequence only under AR with TF as '--' and a footnote explaining TF sequence is ill-defined. But Table 5 (ablations_summary) has a column header 'Sequence (TF)' and reports values there (Baseline 0.026+-0.019, +metadata 0.024+-0.027). The 0.026 value is the AR sequence number from Table 4 (per ablations_summary.tex line 16, sourced from ar_aggregate.json), so the Table 5 column is mislabeled 'Sequence (TF)' when the number is actually AR. This is a column-header error that contradicts Table 4's explicit statement that a TF sequence number is not well-defined.
FIX: Rename the Table 5 column header from 'Sequence (TF)' to 'Sequence (AR)' (the value 0.026 is the AR exact-match from ar_aggregate.json, matching Table 4's AR sequence). Verify the +metadata 0.024 cell is likewise AR. This is an internal contradiction worth fixing before submission.

[A-min-23] (A-figtab) decoder_paper_v2.tex Table tab:lit_comparison (lines 163-189) :: figure/table
ISSUE: Table 1 (literature comparison) has two columns 'Field-level eval' and 'LOCO' that are 'N/A' for every CNC-monitoring and orthogonal-defense row and only 'Yes' for the This-work row. A column that is N/A/No for all but one row carries little discriminating information and makes the table wider than necessary (it is already \resizebox to \textwidth). The 'Validation' column entries ('Held-out parts', 'Held-out prints', 'Train/test', '10-fold CV') are also inconsistently granular.
FIX: Consider merging 'Field-level eval' and 'LOCO' into a single 'Structured / cross-class eval' column, or keep them but acknowledge in the caption that they are deliberately differentiating columns (the point being that no prior work has them). Standardize the Validation column vocabulary (e.g. 'held-out instances' vs 'k-fold CV') for visual consistency.

[A-min-24] (A-figtab) decoder_paper_v2.tex Table tab:per_class_command (per_class_command.tex) caption line 4 vs G53/M30 rows; cross-ref Section 5.2.3 line 313 :: consistency
ISSUE: Table 2's caption asserts 'no class collapses', and the body (line 501) repeats 'No class collapses.' But G53 recall is 0.846 +- 0.327 and M30 recall 0.769 +- 0.213, each at support ~6 per fold; an std of 0.327 on a 6-instance class means at least one fold has near-zero recall. The 'no class collapses' claim is true for F1 means but is in tension with the huge recall std on the n~6 classes, which the power-limitations section (8.x, MDE ~19pp on G0) elsewhere admits are underpowered. The reader sees 'no class collapses' next to +-0.327.
FIX: Soften to 'no class collapses in 5-fold-mean F1; the smallest-support classes (G53, M30, n~6/fold) have wide per-fold recall variance and are underpowered (Section 8.3)'. This aligns the table caption with the statistical-power limitations already stated and avoids overclaiming on the n~6 tail.

[A-min-25] (A-critic) decoder_paper_v2.tex Title/frontmatter (L60-90) :: presentation
ISSUE: For MDPI Sensors, the manuscript omits the optional 'Featured Application' box (a 1-2 sentence application statement Sensors encourages for applied work) and there is no graphical abstract referenced. Given the strongly application-framed contribution (forensic-support audit for manufacturing cybersecurity), a Featured Application box would help editorial framing and is a low-cost completeness item for the target venue.
FIX: Add a 'Featured Application' statement after the abstract, e.g., 'Controller-independent forensic-support audit of executed G-code on desktop CNC mills via multi-modal sensor decoding, complementing file-hash signing and controller attestation for in-distribution tamper triage.' Optionally prepare a graphical abstract from the system-overview figure.

[A-min-26] (A-write) decoder_paper_v2.tex line 100 (RQ1 statement) and line 431, 849 (sampling-rate / 77.7% restatements) :: consistency
ISSUE: The '77.7% of rows occupy <=1 sample period at 4 Hz' fact appears 7 times (RQ1 line 100, sampling-rate context 431, sec:lim-method 849, future work 894, abstract 75, conclusion 912, plus figure logic). RQ1's parenthetical re-derivation of the 'window-aggregated row-modal recovery' qualifier is itself ~5 lines inside the RQ definition.
FIX: State the 77.7%/4 Hz/sub-sample fact once with its full framing (sec:lim-method, line 849 is the natural home) and reduce the other occurrences to the bare statistic or a cross-reference. In RQ1 (line 100), shorten the parenthetical qualifier to one clause and point to Section sec:lim-method for the duration audit.

[A-min-27] (A-write) decoder_paper_v2.tex line 116 (Contributions item 4) and line 392-399 (sec:arch-ablation-summary) :: clarity
ISSUE: sec:arch-ablation-summary (392-399) contains two adjacent paragraphs (395-397) that both say essentially 'a one-row-per-component ablation summary appears as Supplementary Table S1 in Supplementary Section S3' — the same pointer stated twice in consecutive paragraphs.
FIX: Delete the duplicate pointer; keep one sentence directing the reader to Supplementary Table S1. The whole subsection is a pointer-to-supplement and could be compressed to a single paragraph.

[A-min-28] (A-write) decoder_paper_v2.tex line 264 (Threat Model 'Attack classes', 1394 chars) and line 775 (Detection capability by attack class) :: structure
ISSUE: The five attack classes are enumerated in the Problem-Formulation threat model (264, with MITRE ATT&CK mappings) and then the per-class detectability is enumerated again in the Discussion (775). The split is deliberate and signposted, but the attack-class list itself is effectively restated. A reader must hold the line-264 list in mind for ~500 lines until line 775.
FIX: This split is acceptable, but consider adding a compact attack-class -> detectability summary row to Table tab:tamper_auc or a one-line recap at line 775 ('the five attack classes of Section sec:problem-threat-model: (i)... (v)...') so the detectability discussion is self-contained without back-paging.

[A-min-29] (A-write) decoder_paper_v2.tex line 287 (Sensor Encoder, 1388 chars) and line 292 (Decoder Architecture param breakdown) :: clarity
ISSUE: Line 287 lists the seven modality channel counts inline (accelerometer 18, gyro 18, magnetometer 18, env 6, color 24, audio 6, current 8) and line 292 gives a parenthetical parameter breakdown (31.28 M total; stack 18.9 M; remainder grammar-mask+prior+heads). Both are accurate but the inline channel enumeration duplicates the bulleted list in the Dataset section (lines 410-417).
FIX: In line 287 reduce to 'seven modality groups totaling C=98 channels (per-modality breakdown in Section sec:dataset)' rather than re-listing the per-modality counts. Keep the bulleted enumeration only in the Dataset section.

[A-min-30] (A-write) decoder_paper_v2.tex line 422 (Dataset operation-class enumeration) and line 679 (LOCO) and line 912 (Conclusion) :: presentation
ISSUE: The nine operation-class names (adaptive, adaptive150025, face, face150025, pocket, pocket150025, damageadaptive, damageface, damagepocket) are spelled out in full at least three times (Dataset 422, LOCO 679, and referenced in Conclusion). The full nine-name list is long and adds little on repetition.
FIX: Spell out the nine class names once (Dataset, line 422). In LOCO (679) and elsewhere refer to 'the nine operation classes (Section sec:dataset)'. This is minor but contributes to the overall density.

[A-min-31] (A-stats) decoder_paper_v2.tex line 461 (Hedges g_z formula) and supplement uses (lines 240, 644, 1240) :: statistics
ISSUE: Sec. 5.7 defines the small-sample correction as g_z = J·d_z with J = 1 - 3/(4(n-1)-1) and states J≈0.80 at n=5. With n=5, 4(n-1)-1 = 15, so J = 1 - 3/15 = 0.80 (correct). However the supplement reports several g_z values that imply d_z = g_z/0.80; e.g., line 644 'Holm p=0.071, Cohen's d_z=1.46' for the K-spectrum AR-token contrast, but the artifact gives the headline-vs-K335 token contrast d_z=1.84/g_z=1.47 — the 1.46/1.47 'g_z' is being labeled inconsistently as d_z in one place and g_z elsewhere. Worth a consistency pass so d_z and g_z labels are not swapped.
FIX: Audit every reported d_z/g_z pair against the artifacts and ensure the label matches the value (g_z ≈ 0.80·d_z at n=5). In particular reconcile supplement line 644's 'Cohen's d_z=1.46' with the artifact's d_z=1.84 / g_z=1.47 for that contrast (it appears to be the Hedges g_z mislabeled as d_z).

[A-min-32] (A-stats) decoder_paper_v2.tex line 461 (Sec. 5.7 Statistical Analysis) :: statistics
ISSUE: The std-convention disclosure says reported ± values follow each script's native convention (NumPy population s.d. ddof=0 for headline/sensor tables; ddof=1 for K-spectrum/no-numeric), and that the ~10-15% spread 'does not change any significance call'. Mixing ddof=0 and ddof=1 across tables at n=5 is a real inconsistency a careful reader will notice (e.g., headline command 0.0558 is population s.d.; bootstrap CI uses the same fold means). At n=5 the ddof=0-vs-1 factor is sqrt(5/4)=1.118, ~12%, which is non-trivial when std appears in effect-size denominators (Cohen's d) reported elsewhere.
FIX: Standardize on ddof=1 (sample s.d.) for all reported ± at n=5 — it is the conventional choice and the unbiased variance estimator — or, if regeneration is infeasible before submission, state per-table which convention each uses (a one-line note in each affected caption) rather than only a global statement, so d and ± are internally consistent within each table.

[A-min-33] (A-stats) decoder_paper_v2.tex line 461 (bootstrap seeding) and Table bootstrap_ci.tex caption :: statistics
ISSUE: Bootstrap 95% CIs use 10,000 percentile resamples over the five fold-means seeded at 42. A percentile bootstrap of the mean over n=5 is known to be badly under-covered (the resampling distribution of a 5-point mean is highly discrete and the percentile interval is anticonservative/narrow). The bootstrap_ci.tex caption commendably says 'not coverage-calibrated', but the body (e.g., line 589) still cites these as '95% confidence intervals' in places (line 589 'Bootstrap 95% confidence intervals for both configurations appear in Table'), partially undercutting the caveat.
FIX: Ensure every in-body reference to these intervals uses the hedged phrasing ('bootstrap cross-fold spread intervals' or 'smoothed cross-fold ranges') rather than '95% confidence intervals', matching the table caption. Consider reporting the n=5 t-interval alongside (or instead), which has a defensible coverage interpretation under approximate normality, as a cross-check on the bootstrap range.

[A-min-34] (A-stats) decoder_paper_v2.tex line 534 (Sec. 6.4) and line 922 (App. A); tables/per_digit_position.tex footnote; audit/patched_numeric_analysis.json :: statistics
ISSUE: The entropy-accuracy anticorrelation is reported as 'pooled n=6 Pearson -0.89' (and Spearman -0.94). A Pearson correlation on n=6 points is extremely low-power; the paper reports the coefficient without a CI or p-value, and the inference rests on it (it is also carried into the companion paper). With n=6, even |r|=0.89 has a wide CI (roughly [-0.99, -0.20] by Fisher z) and the slot-0/slot-5 endpoints (both near-deterministic, low entropy, near-perfect accuracy) heavily leverage the fit.
FIX: Note the n is small and report the per-(axis,slot) n=30 correlation (Pearson -0.83) as the primary statement, treating the pooled n=6 number as illustrative. Optionally add the Fisher-z 95% CI for the n=6 value or explicitly flag it as descriptive, consistent with the paper's n=5 descriptive-p posture elsewhere.

[A-min-35] (A-write) decoder_paper_v2.tex line 549 (Per-Operation-Class) and line 685 (damagepocket outlier) and line 763 (mode-collapse) and line 763 modal string :: consistency
ISSUE: The 'modal AR output is a facing-pattern toolpath, so face windows score by coincidental agreement' mechanism is explained in sec:per-op-class (549) and the same mechanism underlies the LOCO damagepocket-outlier explanation (685) and the mode-collapse section (763). The three are correct and mutually consistent but the 'coincidental agreement with the modal trajectory' idea is re-explained each time.
FIX: Establish the modal-facing-pattern mechanism once in sec:disc-mode-collapse (which is its natural home) and have sec:per-op-class (549) and the LOCO discussion (685) cite it: 'by the modal-trajectory mechanism of Section X.' Currently sec:per-op-class explains the mechanism before the section that owns it appears.

[A-min-36] (A-stats) decoder_paper_v2.tex line 559 (Sec. 6.10 Calibration); audit/calibration_cross_fold.json and calibration_fold1.json :: consistency
ISSUE: Two slightly different fold-1 command-head ECE/accuracy values circulate: line 559 (and supplement line 120) cite fold-1 ECE 0.122 at accuracy 0.780 against 0.902 mean confidence, sourced from calibration_fold1.json (ece 0.1223, acc 0.7800, n=1982). But calibration_cross_fold.json reports fold-1 command ECE 0.1238 at accuracy 0.7740 on n_eval=991 (the eval-half split used for temperature fitting). The two n's (1982 full-fold vs 991 eval-half) and the two accuracies (0.780 vs 0.774) are not reconciled in the text, which could read as a discrepancy.
FIX: Add a half-sentence clarifying that the illustrative fold-1 ECE 0.122/acc 0.780 is on the full fold-1 command positions (n=1982; calibration_fold1.json), whereas the cross-fold post-scaling figures are computed on the held-out evaluation half (n≈991/fold; calibration_cross_fold.json). The numbers are consistent measurements on different position subsets, but the text should say so to preempt a 'which n?' query.

[A-min-37] (A-stats) decoder_paper_v2.tex line 606 (Sec. 6.7.1, BH-FDR/Holm 840-test grid) :: statistics
ISSUE: The 840-test multiplicity grid reports 'BH-FDR flags 457/840; Holm flags 167/840' and correctly cautions these are n=5 ANOVAs. However, the text does not state the BH-FDR/Holm family definition (whether the 840 tests are treated as one family or partitioned), nor that BH-FDR controls the expected false-discovery proportion (not per-test error), which matters for interpreting '457 flagged'. As written a reader could over-read 457 as '457 real effects', which the paragraph then argues against — but the FDR semantics (≈5% of 457 expected false among rejections under independence; dependence across the drilled per-class cells violates BH independence and warrants BY) are not stated.
FIX: Add one clause: the 840 tests are treated as a single family; BH-FDR assumes independence/PRDS which the per-class drilled cells likely violate (adjacent metrics on the same predictions are positively dependent), so the Benjamini-Yekutieli variant would be the dependence-robust choice and would flag fewer than 457. This strengthens the existing 'robustness sweep, not 457 established effects' reading.

[A-min-38] (A-stats) decoder_paper_v2.tex line 634-636 (Sec. 6.8 Sensor-Modality Ablation), audio-RMS n=4/n=5 :: statistics
ISSUE: The audio-RMS modality is reported at n=5 in the body after a retrain, but the bootstrap_ci.json still shows zero_rms at n=4 (and the sensor_ablation table caption retains the n=4 annotation 'for audit-trail consistency'). The body asserts the F=0.07,p=0.998 between-modality verdict is unchanged either way and pre/post means agree to <0.01, which is reassuring, but Table bootstrap_ci is then internally mixed (one modality at n=4, six at n=5) without the table itself flagging it — only the body does. A reader consulting the CI table alone would not know zero_rms is n=4 there.
FIX: Add the n=4/n=5 audio-RMS note to the bootstrap_ci table as well (not only the sensor_ablation table), or regenerate the zero_rms bootstrap row at n=5 so all modality CIs share the same n. Minor, but avoids a standalone-table inconsistency.

[A-min-39] (A-write) decoder_paper_v2.tex line 721-722, 729-730, 740, 744, 748 (Discussion paragraph headers using '\hspace*{0pt plus 1fill}') :: presentation
ISSUE: Several Discussion paragraphs use a paragraph-title pattern with a trailing '\hspace*{0pt plus 1fill}' and embedded \label, producing a run-in bold heading followed by a forced line break. Combined with the question-form headers ('Is categorical recovery sensor-driven or operation-class structure?') this creates an inconsistent heading style versus the rest of the paper which uses \paragraph{} normally. The mixed style looks unintentional.
FIX: Standardize: either promote these to \subsubsection (they are substantial enough) or use plain \paragraph{} without the \hspace fill. Pick one heading idiom for the Discussion and apply it uniformly.

[A-min-40] (A-write) decoder_paper_v2.tex line 728 ('The deeper question...is the subject of the next subsection') and line 727 (\label{sec:disc-failure-mode} placed mid-paragraph) :: structure
ISSUE: Line 727 ends a paragraph about numeric precision with a label \label{sec:disc-failure-mode} that semantically belongs to the failure-mode discussion, not to this paragraph, and line 728 is a one-line teaser ('The deeper question...is the subject of the next subsection'). Mislabeled cross-reference anchors and teaser sentences add navigational friction.
FIX: Move the \label{sec:disc-failure-mode} to the actual failure-mode content (sec:failure-modes is at line 566) or rename it to reflect what it anchors. Delete the standalone teaser sentence; the subsection header already signals the transition.

[A-min-41] (A-stats) decoder_paper_v2.tex line 735 (Sec. 7.1.2); supplement line 1070 :: statistics
ISSUE: The class-conditional modal null is reported as 0.576 ± 0.003 (class_conditional_modal_5fold.json: per-fold [0.5746,0.581,0.5748,0.5747,0.576], mean 0.5762, std 0.0028 — matches), and the argument is that conditioning the null on operation class 'does not raise it' above the 0.572 unconditional modal. This is correct, but the comparison 0.576 vs 0.572 is within rounding and the text leans on it as a robustness result. The two nulls are computed on different bases (the 0.572 unconditional modal in confound_class_prior.json is the operation-class modal already; 0.576 is also class-conditional) — the near-equality is essentially tautological in this corpus because each operation class has a single dominant command, so the 'stricter null does not beat it' claim is true but nearly definitional, not an independent empirical safeguard.
FIX: Note explicitly that the unconditional 'operation-class modal' (0.572) and the 'class-conditional modal' (0.576) are nearly the same estimand by construction in this corpus (each class is command-near-modal), so their near-equality is expected rather than a strong independent robustness finding — the real safeguard is the leakage upper bound at 0.572 (supplement S21), which the text should foreground over the 0.576≈0.572 comparison.

[A-min-42] (A-related) decoder_paper_v2.tex, line 136 (Section 4.1, 'CNC anomaly detection~\cite{coelho2024anomaly}') vs Table 1 and Kimmell row :: related-work
ISSUE: The CNC-milling cybersecurity precedents that are closest in *domain and intent* (Kimmell et al. 2025 control-injection detection; Coelho et al. 2024 CNC anomaly detection) are discussed in Section 4.3/4.4 and tabulated, but Section 4.1 (CNC monitoring) frames the gap purely around 'tool-condition and operation-classification tasks' (tsanousa2022multisensor). The closest attack-detection neighbors are thus introduced only after the gap statement, so the Section-4.1 gap ('per-row instruction targets are largely absent') momentarily reads as if no security-relevant CNC sensing work exists, before 4.3/4.4 reveal that it does (just at binary-detection granularity).
FIX: Add a half-sentence to the Section 4.1 gap statement acknowledging that the security-adjacent CNC sensing work that does exist (Kimmell, Coelho) operates at *binary attack/anomaly granularity* — forward-pointing to 4.3/4.4 — so the gap is precisely 'instruction-level structured recovery,' not 'any security-relevant CNC sensing.' This tightens the gap claim and removes the brief impression of overstating the void.

[A-min-43] (A-related) decoder_paper_v2.tex, line 145 (Section 4.2, gcode_foundational / G-code-LLM cluster) :: related-work
ISSUE: The G-code-from-LLM works (gllm, image2gcode, chatcnc, gcode_foundational) are dispatched in a single sentence as 'a related but distinct task ... and does not consider sensor-conditioned recovery.' This is accurate but undersells the distinction the paper could draw: those works *generate* G-code from natural-language/image *intent* (forward, design-time), whereas this paper *recovers* G-code from the *physical execution trace* (inverse, run-time). That forward-vs-inverse framing is a sharp, memorable positioning point that the current one-liner leaves implicit.
FIX: Sharpen the sentence to name the direction explicitly: 'These works address the *forward, design-time* problem of synthesizing G-code from human intent (text/image); the present work addresses the *inverse, run-time* problem of recovering the executed G-code from its physical sensor signature — the two share a vocabulary but no task structure.'

[A-min-44] (A-related) decoder_paper_v2.tex, line 150 ('Al~Faruque \etal recovered FDM print geometry from acoustic emissions, achieving high agreement with the source G-code') :: related-work
ISSUE: 'achieving high agreement with the source G-code' is vaguer than the cited literature supports and than the paper's own quantitative register elsewhere. Al Faruque et al. (2016) report specific reconstruction accuracies (axis-movement/length prediction figures in the ~78-86% range depending on axis/metric). A reviewer from the side-channel community will notice the imprecision, especially since this paper is otherwise meticulous about reporting exact numbers and CIs.
FIX: Replace 'achieving high agreement with the source G-code' with the specific reported figure(s) from Al Faruque et al. (e.g., their per-axis reconstruction accuracy), or soften to 'achieving reconstruction accuracy sufficient to reproduce simple printed geometries' if a precise figure cannot be pinned down. This also strengthens the later AM-comparison (l.746) by giving a concrete number to contrast against.

[A-min-45] (A-related) decoder_paper_v2.tex, line 159 (Section 4.4) and line 264 (threat model, MITRE/NIST mappings) :: structure
ISSUE: Standards/framework anchoring (NIST SP 800-82, FMNet 2024, NIST CSF 2.0, MITRE ATT&CK for ICS, IEC 62443) is the strongest external validation of the *problem's* importance, but it is split across Related Work 4.4 (NIST SP 800-82, FMNet) and the Problem-Formulation threat model 3.5 (MITRE ATT&CK, NIST CSF, IEC 62443). A reviewer assessing 'is this gap real and recognized by the community?' has to assemble the picture from two distant sections. The FMNet 2024 'instruction-stream-integrity gap' citation (nist2024fmnet) is the single most direct external endorsement that this exact gap is an acknowledged research need, yet it is one clause deep in 4.4.
FIX: Foreground the FMNet 2024 'instruction-stream-integrity gap' as the explicit motivating gap earlier (it could anchor the second paragraph of the Introduction or open Section 4.4), and add a one-line forward pointer from 4.4 to the MITRE/CSF/IEC mappings in 3.5 so the standards-grounding reads as one coherent argument rather than two.

[A-min-46] (A-related) decoder_paper_v2.tex, line 750 ('Positioning: technology readiness and target verticals', ISO 14649 / STEP-NC sentence) :: related-work
ISSUE: STEP-NC (ISO 14649) is introduced only in the Discussion's positioning paragraph as the 'long-run modernization path,' framing the present decoder as a 'back-compatible monitor for the installed G-code base during that transition.' This is a reasonable framing, but it appears for the first time on p. ~Discussion with no setup in Related Work. The relationship between sensor-based execution verification and the STEP-NC vision of machine-checkable program-level intent is a genuine positioning axis (STEP-NC would make some of this monitoring unnecessary, but only after a multi-decade installed-base transition) and currently reads as a bolt-on.
FIX: Add one sentence to Section 4.4 (Manufacturing Cybersecurity and Process Verification) introducing STEP-NC as the standards-track alternative to externally verifying G-code execution, and note that it remains aspirational for the installed CNC base — so a sensor-side monitor is a near-term complement. Then the Discussion sentence becomes a callback rather than a first introduction.

[A-min-47] (A-claims) decoder_paper_v2.tex:159 (related-security) 'physics-of-record' :: claim/rigor
ISSUE: The phrase 'out-of-band physics-of-record monitor' is introduced in Related Work (line 159) with the caveat 'its guarantee holds only insofar as the sensor path itself is trustworthy.' The supplement (line 1378) is even firmer: 'A deployment that does not enforce these operational mitigations should not invoke the physics-of-record framing.' Yet the threat model (line 260) concedes the trusted-sensor properties 'are operational requirements ... not implemented in the present prototype; the laboratory acquisition pipeline ... uses unauthenticated USB sensor links and is not itself a deployment-grade trust boundary.' So by the paper's own statement the present prototype does NOT meet the precondition for the physics-of-record framing. Using the term at all, even hedged, slightly overstates the present system's trust posture.
FIX: Either drop 'physics-of-record' as a descriptor of the present system and reserve it strictly for the hypothetical deployment that enforces the sensor-path mitigations, or add at line 159 the same explicit statement that the present prototype's unauthenticated USB acquisition does not yet earn the term.

[A-min-48] (A-claims) decoder_paper_v2.tex:264 / :773 (threat model attack class (v) upstream CAM) :: consistency
ISSUE: Attack class (v) 'upstream CAM/post-processor compromise' is listed among the five 'Attack classes considered' in the threat model (line 264) and appears in the MITRE mapping (T0862 Supply Chain Compromise). But Section disc-threat-model (line 773) states the upstream-CAM adversary 'whose substituted toolpath stays in-vocabulary is invisible to the decoder ... the present system is ... not an upstream-design-integrity monitor.' So attack class (v) is enumerated as 'considered' but is actually entirely out of scope/undetectable. Listing it under 'Attack classes considered' alongside detectable classes without an immediate scope flag is mildly misleading.
FIX: In the threat-model attack-class list (line 264), annotate (v) inline as out-of-scope for the present detector (forward-ref to disc-threat-model), so the 'considered' list distinguishes attack classes the detector addresses from those enumerated only for completeness.

[A-min-49] (A-claims) decoder_paper_v2.tex:431 / :849 (4 Hz 77.7% sub-sample claim) :: claim/rigor
ISSUE: The 77.7% <=1-sample-period figure (gcode_row_durations.json, 21,541 instances) is load-bearing for the 'window-aggregated row-modal recovery' reframing of RQ1 (line 100) and the numeric ceiling. It is used consistently. One subtle gap: the paper draws an information-theoretic 'sub-Nyquist floor' conclusion from this (numeric not recoverable 'by an information-theoretic bound', line 88), but a >=1 sample-period row is not strictly Nyquist-unresolvable for a step-like (not oscillatory) event; the envelope still carries which-row-fired information at window granularity (which is exactly why categorical recovery works). The 'information-theoretic bound' language is borrowed from the companion paper and is slightly stronger than what 77.7%-<=1-period alone establishes for the present paper standalone.
FIX: Soften standalone phrasing to 'temporally unresolvable as distinct sub-window transients at 4 Hz' (which the data show) and reserve the formal 'information-theoretic / sub-Nyquist bound' attribution explicitly to the companion paper, rather than stating it as a settled bound in the present paper's own voice (lines 88, 775).

[A-min-50] (A-claims) decoder_paper_v2.tex:486 vs :910 (Conclusion) on 'unsupported' coordinate reconstruction :: consistency
ISSUE: The paper states generative coordinate reconstruction is 'unsupported on this stack' (lines 75, 106, 486, 910) yet Table per_axis reports per-axis value MAE for Z (0.009 in) and R (0.021 in) that are quite small, and the body (line 522) calls the decoder a 'coarse-resolution coordinate recoverer.' These are not contradictory (Z/R have low source entropy and the MAE is over present-axis samples only), but the absolute 'unsupported' framing sits awkwardly next to a 0.009-inch Z MAE. A naive reader could see Table per_axis and conclude coordinate recovery IS supported for Z.
FIX: Where 'coordinate reconstruction unsupported' is stated, add the qualifier that it refers to end-to-end multi-axis exact-coordinate reconstruction (AR sequence exact-match 0.026), and that per-axis Z/R value MAE is small only because those axes are near-deterministic / low-entropy (Section per-axis), not because fine coordinate recovery generalizes.

[A-min-51] (A-claims) decoder_paper_v2.tex:494-499 (Fig five_fold_spread) and :501 :: consistency
ISSUE: Figure five_fold_spread caption and line 501 attribute the command-head fold-1 outlier (0.776 vs folds 2-5 at 0.91-0.92) to 'the -4.4 pp Z-coverage covariate shift audited in Section lim-covariate.' The covariate-shift audit (extended_rigor_audits.json per the text) flags per-row fold-1 Z -4.4pp. But the headline/per-axis tables are full-window, and the confound section (line 733) attributes the same fold-1 outlier to being 'the covariate-shift outlier.' The causal attribution of a 14pp command-accuracy drop on fold 1 to a 4.4pp Z-presence shift is asserted but not quantitatively demonstrated (a 4.4pp shift in one axis's presence support producing a 14pp command-accuracy collapse is a large amplification). This is a plausible-but-unverified mechanistic claim repeated in several places.
FIX: Either soften to 'coincides with' rather than causal attribution, or add a sentence/supplementary pointer quantifying how the Z-coverage shift propagates to a 14pp command drop (e.g. via the fold-1 modal-command differing — confound_class_prior.json shows fold-1 face150025 modal is G3 vs G2 elsewhere, a more direct candidate cause than Z-coverage).

[A-min-52] (A-claims) decoder_paper_v2.tex:616 / :620 (no-numeric attribution downgrade) :: claim/rigor
ISSUE: The no-numeric ablation reports +8.8pp AR struct-token improvement (paired t=4.02, p<0.02, 5/5 positive), then immediately downgrades it: 'A matched-methodology control ... shows the numeric-vocabulary collapse contributes no additional lift beyond the training-schedule change T3.1 already absorbs, downgrading the standalone numeric-desynchronization contributes ~9pp framing.' This is honest but the body still states the +8.8pp prominently (line 599, 616) before the downgrade, and the decomposition language ('decomposes into numeric-desynchronization ... and dominant intrinsic collapse') survives in the conclusion-adjacent text. If the +8.8pp is actually attributable to the SS/dw schedule change (T3.1), then the 'numeric-token desynchronization' component may be near zero, not 'a measurable contribution.' The two statements are in tension within the same paragraph.
FIX: Reconcile: state the +8.8pp once, then commit to the post-control interpretation throughout, e.g. 'the apparent +8.8pp numeric-desynchronization contribution is absorbed by the SS=0/dw=0 schedule change (T3.1 control); the deployment-relevant conclusion is the dominant intrinsic structural-stream collapse, with the numeric-desync component not separately established.' Avoid presenting +8.8pp as 'a measurable numeric-desynchronization contribution' in one sentence and 'no additional lift' in the next.

[A-min-53] (A-claims) decoder_paper_v2.tex:636 / :640 (sensor-modality 'joint necessity' vs LOMO) :: claim/rigor
ISSUE: The frozen-encoder modality ablation is correctly characterized as a 'gating artifact' that 'cannot rank importance,' and the LOMO retraining shows no single modality is load-bearing (<=1.3pp). This is handled with admirable honesty. The only residual tension: the frozen-encoder result (40-44pp uniform collapse) is still presented first and at length, and is the basis for the deployment 'joint necessity' language reused in the abstract-of-conclusion item (3) ('Inference-time modality removal collapses command accuracy uniformly'). Since the paper itself calls the frozen-encoder collapse an artifact, leading the conclusion's sensor-redundancy bullet with the artifact result before the real (LOMO) result inverts the epistemic ordering.
FIX: In Conclusion item (3), lead with the LOMO/LOSO retraining result (the real finding: no single modality/unit is load-bearing, <=2.6pp) and present the frozen-encoder uniform collapse as the contrast/artifact, matching the body's own framing that the retraining result is deployment-relevant and the frozen-encoder result is a gating artifact.

[A-min-54] (A-claims) decoder_paper_v2.tex:75 (Abstract) 'floor of 0.213' :: statistics
ISSUE: The abstract reports the LOCO command floor as '0.213' as a point estimate without its dispersion, while the body (line 106, 683) reports 0.213 +/- 0.191 and notes the 9-class spread sigma=0.191 reflects between-class heterogeneity (one outlier damagepocket=0.678; eight in [0,0.273]). A bare '0.213' floor in the abstract slightly understates how concentrated-at-zero the distribution is (median 0.21, but with one large outlier pulling the mean up). Verified against loco_per_class_9holdouts.json.
FIX: Either report '0.213 (median 0.21; eight of nine classes <=0.27)' or '0.213 +/- 0.191' in the abstract so the floor is not read as a tight estimate; the current bare point estimate is the least informative summary of a highly skewed 9-point distribution.

[A-min-55] (A-claims) decoder_paper_v2.tex:799-800, 805 (feed-rate BA rows) :: figure/table
ISSUE: Table tamper_auc reports feed-rate BA 0.61 (baseline) at TPR 0.216/FPR 0.0055 in-distribution and BA 0.50 'degenerate, n=45' TPR 0.000/FPR 0.000 under LOCO. The verified JSON (threat_model_tamper_loco.json feed_edit) gives tpr 0.0, fpr 0.0, n=45 — consistent. But a BA of 0.61 with TPR 0.216 means the detector misses ~78% of feed-rate tampers; presenting BA=0.61 (which sounds 'above chance') without immediately noting TPR=0.22 in the same glance could let a skimming reader overrate feed-rate detectability. The prose (line 817) does call this an 'in-distribution artifact,' but the table line stands alone.
FIX: Add a one-word table footnote marker on the feed-rate BA rows pointing to the prose caveat (TPR 0.22 in-distribution, 0.00 LOCO; in-distribution low FPR is an artifact), so the BA value is not read in isolation as 'above chance.'

[A-min-56] (A-claims) decoder_paper_v2.tex:88 (Introduction, cost-envelope framing) :: scope/framing
ISSUE: The introduction invokes 'scrapped parts, damaged tooling, or worse' as the cost envelope a wider sensor stack 'could in principle catch,' explicitly labeled 'framing for future cross-platform replication, not a claim of the present system.' This is honest, but it is the same high-value attack class (depth/feed) that the adaptive-adversary section (line 819) shows is invisible by construction on the present stack. Motivating the paper with a cost envelope the system structurally cannot address risks a reader feeling the motivation is bait-and-switch even though each individual sentence is hedged.
FIX: Tighten the intro motivation to lead with what the present system DOES bound (in-envelope command/axis/sign substitution forensic audit) and present the scrapped-part/feed cost envelope explicitly as the motivation for the future higher-rate stack, so the gap between motivation and capability is signposted at the point of motivation, not only resolved 30 pages later in the adaptive-adversary paragraph.

[A-min-57] (A-related) decoder_references.bib, entries faezi2020oligosnoop (l.821) and goh2017dataset (l.286) :: related-work
ISSUE: Two bib entries are defined but never \cite-d anywhere in the main paper, supplement, or appendix (grep confirms zero references). faezi2020oligosnoop (Oligo-Snoop, a non-invasive side-channel against DNA-synthesis machines) and goh2017dataset (SWaT water-treatment dataset) are dead weight in the .bib. BibTeX will silently drop uncited entries so the build is unaffected, but they signal incomplete cleanup after the two-paper split and clutter the source for co-authors/reviewers who read the .bib.
FIX: Either delete both entries, OR — more valuably for faezi2020oligosnoop — actually cite it. Oligo-Snoop is a strong precedent for 'instruction-level recovery from a manufacturing machine's physical side-channel beyond 3D printing' and would broaden the side-channel framing in Section 4.3 beyond the FDM-only set, reinforcing that the per-instruction-recovery question is live across CPS manufacturing, not just AM.

[A-min-58] (A-figtab) supplementary_materials.tex Figure fig:reliability_diagram (126-131) and Section S2 calibration text :: figure/table
ISSUE: The reliability_diagram is fold-1-only (the outlier fold) and the caption correctly flags it as illustrative of the worst fold, with deployment-relevant figures being cross-fold means in the text. Good. However there is no figure for the cross-fold (deployment-relevant) calibration result — only the fold-1 worst-case is plotted. A reader gets a figure for the number the authors say NOT to rely on, and only prose for the number they say IS deployment-relevant.
FIX: Consider adding the cross-fold ECE (e.g. a small bar or box plot of per-fold ECE before/after temperature scaling for the three heads) so the figure supports the deployment-relevant cross-fold claim, not only the fold-1 outlier. At minimum, state in the caption that the cross-fold means (the figures that matter) are in the text/calibration_cross_fold.json and intentionally not plotted.

[A-min-59] (A-figtab) supplementary_materials.tex Section S 'Per-Operation-Class Slot-5 Stratification' Table tab:gcode_examples (lines 320-339) and Figure fig:slot5_by_op_class (313-318), fig:gcode_line_example (341-346) :: figure/table
ISSUE: This single supplement subsection carries three artifacts (a slot5_by_op_class heatmap-style figure, a representative-G-code-rows table, and a per-position prediction-vs-target plate gcode_line_example) all illustrating the same three-regime slot-5 story whose load-bearing numbers are stated to live in the companion limit paper (paperB_entropy). For a Sensors decoder paper whose own thesis is that ten-thousandths recovery is a tokenizer artifact (not information-bearing), three figures/tables on slot-5 op-class stratification is heavy for a result the paper explicitly defers to the companion.
FIX: Consider consolidating to one figure (slot5_by_op_class) plus the representative-rows table, and dropping or merging the gcode_line_example plate, since the paper's own framing is that slot-5 is a structural zero / artifact and the full entropy account belongs to the companion paper. Alternatively, if all three stay, add one sentence stating why three artifacts are warranted for an explicitly-deferred result.

[A-min-60] (A-figtab) supplementary_materials.tex Table tab:outpos (lines 754-776) :: figure/table
ISSUE: Table S (outpos) and the immediately following Figure S (fig:outpos, token_position_curve) present the same per-output-position accuracy data (per-row vs full-window) in table and curve form on the same page. The figure caption and the table caption both state the position-1 0.24->0.62 lift. This is table+figure redundancy for one dataset.
FIX: Keep the figure (the curve makes the position-1 dip visually obvious) and either drop the table or reduce it to the handful of positions the text discusses (0,1,2,10), since the full position list is in the released JSON. Avoid stating the +38pp lift identically in both captions.

[A-min-61] (A-figtab) tables/ directory: per_class_token.tex, per_class_token.csv, per_class_type.tex, per_head_entropy.tex, per_head_metrics.tex, per_head_metrics.csv (and per_head_source_entropy artifacts) :: presentation
ISSUE: Several table source files exist in tables/ but are never \input or \ref'd in either decoder_paper_v2.tex or supplementary_materials.tex (verified: only headline_5fold, per_class_command, per_class_param_type, data_coverage, per_axis_recoverability, ablations_summary, anova, bootstrap_ci, nonumeric, sensor_ablation, lomo_modality, seq2seq_baseline, lomo_sensor, lomo_nested, k_spectrum, per_digit_position are used). per_class_token, per_class_type, per_head_entropy, per_head_metrics are orphaned. This is release-hygiene clutter; a reviewer who is given the source tree may wonder whether a table was dropped by accident.
FIX: Either delete the orphaned .tex/.csv files from tables/ before the Zenodo/GitHub release, or confirm they are intentionally part of the released artifact bundle and add a note in the replicability checklist that tables/ contains both rendered and auxiliary CSV exports. The per_class_token table in particular (token-head per-class metrics) could plausibly be a useful supplement table if the authors meant to include it.