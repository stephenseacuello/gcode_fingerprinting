# PAPER B counts={'total': 150, 'substantive': 20, 'moderate': 70, 'minor': 60} deduped=150

============================================================
## SUBSTANTIVE (20)
============================================================

[B-sub-1] (B-figtab) figures/entropy_per_toolpath.pdf (Figure, \label{fig:entropy_per_toolpath}, tex line 238-243) :: figure/table
ISSUE: Hard legibility defect: the centered matplotlib title 'Per-toolpath empirical source entropy decomposed by head' physically overlaps/overprints the three colored family-group labels ('FACE family', 'POCKET family', 'ADAPTIVE family') drawn at the top of the axes. The words collide (e.g. 'POCKET family' over '...entropy decomposed by head'; 'ADAPTIVE family' over the title and near the right clip edge), making both the title and the family annotations unreadable at print size.
FIX: Regenerate the figure: move the family labels below the title (e.g. as a second annotation row with vertical offset), or drop the embedded title entirely (the LaTeX caption already states the content) and keep only the FACE/POCKET/ADAPTIVE group labels, or use subplot column headers. Add vertical headroom (tight_layout/ subplots_adjust(top=...)).

[B-sub-2] (B-it) toolpath_entropy.tex Sec. 5 line 296: 'the entropy is the model-side tokenized per-(axis, slot) entropy ... and the accuracy is the patched recovery accuracy, so the two are paired over exactly the digits the model can represent (the X/Y/Z 10^-4 cell enters as the tokenizer's 0-bit constant, recovered near-perfectly)' :: statistics
ISSUE: The lead n=30 statistic (r=-0.83, rho=-0.91) INCLUDES the three X/Y/Z ten-thousandths cells, each entering as (entropy=0, accuracy~1.0). These three points sit at the extreme low-entropy/high-accuracy corner and are tokenizer artifacts by the paper's own argument (Sec. 3). They mechanically strengthen a negative correlation: a constant-target cell trivially recovered is not evidence that low source entropy enables recovery — it is evidence the tokenizer discarded the digit. The headline improvement from r=-0.68 to r=-0.83 may be partly leverage from these three corner points. The correlation should be reported both with and without the tokenizer-collapsed cells.
FIX: Report the n=30 Pearson/Spearman alongside an n=27 version excluding the three X/Y/Z 10^-4 tokenizer-constant cells (and ideally the F integer-collapsed cells), to demonstrate the negative ordering is driven by genuine source structure (units->thousandths gradient) and not by leverage from tokenizer-zeroed corner points. Even if the coefficient softens, this is the honest core and the paper's existing humility ('ordering, not a bound') supports presenting it.

[B-sub-3] (B-cam) toolpath_entropy.tex line 114 (Related Work, CAM section, ISO 230-2 sentence) :: claim/rigor
ISSUE: The causal claim 'for a desktop platform the controller resolution is far coarser than the four decimals the postprocessor writes, which is exactly why the finest emitted digit (the thousandths) approaches uniform noise' is a physical non-sequitur. A controller whose positioning resolution is coarser than the thousandths would SNAP/ROUND emitted coordinates to its resolution grid — that produces deterministic, low-entropy clustering at the affected digits, NOT uniform noise. Uniformity of the source thousandths digit is a property of the CAM optimizer emitting effectively-continuous fractional coordinates (engagement-controlled toolpaths), which the paper itself correctly states at lines 114 (first half), 251, and 372. The ISO-230-2/controller-resolution sentence contradicts the paper's own (correct) CAM-emission mechanism and conflates execution-side positioning accuracy with source-side coordinate statistics.
FIX: Delete or rewrite the clause so it does not attribute thousandths-digit uniformity to controller resolution. E.g.: 'Machine-tool positioning accuracy and repeatability (ISO 230-2 [iso230_2]) bound the physically meaningful coordinate precision and are typically far coarser than the four decimals the postprocessor writes; the finest emitted digits are therefore largely below the machine's executional resolution. Their near-uniform source distribution, however, arises on the CAM side — the engagement-controlled optimizer emits floating-point coordinate residuals — not from controller rounding.' Keep the (valid) point that the finest digits are physically unresolvable, but stop deriving uniformity from resolution.

[B-sub-4] (B-cam) toolpath_entropy.tex line 125 (Corpus paragraph), line 96 (Scope statement), line 427 (Field coverage), and abstract/keywords :: claim/rigor
ISSUE: The paper repeatedly states spindle-speed (S) variation is 'effectively absent' / 'no spindle-speed (S) variation' and that 'Entropy figures for S ... are not estimable.' This is physically imprecise and slightly wrong. Every one of the nine source files contains exactly one spindle command, M3 S12000 (verified: 1 S-token per file across all 9 files). Spindle speed is therefore PRESENT and CONSTANT, not absent. Its source entropy is well-defined and equals 0 bits (deterministic), which is exactly the kind of clean endpoint the paper's own framework prizes (a 0-bit slot carries no recoverable information). Calling it 'absent' or 'not estimable' misstates the corpus and understates the result: S is the cleanest possible deterministic field. The same 0-bit-but-present logic already applies to R/F sign and tool number.
FIX: Replace 'spindle-speed (S) variation is effectively absent' with 'spindle speed is held constant at a single commanded value (S12000) across the corpus, so its source entropy is identically 0 bits (a deterministic field).' In Field coverage, change 'Entropy figures for S ... are not estimable' to 'S is single-valued (S12000) and contributes 0 bits; only I/J are genuinely absent and hence not estimable.' This converts a vague 'absence' into a precise 0-bit endpoint that reinforces the paper's thesis.

[B-sub-5] (B-cam) toolpath_entropy.tex line 257 ('Z carries cutting-depth signal'), line 306 ('Z reaches 0.98 at the tenths (cutting-depth quantization)'), line 349 ('Z’s tenths digit recovers ... from cutting-depth quantization'), Fig captions lines 322/372, and Conclusion line 437 :: claim/rigor
ISSUE: The recurring interpretation that Z's low tenths-slot entropy (0.64 bits) reflects 'cutting-depth quantization / cutting-depth steps' is physically mischaracterized. The Z tenths digit distribution is 87.2% '0' and 11.3% '1' (verified in raw_gcode_source_entropy.json per_axis.Z.slots['2']). The '0' mass comes from rapids/retract and shallow moves (Z0., Z0.2, Z0.6) and the '1' from cut moves (Z-0.1xx). The low tenths entropy therefore reflects that the part is SHALLOW (all |Z| < ~0.2 inch) and the rapid-vs-cut bimodality — not depth-of-cut stepping. The actual cutting-depth per-pass steps (e.g. Z-0.1238, Z-0.1247, Z-0.1244, Z-0.1202 ...) live in the hundredths/thousandths, which are at the uniform ceiling (Z hundredths 3.23 bits, thousandths 3.31 bits) — i.e. the depth steps are NEAR-UNIFORM and NOT recoverable. So 'cutting-depth quantization' is what makes Z UNrecoverable at the fine digits, the opposite of what the captions imply.
FIX: Reframe Z's recoverable structure as a shallow-depth / rapid-vs-cut bimodality at the tenths, not 'cutting-depth quantization.' E.g.: 'Z’s tenths digit is low-entropy (0.64 bits) because every move is shallow (|Z|<0.2 in): the digit is 0 for rapids/retracts and 1 for cuts. The depth-of-cut per-pass increments themselves fall in the hundredths–thousandths and are at the uniform ceiling, so it is the coarse shallow-range structure, not the fine depth stepping, that Z recovers.' Update Fig captions 322/372 and lines 257/306/349/437 accordingly.

[B-sub-6] (B-stats) toolpath_entropy.tex line 300 ('On the numeric heads alone (sign + digit) the relationship is strong and within-head (r approx -0.9)'); verified against entropy_accuracy_correlation_fixed.json within_head + numeric_18 :: statistics
ISSUE: The claim that 'on the numeric heads alone (sign + digit) the relationship is strong and within-head (r approx -0.9)' is misleading. The artifact shows: within-head digit r=-0.895 (genuinely strong), but within-head sign r=-0.189 (essentially no relationship), and within-head command r=+0.384 (POSITIVE). The pooled sign+digit value of -0.87 (numeric_18) is carried almost entirely by the digit head; the 'sign' part contributes near-zero/positive. Calling the sign+digit relationship 'within-head r approx -0.9' conflates a pooled cross-head-flavored statistic (sign and digit are different heads with different entropy ranges, so pooling them reintroduces a cross-head contrast) with a true within-head bound. This is the same cross-head-vs-within-head distinction the paragraph is otherwise careful to make.
FIX: State precisely: 'Within the digit head alone the relationship is strong (r=-0.90, n=9); the sign head alone is essentially flat (r=-0.19), and pooling sign+digit (r=-0.87, n=18) recovers a between-head contrast. The single genuinely within-head negative slope is the digit head.' Do not attribute r approx -0.9 to 'the numeric heads' collectively.

[B-sub-7] (B-salami) toolpath_entropy.tex line 92 (Contribution 4) and line 296 (lead result); cf. tables/per_digit_position vs Limitations line 427 :: consistency
ISSUE: The lead 30-cell relationship is repeatedly called "30 well-supported cells" (Contribution 4, line 92; Section 7 lead, line 296). Verified against patched_numeric_analysis.json: the 30 cells are exactly 5 fields (X,Y,Z,F,R) x 6 slots, i.e. F's six slots are INCLUDED. But the Limitations (line 427) state F has thin support (n=16 raw / 159 windowed) and is "noisier than the X/Y/Z core," and the related-work and abstract elsewhere downgrade F. Calling the set "well-supported" while it contains the field the paper itself flags as under-supported is an internal tension a careful reviewer will catch.
FIX: Either drop the "well-supported" qualifier on the 30-cell set and instead say "30 per-(axis, slot) cells (five fields x six slots, F at thin support; see Section 11)", or report the correlation with and without F's cells (n=24) so the reader can see F is not driving r=-0.83. A one-line robustness note ("excluding F's thin-support cells, r=...") would fully resolve it.

[B-sub-8] (B-write) toolpath_entropy.tex lines 127, 129, 152, 386 (repeated 'structural zero' explanation) :: structure
ISSUE: The single most-repeated substantive point in the paper is the X/Y/Z ten-thousandths source-vs-tokenizer 'structural zero is a representation choice' argument. It is stated at length in the abstract (line 68), Section 3 quantization spec (lines 127, 129), Section 4 claim (iii) (line 147), the source-vs-tokenized paragraph (line 152), Table captions (lines 156, 184), claim text (line 180), three figure captions (lines 262, 269, 276), the bathtub paragraph (line 306), figure 7 caption (line 322), Section 7 (line 386), Limitations (line 421), and the Conclusion (line 437). The reader is told ~13 times that the 0.001 grid discards the ten-thousandths and R retains it. This is the dominant redundancy in the body.
FIX: Designate Table 1 (source_vs_tokenized) + its one explanatory paragraph as the canonical statement. In every figure caption and the bathtub/Section 7/Limitations/Conclusion mentions, compress to a parenthetical cross-reference '(tokenizer-discarded; Table 1)' rather than re-explaining the mechanism. This alone could cut ~half a page.

[B-sub-9] (B-write) toolpath_entropy.tex lines 79, 81, 83, 84, 85 (Introduction) :: structure
ISSUE: The Introduction is heavily front-loaded with redundancy. The 'Fano / marginal H(X) not conditional H(X|obs) / not a tight bound / two endpoints / empirical ordering' argument is stated in full at least four times before Section 2: para 1 (line 79), the contributions framing (line 81), the 'Why this is an information-theoretic question' paragraph (line 83), and the 'salami-defense' paragraph (line 85). It then recurs again in Related Work (line 109), the Ceiling claim (line 294), and Limitations (line 429). A reader hits the same caveat ~7 times. This is the single biggest source of bloat in the paper.
FIX: State the marginal-vs-conditional caveat once, crisply, in the Introduction (it belongs in the 'Why this is an information-theoretic question' paragraph). Reduce the para-1 and contributions-framing instances to a single clause cross-referencing it. In Related Work and the Ceiling section, replace the full restatement with a one-line pointer ('as noted in Section 1, we measure H(X), not H(X|obs)').

[B-sub-10] (B-stats) toolpath_entropy.tex lines 92, 296, 298, 338, 339, 345, 388, 437 (every reported r/rho); checked audit/patched_numeric_analysis.json + entropy_accuracy_correlation_fixed.json :: statistics
ISSUE: No confidence interval is reported for ANY of the headline correlations (n=30 r=-0.83, n=6 pooled r=-0.89, n=54 cross-head r=-0.68, n=18 numeric r=-0.87). At n=30 the 95% CI on a Pearson r=-0.83 via Fisher z is roughly [-0.92, -0.67]; at n=6 the CI on r=-0.89 is enormous (roughly [-0.99, -0.05]) and effectively uninformative. The project already computes bootstrap CIs elsewhere (audit/bootstrap_ci.json, per_axis_bootstrap_ci.json, loco_bootstrap_ci.json), so omitting them here is conspicuous and weakens an otherwise careful statistics presentation. A point estimate of a correlation without a CI is exactly the kind of thing an Entropy reviewer flags.
FIX: Add Fisher-z or bootstrap 95% CIs to the lead n=30 statistic and (at minimum) note the n=6 CI is uninformatively wide as further justification for the 'illustrative only' label. E.g. 'Pearson r=-0.83 (95% CI [-0.92,-0.67] by Fisher z, n=30; bootstrap CI ...).' Even one CI on the load-bearing n=30 number materially hardens the claim.

[B-sub-11] (B-write) toolpath_entropy.tex lines 92, 296, 437 (and Table per_head_entropy line 22) :: consistency
ISSUE: The cell-level scatter is described inconsistently as 'Spearman rho=-0.91' (line 92, contribution 4) but the abstract (line 68) and the lead-result paragraph (line 296) and conclusion (line 437) all give the cell-level pair as 'r=-0.83; Spearman rho=-0.91'. Contribution (4) at line 92, however, writes 'r=-0.83, Spearman rho=-0.91' for the 30-cell figure but separately the figure 6 caption / contribution text mention rho. More importantly, line 92 lists the convergence claim under contribution (4) but the 30-cell rho appears as -0.91 there while the audit JSON confirms -0.91 — consistent. The genuine inconsistency: command-head TF accuracy is reported as 0.888 in the abstract/intro/seq2seq table but as 0.891 in the per_head_entropy table (line 22) and at line 386 ('command 0.891 vs 0.888 patched vs released'). The paper uses 0.888 (released) in headline contexts and 0.891 (patched) in the per-head table without a one-line reconciliation at each point of use.
FIX: Add a single footnote or parenthetical the first time both appear together (it exists at line 386 but not where 0.888 is used in the abstract/Section 6). At minimum, in the Section 6 convergence paragraph (line 302) where '0.888 decoder' is quoted, add '(0.891 on the patched retrain; the categorical heads are TF/AR-invariant and drift <=1.5pp under the patch).'

[B-sub-12] (B-it) toolpath_entropy.tex throughout (e.g. lines 68, 81, 152, 437): 'recoverability LIMIT' / 'precision floor' language vs. the explicit 'ordering, not a tight bound' hedge :: scope/framing
ISSUE: There is a persistent tension between the headline framing ('recoverability LIMIT', 'the genuine information-theoretic precision floor', title 'sub-Nyquist Floor') and the carefully correct statistical hedge ('we measure marginal H(X), not a Fano bound; we report an empirical ordering'). The marginal source entropy H(X) is NOT an upper bound on recovery accuracy in either direction: a high-H(X) digit can in principle be perfectly recoverable if the channel is informative (H(X|obs)=0), and a low-H(X) digit can be unrecoverable if the channel is uninformative. So 'fine decimals at 3.3 bits = the precision floor / unrecoverable' is a statement about the SOURCE's intrinsic variability, NOT an information-theoretic limit on what a sensor could recover — that would require H(X|obs) or I(X;obs). The word 'floor'/'limit'/'ceiling' applied to H(X) implies a bound that only H(X|obs) can supply.
FIX: Globally reserve 'limit/floor/bound' for the two quantities that genuinely are limits (the 0-bit slots: H(X)=0 => unrecoverable-by-construction-as-constant; and the sub-Nyquist observability floor). For the 3.3-bit fine-decimal plateau, consistently say 'the source carries maximal (near-uniform) variability here, so absent a directly informative channel a recoverer is left at the prior' — i.e. frame it as a necessary-not-sufficient ceiling conditional on the channel being uninformative about the fine digit. The paper already says this in places (lines 83, 146, 296); make the title and abstract match. Specifically reconsider 'genuine information-theoretic precision floor' (lines 127, 146, 437) since H(X) alone is not such a floor.

[B-sub-13] (B-critic) toolpath_entropy.tex, Sec. 6 lead result (line 296) and abstract (line 67) :: statistics
ISSUE: The load-bearing claim — recovery accuracy is inversely ordered by source entropy (r=-0.83, n=30 cells) — is never tested against confounds that co-vary with entropy: per-slot/per-class symbol frequency, per-cell sample size n, window/block length, and the high line-level train/test overlap. A skeptical reviewer can argue the ordering partly reflects 'frequent/short/memorized slots are easy' rather than 'low-entropy slots are easy.' The audit directory already contains confound_class_prior.json and per-row duration/length data that could support a control.
FIX: Add a partial-correlation or two-predictor regression of accuracy on (entropy, log n) or (entropy, modal-fraction), and report whether the entropy coefficient survives controlling for the nuisance. One sentence ('the entropy slope is robust to controlling for per-cell support and slot frequency, partial r=...') would convert an associational claim into a defended one.

[B-sub-14] (B-critic) toolpath_entropy.tex, Sec. 8 Audit-Leverage Corollary (lines 411-413) :: scope/framing
ISSUE: The audit-leverage corollary is the paper's only forward-looking, actionable contribution (it is even foregrounded in the abstract's 'the question underlying any sensor-side forensic control'), yet it is entirely qualitative: it asserts that low-entropy op-classes concentrate mass on a few values so deviations are 'statistically conspicuous and ideal for sensor-side audit,' with no quantitative measure of that conspicuousness and no detector. The supporting premise is directly measurable and an artifact already computes it — audit/class_conditional_modal.json reports an 0.81 (±0.018) class-conditional modal-command predictor across 5 folds — but this number appears nowhere in the paper, and the facing-X modal masses (27%/21%/~7%/~7%, ~62% in four values) are asserted in-text without a table or figure.
FIX: Either (a) add a small table/figure of per-op-class modal-coverage (fraction of coordinate mass in top-k values) plotted against per-op-class source entropy, citing audit/class_conditional_modal.json and audit/raw_gcode_source_entropy.json, to demonstrate the audit-leverage gradient empirically within this paper; or (b) explicitly label the corollary an untested design hypothesis ('We do not validate a detector here; the corresponding tamper operating points are reported in [paperA].') so a reader does not read it as a result.

[B-sub-15] (B-stats) toolpath_entropy.tex, Sec. ceiling, line 296 (lead result) + abstract line 68 + contribution (4) line 92 + conclusion line 437; verified against audit/patched_numeric_analysis.json corrected_per_axis_slot_entropy + patched_per_axis_slot :: statistics
ISSUE: The load-bearing lead statistic (Pearson r=-0.83, Spearman rho=-0.91, n=30) is computed over 30 per-(axis,slot) cells, but 13 of those 30 cells (43%) are zero-entropy tokenizer-constant cells recovered near-perfectly (entropy approx 0, accuracy approx 1.0): X-s0, X-s5, Y-s0, Y-s5, Z-s0, Z-s1, Z-s5, F-s2/3/4/5, R-s0/s1. These are high-leverage points clustered at the (0 bits, ~1.0 acc) corner that mechanically anchor the negative slope. Recomputing after removing the 10 trivially-recovered constant cells (entropy<0.01 AND acc>0.99) gives Pearson r=-0.77 / Spearman -0.86 (n=20); removing all near-zero-entropy cells gives r=-0.78 / -0.80 (n=17). The paper presents -0.83 as 'markedly stronger' than the pre-patch -0.68 and 'the honest empirical core,' but a large share of the strengthening comes from the X/Y/Z slot-5 ten-thousandths flipping from released accuracy 0.769 to patched 0.993 (delta +0.224, bathtub artifact in patched_numeric_analysis.json) — i.e., the tokenizer made a known-constant slot recover better, which is not the substantive fine-digit ordering. A reviewer who recomputes will see this.
FIX: Report the correlation with and without the tokenizer-constant (0-bit) cells, and explicitly state the relationship is dominated by the contrast between deterministic and high-entropy slots. Add one sentence: 'Excluding the 10 zero-entropy tokenizer-constant cells (which enter trivially at ~1.0 accuracy), the relationship over the 20 informative cells is r=-0.77 (rho=-0.86), so the ordering is not an artifact of the deterministic endpoints but is somewhat attenuated when restricted to slots the source actually varies.' Reframe the 'markedly stronger than -0.68' claim to note that part of the gain is the slot-5 constant flip under the patch, not the fine-digit relationship per se.

[B-sub-16] (B-salami) toolpath_entropy.tex, Section 7 line 300 ('The ordering is cross-head, not within-head') vs Contributions/abstract framing of the ordering :: claim/rigor
ISSUE: The honest and important disclosure at line 300 — that the headline negative entropy-accuracy ordering is CROSS-head (driven by the contrast between a near-ceiling flat categorical head and a high-entropy numeric head), not a within-head bound — somewhat undercuts the abstract/Contribution-4 framing, which presents 'recovery accuracy is inversely related to entropy (r=-0.83)' as the clean result. The r=-0.83 is computed over per-(axis,slot) cells (so it is genuinely within the numeric/coordinate fields, which is fine), but the r=-0.68 54-cell version (Figure entropy_accuracy_all_heads) IS the cross-head one whose slope is partly a categorical-vs-numeric artifact. The paper distinguishes these but the abstract cites only the favorable -0.83/-0.89 and does not signal that the broader cross-head relationship is partly compositional. A skeptical reviewer could read the strongest-stated correlation as cherry-picked from the cell set that excludes the cross-head confound.
FIX: In the abstract and Contribution 4, anchor explicitly on the within-coordinate-field interpretation of the n=30 cell correlation (it pairs entropy and accuracy over the five numeric fields x six slots, a within-coordinate relationship) and note in one clause that the broader per-head ordering is cross-head/compositional. This pre-empts the 'which correlation is load-bearing and why' question and makes clear the -0.83 is not the confounded cross-head slope.

[B-sub-17] (B-salami) toolpath_entropy.tex, Section 7 line 302 and Section 11 line 431; whole-paper TF/AR framing :: scope/framing
ISSUE: The paper's independent theoretical contribution rests almost entirely on TEACHER-FORCED accuracy (the entropy-accuracy ordering, the bathtub, r=-0.83 are all TF). The paper is admirably explicit that AR numeric recovery collapses to ~0.10 (lines 376, 431), but this means the 'recoverability LIMIT' that B claims to own is, in deployment, dominated by an exposure-bias mode-collapse that B attributes entirely to the companion decoder (cite paperA). For an Entropy paper claiming an information-theoretic recoverability characterization, the central confirmation being a TF upper bound — whose deployment gap is explained only by reference to Paper A's decoder dynamics — is the strongest place B leans on Paper A. A reviewer may ask: is the 'ordering' an information-theoretic property of the source, or an artifact of the TF evaluation protocol of one specific decoder?
FIX: Strengthen B's self-contained case by either (a) showing the TF entropy-accuracy ordering reproduces on the from-scratch baseline's TF numeric head too (you have baseline TF numeric 0.754 in the seq2seq table — does its per-slot TF accuracy also anticorrelate with entropy?), which would make the ordering architecture-independent and genuinely source-side rather than decoder-side; or (b) adding one sentence explicitly arguing why a TF per-position ordering IS the right information-theoretic object (it isolates per-symbol recoverability from sequence-level error propagation) independent of any decoder's AR dynamics, so the AR collapse is a deployment caveat, not a threat to the source-entropy claim.

[B-sub-18] (B-salami) toolpath_entropy.tex, Section 9 (Audit-Leverage Corollary), line 415 :: scope/framing
ISSUE: The corollary names "drilling cycles" as a canonical low-entropy operation class ideal for sensor-side audit, but the corpus contains NO drilling operations. Verified: data_clean/ holds only face*/pocket*/adaptive* G-code (grep for drill/G81/G83/peck returns nothing). This invokes an operation class outside the single-corpus scope the paper otherwise polices rigorously (Scope statement line 96; Limitations line 423), and it weakens the anti-salami posture by gesturing at generality the corpus cannot support. A reviewer who checks the corpus will read it as an overclaim.
FIX: Replace "facing-pass repeats, drilling cycles" with corpus-grounded examples only, e.g. "facing-pass workpiece-edge repeats, Z cutting-depth steps" — the two low-entropy structures actually measured (facing X 2.10 bits; Z tenths 0.64 bits). If a drilling forward-reference is wanted, mark it explicitly as a hypothesis outside the present corpus.

[B-sub-19] (B-it) toolpath_entropy.tex, abstract (line 68) and conclusion (line 437); also Sec. 3 para (lines 127, 152) and Tables 2-3 :: claim/rigor
ISSUE: The central object is the MARGINAL per-digit-slot Shannon entropy H(X_s). The paper repeatedly sums these per-slot marginals to a per-axis 'bit budget' (Fig. bits_recoverable_per_axis: X 14.9, Y 14.8, Z 10.5, R 8.1, F 4.9 bits; verified = sum of per-slot source marginals) and treats the per-slot marginals as the recoverable information content. But sum_s H(X_s) >= H(X) (the joint coordinate entropy) by subadditivity, with equality only if the digit slots are independent. They are demonstrably NOT independent (e.g., facing's coordinate-repetition concentrates whole coordinate values like X=-0.1254, so the tenths/hundredths/thousandths digits co-vary). The 'bit budget' therefore systematically OVER-states the true source information by the total inter-slot mutual information, and the per-slot decomposition double-counts shared structure. This is the load-bearing quantitative framing of Sec. 6.
FIX: Add one explicit sentence where the bit budget is introduced (Fig. bits_recoverable_per_axis caption / line 349) stating that sum_s H(X_s) is an UPPER BOUND on the joint per-axis coordinate entropy H(X) because digit slots are not independent (subadditivity; the gap is the inter-slot redundancy / total correlation), so the per-axis bit totals are marginal-sum upper bounds, not the joint source entropy. Optionally report the empirical joint H(X) per axis (directly estimable from the same corpus, n=1707-5646 whole coordinate values) alongside the marginal sum to quantify the redundancy gap; the joint will be markedly smaller for low-entropy axes/op-classes (facing) where coordinates repeat.

[B-sub-20] (B-consistency) toolpath_entropy/tables/per_axis_recoverability.tex, footnote (\footnotesize\textit block, sign-asymmetry sentence) :: consistency
ISSUE: The Paper B per-axis table footnote states the X/Y/Z sign-accuracy asymmetry shows 'the sensor pathway distinguishes signed motion along Y more reliably than along X or Z on this geometry.' This is a sensor-separability interpretation that DIRECTLY CONTRADICTS (a) Paper B's own body in Section raw-entropy / Table sign_entropy ('only Z's sign carries recoverable information ... a head cannot recover information a deterministic source does not contain'), where high Y/X sign accuracy is attributed to the near-deterministic PRIOR (H_Y^sign=0.08, H_X^sign=0.38, H_Z^sign=1.04 bits), and (b) the identically-sourced Paper A table, whose caption explicitly says 'the high Y/X sign accuracy reflects the prior, not sensor discrimination.' The two papers draw from the same v8_per_field_fullwindow.json yet give opposite causal readings of the same numbers.
FIX: Replace the Paper B footnote sentence with the prior-driven explanation used in the body and in Paper A: e.g. 'The sign asymmetry is prior-driven: Y (0.997) and X (0.942) reflect near-deterministic sign priors (H_Y^sign=0.08, H_X^sign=0.38 bits), while Z (0.938) is the only axis whose sign is genuinely balanced (H_Z^sign=1.04 bits) and therefore the only one carrying real sensor-driven sign signal.' Do not assert sensor-pathway discrimination favoring Y.

============================================================
## MODERATE (70)
============================================================

[B-mod-1] (B-figtab) Table per_head_entropy (tab:per_head_entropy, command row = 0.891) vs Table seq2seq_baseline (tab:seq2seq_baseline, Main command = 0.888) and tex line 302 :: consistency
ISSUE: The decoder's command accuracy is reported as two different numbers across tables: Table per_head_entropy uses the PATCHED value 0.891, while Table seq2seq_baseline and the body text (line 302, '0.877 baseline vs. 0.888 decoder') use the RELEASED value 0.888. This is explained internally (the seq2seq table footnote says categorical entries are the released checkpoint; line 386 lists 'command 0.891 vs 0.888 patched vs released'), but a reader comparing the two headline tables sees command=0.891 in one and 0.888 in the other with no cross-pointer at the point of reading.
FIX: Add a one-clause footnote to Table per_head_entropy noting 'command 0.891 is the patched value; the released 0.888 is used in the seq2seq comparison (Table 4) per its checkpoint-mixing note', or harmonize on one value with the delta stated parenthetically.

[B-mod-2] (B-figtab) figures/bits_recoverable_per_axis.pdf (Figure, \label{fig:bits_recoverable_per_axis}, tex line 351-356) :: figure/table
ISSUE: The two-line embedded title 'Per-axis bit budget... (SOURCE entropy x patched recovery accuracy)' collides with the per-bar annotation labels '10.1/14.9' and '9.6/14.8' sitting atop the X and Y bars (the X/Y bars reach the top of the axes at ~14.9, so the labels land in the title band). The '9.6/14.8' label is partially obscured by the title's second line.
FIX: Increase the y-axis upper limit (e.g. to 16) so the bar-top labels clear the title, or move the recovered/total annotations inside the bars, or shorten/remove the embedded title since the LaTeX caption is self-contained.

[B-mod-3] (B-figtab) figures/per_head_opclass_entropy_heatmap.pdf caption (tex lines 224-228) vs in-text refs (lines 141, 220) :: figure/table
ISSUE: The LaTeX caption never states the hatching convention, yet two in-text passages tell the reader that 'cells with n<30 are hatched' in this figure (lines 141, 220). The figure's own embedded title carries the convention ('red hatch = min per-fold n<30 (1 cells)') but (a) the caption should restate it to be self-contained, and (b) the embedded title has a grammar error '(1 cells)' that should read '(1 cell)'.
FIX: Add to the caption: 'The single cell with min per-fold n<30 (command x damageadaptive, n=9) is hatched in red.' Fix the embedded-title pluralization to '(1 cell)' on regeneration.

[B-mod-4] (B-figtab) figures/per_slot_per_opclass_entropy_heatmap.pdf (op-class columns) vs figures/per_head_opclass_entropy_heatmap.pdf (op-class columns) :: consistency
ISSUE: Two heatmaps both labeled as 'nine operation classes' show different column label sets and orderings: the per-slot heatmap uses the nine RAW source files (face/face075/face150/pocket/pocket075/pocket150/adaptive/adaptive075/adaptive150, family-grouped) while the per-head heatmap uses the nine WINDOWED labels (adaptive, adaptive150025, damageadaptive, damageface, damagepocket, face, face150025, pocket, pocket150025, alphabetical). The distinction is explained once at line 125 but is easy to miss and looks like a labeling error when the two figures sit a few pages apart.
FIX: Add a short clause to each caption clarifying the class set ('raw source-file classes' vs 'windowed sensor-corpus classes incl. damage/150025 variants'), and if feasible adopt a common ordering convention (family-grouped) in both.

[B-mod-5] (B-figtab) figures/per_slot_per_opclass_entropy_heatmap.pdf vs quantization_heatmap.pdf vs per_axis_per_slot_per_opclass_entropy_heatmap.pdf :: figure/table
ISSUE: The three SOURCE-entropy heatmaps use inconsistent colormaps: per_slot_per_opclass uses a magma/inferno scheme (black->purple->orange->yellow) while quantization_heatmap and per_axis_per_slot use viridis (purple->teal->yellow). Same physical quantity (source entropy in bits, same 0-3.32 range) rendered with two different color encodings makes cross-figure reading harder and looks unintentional.
FIX: Standardize all per-(axis/slot/op-class) SOURCE-entropy heatmaps on one colormap (viridis) and one shared color scale (0 to log2(10)=3.32 bits) so the reader can compare cells across figures.

[B-mod-6] (B-salami) tables/per_digit_position.tex (slot-5 entropy 0.311) vs Section 4 narrative (X/Y/Z ten-thousandths = 0.00 bits) :: figure/table
ISSUE: The per-digit-position table reports pooled slot-5 (ten-thousandths) entropy as 0.311 bits, while the entire Section 4/Section 8 narrative repeatedly states the ten-thousandths is a 'structural zero' at 0.00 bits (tokenizer-discarded). The 0.311 is correct because the pool includes R (which retains 2.5 bits at 0.0001) and F, but the table caption does not explain why the 'structural zero' slot shows 0.311 bits, and a reader cross-referencing the table against the prose will see an apparent contradiction (0.00 vs 0.311).
FIX: Add a clause to the per_digit_position caption: 'slot-5 entropy (0.311 bits) is nonzero only because the pool mixes the X/Y/Z tokenizer-zeroed ten-thousandths with R's retained 0.0001 digit (2.5 bits); on X/Y/Z alone slot-5 is 0.00 bits — see Table source_vs_tokenized.' This removes the apparent prose/table conflict.

[B-mod-7] (B-salami) tables/per_head_entropy.tex (command TF acc = 0.891) vs abstract/intro/conclusion/seq2seq table (command = 0.888) :: consistency
ISSUE: Two different command-head accuracies circulate. The per-head entropy table and Section 8 (line 386, patched-vs-released list) use 0.891 (patched retrain). The abstract (0.888), intro (line 81/96 implied), the seq2seq table's Main column (0.888 TF and AR), Section 7 line 302 ('0.877 baseline vs. 0.888 decoder'), and the conclusion ('0.877 ~ 0.888') use 0.888 (released). The float-epsilon section explains both numbers exist (0.891 patched vs 0.888 released, drift <=1.5pp), but the paper never tells the reader WHICH is 'the' decoder command accuracy, and uses the released 0.888 in the headline convergence claim while the per-head table uses patched 0.891. Since the paper insists every fine-digit number is on the patched retrain, mixing 0.888 (released) into the convergence comparison is a subtle provenance slip.
FIX: Pick one number for the command-head convergence claim and footnote the other. Cleanest: use patched 0.891 everywhere the decoder command accuracy appears (matching the per-head table and the paper's 'everything fine-digit is patched' rule), and add a parenthetical '(released 0.888; <=1.5pp patch drift)' once. Or, if the convergence is deliberately on the released checkpoint (as the seq2seq table caption says the categorical entries are released), state that explicitly at line 302 so 0.891 and 0.888 are not silently both 'the decoder.'

[B-mod-8] (B-it) toolpath_entropy.tex Sec. 1 (line 81) and Sec. 2 (line 109): the 'two endpoints' argument that a uniform digit 'leaves a recoverer at the modal-class prior unless the channel is informative' :: claim/rigor
ISSUE: This endpoint claim is itself a conditional-entropy statement smuggled in via marginal entropy. 'A uniform digit leaves a recoverer at the prior' is true ONLY IF the channel is uninformative (I(X;obs)=0); the paper states 'unless the channel is informative about it' but then in practice treats the 3.3-bit plateau AS the operative limit (the bathtub trough is attributed to it). The endpoint is therefore not actually 'clean' — it depends on the unmeasured H(X|obs). The empirical fact that accuracy IS low at the uniform digits is then evidence that the 4 Hz channel happens to be uninformative there, which is an empirical (channel + Nyquist) finding, not a consequence of source entropy.
FIX: Make explicit that the observed low accuracy at the 3.3-bit slots is the JOINT consequence of (i) maximal source variability and (ii) the empirically-uninformative low-rate channel (the Nyquist floor), and that source entropy alone does not predict it — the channel does the work. This is consistent with the paper's coupled-limits framing (line 288) but the 'endpoint' rhetoric in the intro presents it as if source entropy were sufficient.

[B-mod-9] (B-it) toolpath_entropy.tex Sec. 10 Audit-Leverage Corollary (line 415): 'a maliciously substituted coordinate is indistinguishable from a legitimate one because the legitimate distribution itself approaches the uniform-noise ceiling' :: claim/rigor
ISSUE: This security corollary conflates SOURCE marginal entropy with DETECTABILITY of a substitution. High marginal H(X) at the thousandths does NOT imply a substituted coordinate is undetectable: detection by sensor-side audit depends on whether the substitution changes the OBSERVABLE (the sensor signature), i.e. on the channel, and on JOINT/sequential structure (a coordinate inconsistent with the toolpath geometry is detectable even if its thousandths digit is uniform). The corollary as stated treats per-digit marginal uniformity as if it implied per-coordinate indistinguishability in the observation space, which does not follow.
FIX: Scope the corollary to what is shown: where the SOURCE coordinate distribution is near-uniform at the fine decimals AND the low-rate channel is uninformative there (Nyquist floor), a fine-decimal substitution is not statistically conspicuous in the recovered marginal. Add that geometric/sequential consistency checks and the joint coordinate distribution can still flag substitutions, so 'unauditable by sensor-side recovery alone' should read 'unauditable by marginal fine-decimal sensor recovery alone'. This keeps the (valuable) corollary but stops it overreaching from marginal entropy to full indistinguishability.

[B-mod-10] (B-it) toolpath_entropy.tex Sec. 2 (line 109) and Sec. 11 (line 429): rate-distortion theory (berger1971ratedistortion) is cited as 'framing lossy coordinate recovery' but never instantiated :: claim/rigor
ISSUE: Rate-distortion is invoked twice (Sec. 2, and as the conceptual backdrop for 'lossy coordinate recovery') but the paper never computes or uses an R(D) function, a distortion measure, or a rate. The framing is decorative: the actual results are per-slot marginal entropies and a top-1 accuracy correlation, neither of which is a rate-distortion quantity. Citing R(D) as the frame for the recoverability LIMIT risks a reviewer expecting an R(D) curve (e.g. coordinate MAE vs bits) that the paper does not deliver — and the per-axis MAE data (X 0.248, Z 0.009 in) plus per-slot entropy is actually most of what an R(D)-style plot would need.
FIX: Either (a) drop the rate-distortion framing to avoid an unmet expectation, replacing it with the lossy-source-coding intuition only; or (b) operationalize it minimally: plot achieved coordinate distortion (per-axis MAE, already in patched_numeric_analysis.json) against the per-axis recoverable-bits budget, and state explicitly this is an empirical (achieved) operating point, not the optimal R(D) frontier. Option (b) materially strengthens the 'limit' claim with almost no new computation.

[B-mod-11] (B-it) toolpath_entropy.tex Sec. 4 'Entropy-estimator bias' (line 141) and Sec. 2 (line 109); keyword list (line 71) advertises 'Miller--Madow correction' :: statistics
ISSUE: Miller-Madow corrects the downward bias of the plug-in entropy, but the load-bearing inferential claim is the NEGATIVE entropy-accuracy correlation, where the relevant concern is the differential bias of small-n op-class cells: rare cells (e.g. command x damageadaptive at n=9 observations/fold, verified) have plug-in entropy biased LOW by O((K-1)/(2n ln2)) ~ a much larger amount than the well-supported cells, which can systematically distort a correlation between entropy and accuracy that mixes large-n and small-n cells. The paper bounds MM on the load-bearing X/Y/Z slots (<=0.004 bits, verified) but the 54-cell head x op-class scatter (Fig. entropy_accuracy_all_heads, r=-0.68) mixes n=9 cells with n=144 cells and reports UNCORRECTED plug-in entropies as the x-axis, with no statement that the differential bias is negligible for THAT scatter.
FIX: Add one sentence to the 54-cell scatter discussion (line 300/345) noting that the per-cell n ranges from ~9 to ~144 so the plug-in entropy bias is heterogeneous across cells; either apply the MM correction to the 54-cell x-axis and report the corrected r, or state the magnitude of the worst-case differential bias (token head K up to ~thousands at n=20 can be a sizeable fraction of a bit) and confirm the sign/strength of r is unchanged. As written, MM is verified only where it is smallest.

[B-mod-12] (B-it) toolpath_entropy.tex Sec. 5 'Pooled six-slot relationship (illustrative only)' (line 298) and Fig. entropy_accuracy_overlay; abstract reports 'pooled -0.89' :: statistics
ISSUE: The pooled six-slot Pearson r=-0.89 (n=6) is contaminated by the slot-5 ten-thousandths point, which is an ARTIFACT of the tokenizer (entropy ~0.31, accuracy 0.99). Including it makes both pooled endpoints (slot-0 low-H/high-acc and slot-5 low-H/high-acc) drive a U-shaped accuracy against a monotone-ish entropy, so the linear Pearson coefficient is being computed on a non-monotone bivariate cloud where a single tokenizer-induced point sits at the 'easy' end. The paper correctly downgrades this to illustrative on n=6 / normality grounds, but does not flag that the point itself is a representation artifact the paper spends Sec. 3 arguing should be set aside.
FIX: In the 'illustrative only' paragraph, add that the n=6 pooled fit additionally mixes the tokenizer-collapsed slot-5 point (a representation zero, not a source quantity) with the five source slots, so the pooled coefficient is doubly non-load-bearing; report it as a visual aid and keep the n=30 cell statistic as the sole inferential anchor. Consider also reporting the pooled fit over the 5 source slots excluding the tokenizer-collapsed slot-5 to show robustness.

[B-mod-13] (B-it) toolpath_entropy.tex Sec. 5 (line 302), Table seq2seq: 'reach the same teacher-forced command accuracy (0.877 baseline vs. 0.888 decoder, well within fold variance)' :: statistics
ISSUE: 'Well within fold variance' is asserted but no paired test is given for the command-head convergence, which is a load-bearing architecture-invariance claim. The s.d. are 0.031 (baseline) and 0.056 (decoder) at n=5 folds; an 0.011 gap is plausibly n.s., but the claim 'reach the SAME accuracy' is an equivalence/null claim that needs either a paired test (with explicit non-significance) or, better, a TOST equivalence test, especially since the same section does report paired t-tests for the K-spectrum. A non-significant difference is not evidence of equivalence without a stated margin.
FIX: Report the paired comparison for TF command (paired t or Wilcoxon across the 5 folds, with p) and ideally a TOST against a pre-stated equivalence margin (e.g. +-5 pp, matching the 'wins by >5pp' bolding threshold used in the table), so 'land at the same ceiling' is backed by an equivalence statistic rather than an eyeballed overlap of s.d.'s. This directly hardens the central cross-architecture claim.

[B-mod-14] (B-it) toolpath_entropy.tex Sec. 6 'Nyquist floor' (line 286) and abstract (line 68); audit/gcode_row_durations.json measured on n_files=120, while source entropy is on 9 raw files :: consistency
ISSUE: The 77.7% sub-Nyquist statistic is measured on the 120-file aligned (windowed/executed) corpus (21,541 row-execution instances), whereas the entire source-entropy apparatus is computed on the 9 authored raw .gcode files (6,051 code lines). The paper presents both as 'the source' and 'the two limits' without flagging that the Nyquist floor and the entropy ceiling are measured on DIFFERENT corpora (executed 120-file vs authored 9-file). A careful reader will ask whether the 9 authored files' block durations match the 120 executed files', i.e. whether the two anchors describe the same source.
FIX: In Sec. 6, add a clause noting the duration audit is over the 120-file executed/aligned corpus (the 9 authored toolpaths replicated across runs/conditions), and state that the executed corpus is the per-run instantiation of the same 9 authored programs so the block-duration distribution is representative of the authored source. One sentence reconciling the two corpora removes the apparent corpus mismatch between the two 'limits'.

[B-mod-15] (B-it) toolpath_entropy.tex Sec. 6 'recovered/unrecovered bits' (Fig. caption line 354): 'green/red shading approximates recovered (~H_s a_s) vs. unrecovered (~H_s(1-a_s)) bits' :: statistics
ISSUE: The quantity H_s*a_s ('recovered bits') is not an information-theoretic quantity. Recovered information is properly H(X_s) - H(X_s | prediction) = I(X_s; prediction), i.e. mutual information; the product of marginal entropy and top-1 accuracy has no such interpretation (a recoverer can be 90% accurate yet leave high residual entropy if its errors are spread, or 50% accurate yet near-deterministic conditionally). The heuristic happens to give a monotone-ish shading but is presented in a figure whose whole point is bits-of-information accounting.
FIX: Either (a) relabel the shading as a heuristic 'accuracy-weighted entropy' and state in the caption that it is an illustrative proxy, NOT I(X_s; X-hat_s); or (b) compute the actual conditional entropy H(X_s | X-hat_s) from the per-slot confusion matrices (available from predictions.npz used elsewhere) and shade by I(X_s;X-hat_s)=H(X_s)-H(X_s|X-hat_s), which would also directly support the 'recoverability' framing the paper claims to want (Sec. 11 'conditional-entropy estimate is a natural follow-on').

[B-mod-16] (B-it) toolpath_entropy.tex Sec. 6 (line 288): 'a transient shorter than one sample period aliases: the 4 Hz stack observes the lower-frequency cutting-regime envelope, not the individual block' :: claim/rigor
ISSUE: This conflates two distinct phenomena under the word 'aliases'. Nyquist/aliasing concerns whether a band-limited signal can be RECONSTRUCTED from samples (a signal with content above f_s/2 produces aliases). A G-code block 'shorter than one sample period' is a duration/observability argument (the block may not be sampled at all, or be merged with neighbors), which is about temporal RESOLUTION and missed events, not strictly spectral aliasing. The genuine information-theoretic statement is closer to: blocks shorter than the sample period are sub-sampled so distinct per-block transients are not separable (a sampling/observability limit), with aliasing being the spectral manifestation only if the underlying physical signal has spectral content above 2 Hz. The paper invokes Nyquist-Shannon (a reconstruction theorem) to justify a per-event resolution claim.
FIX: Tighten the mechanism: state that at 4 Hz, blocks executing in < 1 sample period are not individually resolvable (temporal aliasing/merging of distinct transients), and that to the extent the physical emission has spectral content above the 2 Hz folding frequency it is aliased irrecoverably. Distinguish 'cannot resolve distinct per-block transients' (observability) from 'spectral aliasing' (reconstruction), and cite Nyquist for the latter. This also pre-empts a reviewer noting that the cutting-regime envelope itself is precisely the sub-2-Hz content that IS recoverable.

[B-mod-17] (B-it) toolpath_entropy.tex Sec. 7 (line 296, line 345) and Fig. entropy_accuracy_all_heads: the 54-cell scatter uses released-checkpoint accuracies (digit 0.585) while the n=30 lead uses patched (digit 0.570) :: consistency
ISSUE: Verified in audit JSONs: entropy_accuracy_correlation_fixed.json (the 54-cell, r=-0.68) is keyed to headline_tf_acc_used digit=0.585 (RELEASED checkpoint), whereas the lead n=30 (patched_numeric_analysis.json, r=-0.83) uses the PATCHED retrain (digit 0.570). The paper states 'Every quantitative fine-digit result in this paper is computed on the patched-encoder retrain' (line 386, bold) — but the 54-cell scatter (a fine-digit / digit-head-containing result) is on the released checkpoint. The paper does flag the n=30 as the load-bearing encoding-consistent statistic and the 54-cell as superseded-context, but the bold blanket claim and the 54-cell provenance are in tension.
FIX: Soften the line-386 blanket statement to 'Every LOAD-BEARING fine-digit result ... is on the patched retrain' and add a half-sentence to the Fig. entropy_accuracy_all_heads caption noting it uses released-checkpoint accuracies (the patch affects only the numeric tier by <=1.5 pp, so the cross-head r=-0.68 is unaffected in sign/strength), to reconcile with the 'all on patched' claim. Alternatively regenerate the 54-cell scatter on patched accuracies for full consistency.

[B-mod-18] (B-it) toolpath_entropy.tex Sec. 7 (line 306) and Table per_digit_position; verified vs audit/entropy_accuracy_correlation_fixed.json + patched_numeric_analysis.json corrected_pooled_slot_entropy :: claim/rigor
ISSUE: The pooled bathtub is claimed to 'track per-slot source entropy', with the thousandths called the trough AND 'the true precision floor.' But the pooled entropies are hundredths=3.301 > thousandths=3.287, while accuracies are hundredths=0.547 > thousandths=0.417. So accuracy keeps falling from hundredths to thousandths even though entropy slightly DROPS — the monotone entropy->accuracy ordering is locally INVERTED exactly at the trough the paper emphasizes. The deepest accuracy is not at the entropy maximum; the residual driver is plausibly autoregressive/positional depth (deeper decimal = later digit position), not source entropy. This is a real seam in the headline 'ordering keyed to H(X)' story.
FIX: Add a sentence acknowledging that at the hundredths->thousandths transition accuracy continues to fall while marginal entropy is flat-to-slightly-lower, so the trough location is set partly by digit position/conditioning depth rather than by source entropy alone; this is consistent with (and reinforces) the paper's own framing that the relationship is an ordering, not a tight per-slot bound. Avoid calling the thousandths 'the true precision floor' as if entropy-determined; call it the accuracy trough and note entropy is at-ceiling across hundredths-thousandths jointly.

[B-mod-19] (B-it) toolpath_entropy.tex Sec. 8 (line 386) and Fig./Table captions: 'the X/Y/Z ten-thousandths is discarded by the 0.001 V8 tokenizer grid ... recovered at 0.99 as the deterministic value the tokenizer makes it' :: claim/rigor
ISSUE: The paper says the X/Y/Z 10^-4 tokenized target is 'identically a constant (0 source bits)', yet patched accuracy is 0.993 (pooled) / 1.000 (X) / 0.9999 (Y) / 0.9987 (Z), NOT 1.000. If the target were truly a single constant symbol after the 0.001 grid, a classifier predicting the majority class would score exactly 1.000. The sub-1.0 values (and slot5_by_opclass pocket=0.9745, verified) indicate the 10^-4 slot is NOT identically constant in the tokenized stream — there is residual variation (e.g. R is on a 0.0001 grid and shares the slot-5 accumulator, or a small fraction of X/Y/Z values carry a non-zero ten-thousandths through rounding). The 'identically a constant / 0 bits' phrasing is slightly too strong given accuracy != 1.0.
FIX: Either reconcile: state that the X/Y/Z 10^-4 tokenized digit is constant=0 by construction and that the <1.0 pooled accuracy reflects axis-pooling with R (0.0001 grid, slot-5 entropy 2.508) and/or boundary-rounding residue, OR soften 'identically a constant' to 'near-constant (tokenized entropy 0.00 bits to two decimals; residual <1% mass)'. The pocket=0.9745 family value in particular deserves a word, since a true constant target cannot score 0.9745.

[B-mod-20] (B-it) toolpath_entropy.tex Sec. 9 K-spectrum (line 400): 'finer numeric quantization does not lower the source entropy on the fine decimals --- the thousandths digit is near-uniform regardless of how the vocabulary buckets it' :: claim/rigor
ISSUE: This claim is asserted but not shown for the K-spectrum. Coarser K (e.g. K=24 placeholder, K=69 1-digit) by definition discards fine decimals (1-digit numerics cannot carry a thousandths digit), so the per-slot SOURCE entropy of the tokenized target at the thousandths is NOT invariant across K — it is collapsed to 0 for small K exactly as the 0.001 grid collapses the X/Y/Z ten-thousandths. The statement is true for the SOURCE (raw, pre-tokenization) but the K-spectrum operates on the tokenized target, where finer/coarser K demonstrably changes which slots survive. The sentence blurs the source/tokenized distinction the rest of the paper works hard to maintain.
FIX: Clarify: the SOURCE thousandths digit is near-uniform regardless of K (it is a property of the raw coordinates), but the TOKENIZED target's fine-slot entropy is of course K-dependent (small K discards fine decimals entirely, as Table 2's 0.001-grid argument shows). State that the K-spectrum varies the structural-token granularity, and that no K choice can recover information the SOURCE does not concentrate at the fine decimals — that is the intended point and it is about the source, not the tokenized entropy.

[B-mod-21] (B-consistency) toolpath_entropy.tex Table 4 (tab:sign_entropy) caption, vs raw_gcode_source_entropy.json key per_axis_sign :: statistics
ISSUE: Table 4's caption distinguishes the raw-source sign column from the model-side column by saying the model-side 'is the 3-way (positive/negative/zero) sign entropy,' implying the raw column is a 2-way (pos/neg) entropy. But the audit JSON shows the raw per_axis_sign entropies are also 3-way: Z's raw H=1.10 bits is computed over pos_frac 0.494 / neg_frac 0.490 / zero_frac 0.016 (a pure 2-way pos/neg renormalization would give ~1.00 bit, not 1.10). So both columns are 3-way; the caption's contrast is incorrect and mis-describes the raw estimator.
FIX: Drop or fix the parenthetical: state that BOTH the raw-source and model-side sign entropies are 3-way (positive/negative/zero), the small zero mass (Z zero_frac=0.016) lifting Z above 1.0 bit. This also reconciles with the per_head_entropy.tex note that the sign-pooled value 'is the 3-way ... entropy and equals 0.33 bits ... (the renormalised positive-vs-negative entropy is lower).'

[B-mod-22] (B-cam) toolpath_entropy.tex command-head vocabulary {G0,G1,G2,G3,G53,M30} (per_head_entropy.tex) and Source section line 122-131 :: methodology
ISSUE: The source corpus contains non-cutting positioning/setup motion that is never physically interpreted: G53 (machine-coordinate-system rapid, 6 occurrences) and G55 (work-coordinate-system select, 3) appear in the source, and G53 is one of the six command-head classes. G53 moves are referenced to MACHINE coordinates, not the part, so their X/Y/Z values are tied to machine home / fixture offsets and are categorically different from the part-relative cutting coordinates whose entropy the paper analyzes. Pooling G53 machine-frame coordinates with G0/G1/G2/G3 part-frame coordinates into the per-axis slot entropy could shift the integer/tenths slots (the 'units mass on {0,1}' claim, line 349) without that being a CAM-toolpath property. The paper neither flags this nor states whether G53 rows are included in the entropy computation.
FIX: Add one sentence to the Source section stating how setup/positioning blocks (G53 machine-frame moves, G55 WCS select, tool change) are handled in the coordinate-entropy computation — either that they are a negligible fraction (a few rows out of 6,051 code lines) or that they were excluded — so the reader knows the per-axis entropy is computed on part-frame cutting coordinates, not a mix of machine- and work-frame values.

[B-mod-23] (B-write) toolpath_entropy.tex line 109 (Related Work, info-theory subsection) :: clarity
ISSUE: The Section 2.1 paragraph is a single ~250-word block citing 11 references (Shannon x2, Fano, Feder-Merhav, Cover-Thomas, Berger, Miller, Paninski, Nyquist, Shannon-1949, Lloyd, Max) with the Miller-Madow bias correction, the Nyquist floor, and the Lloyd-Max view all crammed into one paragraph. The density makes it hard to find any single result. The Miller-Madow numeric detail ('<=0.004 bits') belongs in the methods/Section 4, not in Related Work.
FIX: Split Section 2.1 into two short paragraphs (entropy/Fano canon; sampling + quantization theorems). Move the specific '<=0.004 bits negligible' Miller-Madow magnitude out of Related Work — it is already stated at line 141 — and leave only the citation here.

[B-mod-24] (B-stats) toolpath_entropy.tex line 141 (Miller-Madow) + line 92 contribution (4) 'Miller-Madow bias note'; verified bias magnitude reasoning :: statistics
ISSUE: The Miller-Madow treatment is applied to the per-cell SOURCE entropies (well-supported, n>700, correction <=0.004 bits) but the load-bearing CORRELATION axis uses model-side tokenized entropies estimated on the windowed test split, where some op-class cells have small n (the n=54 cell counts in entropy_accuracy_correlation_fixed.json include pocket n=19, damageadaptive n=20, damageface n=20). The plug-in bias on a 6.97-bit token-head distribution estimated from n=19 windows is non-negligible and is downward, which would systematically bias the high-entropy (low-accuracy) cells' entropy estimate, potentially flattening the very slope being reported. The paper bounds bias on the source slots but not on the entropy values that actually enter the headline scatter.
FIX: State the Miller-Madow (or note the lack of correction) for the entropy values entering the n=30 and n=54 scatters, or at least confirm that applying it does not change the reported r to the stated precision. Flag the small-n token-head cells (pocket n=19) whose entropy estimates are most bias-prone.

[B-mod-25] (B-cam) toolpath_entropy.tex line 152 / Table 1 caption / line 127 ('hundredths--thousandths ... where the digit distribution reaches the uniform ceiling and is unrecoverable') :: claim/rigor
ISSUE: The hundredths (10^-2) is described throughout as part of the 'uniform-noise ceiling / genuine precision floor' band, but it is physically the 254 um decade — a coordinate decade a desktop mill can both command and (often) execute. Treating the hundredths as 'unrecoverable uniform noise' is correct as a SOURCE-entropy statement (the CAM optimizer fills it uniformly) but the physical framing 'precision floor ... unrecoverable' blurs two different things: (1) the hundredths is information-rich but high-entropy (hard to recover from a 4 Hz channel), vs (2) the ten-thousandths (2.54 um) which is below any plausible physical resolution and genuinely meaningless. Calling 254 um a 'precision floor' alongside truly sub-resolution digits risks implying the part is only accurate to 254 um, which a manufacturing reviewer would dispute for a part with R0.0128-inch (325 um) arc features.
FIX: Clarify that 'precision floor' here means the floor of SOURCE-entropy recoverability, not machine precision: 'the hundredths–thousandths is where the CAM source becomes near-uniform and thus the floor of what is statistically recoverable; this is distinct from the machine’s physical positioning precision, which is finer.' This separates the information-theoretic floor from the manufacturing-accuracy floor.

[B-mod-26] (B-consistency) toolpath_entropy.tex line 195/196 (Table 3 F row source=0.00/0.00) vs source_vs_tokenized_entropy.json F source=[1.936,1.936,0.989,...] :: figure/table
ISSUE: Table 3 (tab:raw_vs_npz) lists F at the thousandths and ten-thousandths as source 0.00 / model 0.00, which is correct for those two slots. But the broader text (line 129, 152, 196) repeatedly states F is 'omitted ... for thin support' from Table 2, yet F IS shown in Table 3. More importantly, the source_vs_tokenized JSON shows F has substantial source entropy at the tens/units/tenths (1.936/1.936/0.989), and F tenths is tokenized to 0.00 by the 1.0 grid — a SECOND tokenizer-collapsed slot beyond the X/Y/Z ten-thousandths. The paper's repeated framing 'the ONLY slot the tokenizer grid drives to a constant is the X/Y/Z ten-thousandths' (line 129, 152, abstract) is then only true after excluding F by fiat; the JSON shows F tenths is also collapsed (0.989 source -> 0.00 tokenized).
FIX: Tighten the 'only collapsed slot' claim to explicitly bracket F every time it appears (it already does at line 129 parenthetically, but the abstract and line 152's bold statement should also carry the 'among well-supported axes; F tenths likewise collapses but is excluded for thin support' qualifier), so the uniqueness claim is not contradicted by the F row visible in Table 3.

[B-mod-27] (B-stats) toolpath_entropy.tex line 201 + Table tab:sign_entropy lines 211-213; raw vs model-side sign entropy :: statistics
ISSUE: Table sign_entropy mixes a 2-way raw-source sign entropy (positive fraction column, max 1 bit) with a 3-way (positive/negative/zero) model-side sign entropy in adjacent columns and calls the agreement an 'invariant ordering.' But the two are not the same quantity: Z raw=1.10 bits is reported as a 2-state entropy (49/49 split implies ~1 bit, but 1.10>1 means a third 'zero/absent' state is already in the raw column too) while X raw=0.23 and model-side=0.38 differ by 65%, and Y raw=0.11 vs model 0.08. The text attributes the X/Y discrepancy to 'the windowed split re-weighting rare negative moves,' which is plausible, but the table presents these side-by-side without flagging that they are different estimands (2-way vs 3-way) and different populations, inviting a reader to read the columns as directly comparable. The caption note ('this is the 3-way entropy') only appears for the model-side column.
FIX: Make the raw column unambiguously the same estimand as the model-side column (both 3-way, or both 2-way), or add an explicit 'these are different estimands' note in the table body. Z raw=1.10 with a 49/49 split needs the third-state mass stated. As written the >1-bit Z value contradicts a pure binary sign interpretation given in the text ('genuine binary load').

[B-mod-28] (B-consistency) toolpath_entropy.tex line 220 ('command: $1.468\to1.498\pm0.032$~bits') vs per_head_source_entropy_5fold.json (command h_emp_fold1=1.656, h_emp_mean_5fold=1.670) :: statistics
ISSUE: Line 220 discusses TWO different command-head entropy quantities and the reader can conflate them. The 'op-class-conditional mean entropy' (mean of H(head|op-class)) is 1.468->1.498 (fold-1->5-fold), while the MARGINAL per-head command entropy is 1.67 (per_head_source_entropy_5fold.json command h_emp_mean_5fold=1.670, fold1=1.656). The text does say 'the marginal entropy is 1.67 bits against the 1.50-bit op-class-conditional mean,' which is correct, but the +0.030-bit shift claimed for the conditional mean (1.468->1.498) cannot be checked against the cited file per_head_per_opclass_entropy_5fold.json from the snippets I can see, and the marginal command fold1->5fold shift is +0.014 (1.656->1.670), not +0.030. Ensure the +0.030 figure refers strictly to the conditional mean and is reproducible from the cited key.
FIX: Confirm 1.468 and 1.498 are the op-class-conditional means in per_head_per_opclass_entropy_5fold.json and that +0.030 is their difference; add the exact JSON key. Keep the marginal (1.656->1.670, +0.014) clearly separated, since the 'all other heads <=0.015 bits' claim is about the conditional means but the only marginal command shift available is +0.014, risking conflation.

[B-mod-29] (B-write) toolpath_entropy.tex line 220 (per-head/per-position paragraph) :: clarity
ISSUE: This ~200-word paragraph is one of the densest in the paper, packing the 5-fold-vs-fold-1 reconciliation (1.468->1.498), the max-cell-shift (+0.12 in adaptive150025), the n=9 hatched cell, the marginal-vs-conditional command entropy (1.67 vs 1.50), and three figure pointers into a single block. The clause 'distinct from---and necessarily no larger than---the marginal per-head source entropies' is correct but the nested em-dash construction is hard to parse on first read.
FIX: Break into two paragraphs: (1) the fold-provenance reconciliation (cosmetic, +0.030 bits max), (2) the marginal-vs-conditional distinction with the 1.67/1.50 example. Rewrite the nested-dash clause as 'These op-class-conditional means are no larger than the marginal per-head entropies of Table 2, because conditioning removes the between-op-class component (e.g., command: 1.67-bit marginal vs 1.50-bit conditional mean).'

[B-mod-30] (B-cam) toolpath_entropy.tex line 251 (Adaptive HSM regime) and line 114 (Otkur/Lazoglu trochoidal cite) :: claim/rigor
ISSUE: The adaptive-HSM regime is described as 'engagement-controlled optimizer emitting floating-point residuals' and the related work anchors it on Otkur & Lazoglu's trochoidal model. But there is no evidence in the paper that the corpus adaptive paths are trochoidal specifically (vs. e.g. Fusion 360 'Adaptive Clearing', which is a vendor engagement-control strategy not identical to classical trochoidal milling). The mechanism 'continuously curving engagement-controlled paths whose coordinates are effectively continuous-valued' is plausible and consistent with the ceiling entropy, but the trochoidal attribution is asserted, not demonstrated, and the actual CAM software/strategy used to author the toolpaths is never named (only 'CAM postprocessor' generically and 'Bantam Tools desktop mill' as the platform).
FIX: Name the CAM package and the actual adaptive strategy used to author the corpus (e.g. 'Fusion 360 Adaptive Clearing' if that is what generated them), and soften the trochoidal claim to 'an engagement-controlled adaptive strategy of the trochoidal family' unless the paths are verifiably trochoidal. This both improves reproducibility and prevents an HSM-literate reviewer from rejecting the trochoidal label.

[B-mod-31] (B-consistency) toolpath_entropy.tex line 257 ('$0.64$~bits at the tenths, recovered through the hundredths at $0.91$') and line 349 (Z tenths 'H=0.64 bits source, recovered 0.98 ... through the hundredths (0.91)') :: claim/rigor
ISSUE: The Z tenths source entropy is quoted as 0.64 bits (pooled Z, JSON per_axis Z slot2 = 0.637), but Section three-regimes line 251 quotes the ADAPTIVE Z tenths plinth as 0.61 bits and line 257 ('Per-axis specificity') quotes 0.64 for facing-context Z. The per-family JSON gives face Z tenths 0.728, pocket 0.624, adaptive 0.610 — so 0.64 is the pooled value, not a family value, yet line 257 places '0.64 bits at the tenths' inside a sentence about per-axis specificity that mixes facing X repeats with Z. The 0.61 vs 0.64 difference is real (adaptive-only vs pooled) but the text does not flag which population each refers to.
FIX: State explicitly that 0.64 bits is the POOLED Z tenths entropy (across families) and 0.61 bits is the ADAPTIVE-only Z tenths; or unify on the pooled 0.64 throughout the Z-tenths discussion and move the adaptive-specific 0.61 into the adaptive paragraph with that label.

[B-mod-32] (B-stats) toolpath_entropy.tex line 286 / Sec nyquist; verified audit/gcode_row_durations.json (77.71%, median 1.0, mean 2.90, p90 7, max 181, n=21541) :: statistics
ISSUE: The 77.7% sub-Nyquist figure is reported as a single point statistic with no uncertainty and no per-class breakdown, yet it is one of the 'two genuine limits.' The duration distribution is strongly heavy-tailed (mean 2.90 samples but median 1.0; max 181). The fraction <=1 sample almost certainly varies substantially by operation class (adaptive HSM emits many short blocks; facing fewer), which would directly couple to the entropy regimes that are the paper's other axis. Reporting only the pooled 77.7% misses an opportunity to show the Nyquist floor and the entropy ceiling are correlated across op-classes, and leaves the figure looking like an unconditioned aggregate. There is also no statement of how 'row execution duration' was derived (commanded feedrate vs measured), which matters for whether the floor is physical or modeled.
FIX: Report the per-op-class (or per-family) fraction <=1 sample to show the floor is not uniform and to connect it to the three regimes; add a one-line provenance for how durations were computed (the JSON implies commanded-feedrate-derived rather than sensor-measured — clarify, because 'measured per-row execution durations' on line 81/91 may overstate if it is computed from feedrate and geometry rather than observed timestamps).

[B-mod-33] (B-cam) toolpath_entropy.tex line 288 (Nyquist consequence) and line 419-431 (Limitations) :: scope/framing
ISSUE: The Nyquist argument states a transient 'shorter than one sample period cannot be resolved' and that a >=44.1 kHz mic / >=1 kHz encoder 'would lift the Nyquist floor.' Physically the relevant quantity for resolving an individual G-code block as a distinct transient is not just the sampling theorem on a band-limited signal but whether consecutive blocks produce separable acoustic/vibration events; at 4 Hz with median block = 1 sample, adjacent short blocks are not merely aliased but temporally merged (block boundaries fall within one sample). The paper invokes the sampling theorem somewhat loosely — sub-sample-duration events are an issue of temporal resolution / event separation, and the classical Nyquist theorem concerns frequency-content reconstruction of a continuous band-limited signal, not per-event individuation. A CAM/DSP-literate reviewer may find the Nyquist invocation an imperfect fit.
FIX: Tighten the physical statement: frame the floor as a temporal-resolution / event-separability limit (blocks shorter than one sample period cannot be individuated and are merged into the aggregate envelope), and cite Nyquist for the supporting band-limiting/aliasing intuition rather than as the literal theorem being instantiated. This makes the 'physical floor' robust to a signal-processing reviewer.

[B-mod-34] (B-consistency) toolpath_entropy.tex line 296 ('$r=-0.68$, which carried a known encoding inconsistency') and line 388 ('improves from $-0.68$ ... to $-0.83$') :: consistency
ISSUE: The pre-patch per-(axis,slot) n=30 Pearson r is given as -0.68 in two places, but Figure entropy_accuracy_all_heads (line 345) reports the n=54 head x op-class Pearson as -0.68 too (matching entropy_accuracy_correlation_fixed.json all_54 = -0.679), and that same figure says r=-0.68 'supersedes the r=-0.55 obtained before the head x op-class alignment repair.' So -0.68 is doing double duty: it is both (a) the pre-patch n=30 cell-level Pearson and (b) the post-repair n=54 head-level Pearson. Two different correlations (different n, different cell definition) carry the identical value -0.68 within the same paper, which is confusing and could be read as a copy-paste error.
FIX: Verify the pre-patch n=30 value really is -0.68 (not borrowed from the n=54 figure). If both genuinely equal -0.68, add a half-sentence disambiguation at each occurrence (e.g., 'the pre-patch n=30 cell-level r=-0.68 — coincidentally equal to the post-repair n=54 head-level r in Fig. X'). If it is a transcription of the n=54 value, correct it.

[B-mod-35] (B-stats) toolpath_entropy.tex line 296 ('matches the raw-source tokenized rows ... to within population sampling on X/Y/Z, with R differing by ~0.5 bits'); Table raw_vs_npz lines 192-195 (R 2.47 source vs 2.327 model; 2.51 vs 1.989 at slot5) :: consistency
ISSUE: R is included as a full axis in the load-bearing n=30 correlation, yet its model-side tokenized entropy differs from source by ~0.5 bits (line 296) and Table raw_vs_npz shows R slot-5 source 2.51 vs model-side 1.989 — a 0.5-bit gap attributed to 'small windowed support.' R contributes 6 of the 30 cells (20%) to the lead statistic, several of them in the mid-entropy / high-accuracy region (R s2-s5 at 0.89-2.33 bits, acc 0.88-0.91) that is doing real work pulling the slope. If R's entropy is mismeasured by 0.5 bits due to thin windowed support, those 6 cells carry meaningful error in the headline x-axis. The paper flags the discrepancy but still uses the cells at face value.
FIX: Report the n=30 correlation with R cells using the SOURCE entropy (well-supported, n=431) rather than the thin-support windowed model-side entropy, or report a sensitivity (drop R: n=24) showing the coefficient is stable. State the R cells' entropy uncertainty explicitly given their 20% weight in the lead statistic.

[B-mod-36] (B-stats) toolpath_entropy.tex line 296 (entropy axis = corrected_per_axis_slot_entropy, 'tokenized object') and line 338 caption; cross-check Table tab:source_vs_tokenized line 165-175 :: statistics
ISSUE: The lead n=30 correlation pairs MODEL-SIDE TOKENIZED entropy (audit/patched_numeric_analysis.json corrected_per_axis_slot_entropy) with patched accuracy, deliberately so both axes are 'the tokenizer-representable object.' But this means the entropy used in the headline number is NOT the decoder-free source entropy that is the paper's central object — it is the entropy of the model's own targets. The X/Y/Z slot-5 cell therefore enters as a 0-bit constant precisely because the tokenizer made it so, and that same tokenizer choice forces accuracy to ~1.0. The (0,1) cell is thus a near-tautological pairing (a constant target is trivially recovered), not an empirical confirmation of an entropy ordering. The paper acknowledges this ('the X/Y/Z 10^-4 cell enters as the tokenizer's 0-bit constant, recovered near-perfectly') but does not acknowledge that including such tautological cells in a correlation inflates |r|.
FIX: Explicitly flag that the tokenizer-constant cells contribute a near-tautological (0 bits -> recovered as constant) pairing, and that the substantive test of the ORDERING is whether accuracy declines monotonically across the cells where the source/target actually varies. Report the correlation restricted to varying cells (as in finding 1) as the substantive version, keeping the full n=30 as the 'including trivial endpoints' version.

[B-mod-37] (B-stats) toolpath_entropy.tex line 298 (pooled n=6) and Fig caption lines 330/338; cross-ref conclusion line 437 :: statistics
ISSUE: The n=6 pooled correlation (r=-0.89, rho=-0.94) is correctly labeled 'illustrative only,' but it still appears in the abstract-adjacent narrative, the contributions (line 92 lists '-0.83'; conclusion line 437 says 'pooled six-slot r=-0.89, illustrative at n=6'), and two figure captions. With n=6 the Pearson estimate is not just 'bivariate normality uncheckable' — the estimate itself is dominated by the same 2-3 endpoint cells (tens at ~0 bits/1.0 acc, thousandths at ~3.3 bits/0.42 acc). Reporting a precise two-decimal r and rho for n=6 gives an unwarranted impression of precision even with the caveat.
FIX: Consider dropping the two-decimal precision for the n=6 statistic (or report it only as 'strongly negative, n=6') and lean entirely on the n=30 (caveated) and within-digit-head (n=9) numbers. If retained, add the (uninformatively wide) CI to make the point that n=6 cannot support a precise coefficient.

[B-mod-38] (B-consistency) toolpath_entropy.tex line 302 ('$18.4$~pp ... $0.754$ vs.\ $0.570$') vs tab:seq2seq_baseline.tex caption (states both $+18.4$ here and Paper A's $+16.9$)  :: consistency
ISSUE: The numeric-gap magnitude is reported as 18.4 pp in Paper B (vs patched decoder 0.570) but 16.9 pp in Paper A (vs released decoder 0.585). 0.754-0.570=0.184 and 0.754-0.585=0.169, so each is internally correct, BUT Paper A's own seq2seq table row shows Numeric Main=0.585 with Delta = -0.169, while Paper B's row shows Numeric Main=0.570 with Delta=-0.184. The Baseline numeric TF (0.754) is shared. A reader comparing the two companion tables sees the SAME baseline against two different 'Main' numeric values and two different deltas. Paper B's caption does explain this (released vs patched basis), but Paper A's caption does not cross-note that the patched basis would give 18.4 pp.
FIX: Ensure Paper A's seq2seq caption mirrors Paper B's reconciliation sentence (16.9 pp is against the released 0.585; 18.4 pp against the patched 0.570 used in Paper B). At minimum cite the cross-paper figure in both so the two deltas are not read as a discrepancy.

[B-mod-39] (B-write) toolpath_entropy.tex line 302 and seq2seq_baseline.tex line 31 :: clarity
ISSUE: The 18.4-pp numeric reversal is explained correctly but at length in three places (Section 6 paragraph line 302, the Table 4 caption line 15, and the Table 4 footnote line 31), each re-deriving the 'bucketization trades TF digit precision for grammar-mask tractability' mechanism. The footnote alone (line 31) restates nearly everything in the caption.
FIX: Let the table caption state the headline numbers and the one-sentence mechanism; let the body paragraph (line 302) carry the interpretation. Trim the Table 4 footnote (line 31) to the methodological null-model definition (what the baseline removes) and the AR-command 'where command identity is harvested' point, dropping the duplicated TF-numeric mechanism.

[B-mod-40] (B-stats) toolpath_entropy.tex line 345 / Fig entropy_accuracy_all_heads (n=54, r=-0.68); verified entropy_accuracy_correlation_fixed.json all_54 / within_head :: statistics
ISSUE: The n=54 cross-head correlation (-0.68) is honestly labeled cross-head, but the within-head breakdown reveals the negative slope is heterogeneous and partly driven by ONE head (digit, r=-0.90) plus two moderately-negative heads (token -0.80, type -0.85), while command is +0.38, param_type -0.28, sign -0.19. Pooling six heads with different entropy ranges and different accuracy ceilings into a single Pearson r is a Simpson-paradox-prone aggregation: the -0.68 is largely a between-head mean shift (high-entropy digit/token heads have lower accuracy than low-entropy categorical heads). The paper says this ('the negative slope is cross-head'), but does not caution that a pooled Pearson over heterogeneous head populations is not a clean estimate of any single quantity and is sensitive to how many cells each head contributes (here exactly 9 each, which is at least balanced).
FIX: Add a sentence noting the n=54 pooled r is a between-head aggregate susceptible to Simpson-type effects, that 4 of 6 heads individually show weak or positive within-head slopes, and that the only clean within-head negative relationship is the digit head. Consider reporting the n=54 as a descriptive visual only (like the n=6) rather than alongside the load-bearing n=30.

[B-mod-41] (B-cam) toolpath_entropy.tex line 349 ('feed rates run ~10--100 inch/min') :: claim/rigor
ISSUE: The stated feedrate range '~10--100 inch/min' does not match the corpus. The only F values in the nine source files are F7.3, F13.3, F22, F40 (verified by grep; n=16 total). The actual range is 7.3 to 40 inch/min: the minimum is below 10 and the maximum (40) is well under 100. The loose '10--100' overstates the upper end by 2.5x and excludes the smallest value. Also note F7.3 and F13.3 carry a fractional digit, so the claim elsewhere that F is purely integer-valued (1.0 grid 'collapses' the F tenths) is consistent only because the tokenizer discards the .3 — worth a one-line acknowledgment that the source F is not integer.
FIX: Replace '~10--100 inch/min' with the actual values: 'feed rates take four values in this corpus (F7.3, F13.3, F22, F40 inch/min; i.e. roughly 185--1016 mm/min)'. Optionally add that the source F tenths (e.g. 7.3, 13.3) are non-zero but discarded by the 1.0 tokenizer grid, mirroring the X/Y/Z ten-thousandths representation effect.

[B-mod-42] (B-write) toolpath_entropy.tex line 349 (per-axis bit budget paragraph) :: clarity
ISSUE: This paragraph mixes recovery accuracies, source entropies, and MAE-in-inches in a single dense block, and parenthetically handles the F=1.115 MAE anomaly ('off this coordinate scale --- feed rates run ~10-100 inch/min --- and is not a recovery failure'). The MAE values (X 0.248, Y 0.205, Z 0.009, R 0.021) are introduced here for the first and only time without a table, so the reader cannot cross-check them, and the units jump between bits, accuracy fractions, and inches within two sentences.
FIX: Either tabulate the per-axis MAE alongside accuracy and entropy (a small 5-row table would let the reader see the X/Y vs Z/R contrast at a glance), or move the MAE sentence to a footnote. If kept inline, separate the bit-budget (entropy units) discussion from the MAE (inch units) discussion into distinct sentences with explicit unit labels.

[B-mod-43] (B-stats) toolpath_entropy.tex line 376 + 431 (teacher-forced upper bound; AR digit approx 0.10) :: claim/rigor
ISSUE: The teacher-forced upper-bound caveat is stated well, but the entire entropy-accuracy ORDERING (the paper's central empirical claim) is established only under TF, while the paper simultaneously states that AR deployment digit accuracy collapses to ~0.10 (a near-constant floor across slots). Under AR there is essentially NO accuracy spread left to be ordered by entropy — the ordering is a TF-only phenomenon. The paper should be more explicit that the source-entropy ordering is empirically demonstrable ONLY in the TF regime and is not observable in the deployment (AR) regime, because AR accuracy is floored independent of slot entropy. As written, a reader could infer the ordering has some deployment relevance; it does not (the deployment floor is set by exposure bias + Nyquist, not by the per-slot entropy gradient).
FIX: Add to the caveat (line 376 or limitations 431): 'Under AR the digit accuracy floor (~0.10) is approximately flat across slots, so the entropy-ordered accuracy gradient is observable only under teacher forcing; the ordering characterizes in-principle recoverability, and has no AR-deployment-observable signature.' This prevents over-reading the ordering as deployment-relevant.

[B-mod-44] (B-consistency) toolpath_entropy.tex line 386 ('command $0.891$ vs.\ $0.888$') and line 302 / Table tab:seq2seq_baseline ('$0.888$ decoder') and tab:per_head_entropy ('command ... $0.891$') :: consistency
ISSUE: The decoder command TF accuracy is quoted as two different numbers depending on checkpoint without that being flagged at point of use. Section float-epsilon (line 386) lists patched command = 0.891 vs released 0.888. But the convergence claim (line 302), the abstract, the conclusion (line 437: '0.877≈0.888'), and tab:seq2seq_baseline all use 0.888 (released) for the decoder command, while tab:per_head_entropy uses 0.891 (patched). Since per_head_entropy is the patched-encoder table and command is categorical/checkpoint-invariant to within drift, a reader cross-referencing the two tables sees command listed as both 0.888 and 0.891 with no in-table cross-note.
FIX: Add a one-clause footnote in tab:per_head_entropy (or in the convergence paragraph) noting command is 0.888 released / 0.891 patched and that the convergence and headline figures use the released 0.888 (categorical heads being checkpoint-invariant to <=1.5 pp). Pick one value consistently for the headline command number and footnote the other.

[B-mod-45] (B-write) toolpath_entropy.tex line 392-400 (Section 8 k-spectrum) :: scope/framing
ISSUE: Section 8 (Vocabulary Cardinality) and Section 9 (Audit-Leverage Corollary) are the two sections most tangential to the paper's stated central object (source entropy structure). Section 8 spends most of its length retracting a 'sweet-spot' claim and disentangling a schedule confound — important rigor, but it reads as inherited from Paper A's ablation rather than advancing the entropy-floor argument. The connection to the entropy floor (last sentence of line 400: 'finer numeric quantization does not lower the source entropy on the fine decimals') is the actual relevant point and is buried at the end.
FIX: Lead Section 8 with the entropy-relevant conclusion (vocabulary cardinality does not move the fine-decimal source entropy; it only changes placeholder granularity), then present the spectrum and the withdrawn-sweet-spot rigor as support. Consider whether the full schedule-confound retraction can be compressed by cross-citing Paper A, given line 396 already defers the full ablation there.

[B-mod-46] (B-write) toolpath_entropy.tex line 437 (Conclusion) :: structure
ISSUE: The Conclusion is a single ~330-word paragraph that re-states essentially every headline number in the paper (two limits, 3.32-bit ceiling, ten-thousandths representation choice, facing 2.10/pocketing 3.32/adaptive 3.31, r=-0.83/rho=-0.91, pooled -0.89, cross-head, 0.877~0.888, 18.4pp, <1.5pp drift, single-corpus scope, TF upper bound, next steps). It is comprehensive but undifferentiated — every sentence carries equal weight, so the reader cannot tell what the one or two takeaways are.
FIX: Split into two short paragraphs: (1) the two genuine limits + the entropy structure + the ordering (the scientific result); (2) scope/caveats + registered next steps. Trim the re-quoted secondary numbers (0.877~0.888, 18.4pp, <1.5pp drift are tertiary in a conclusion) to keep the focus on the entropy-ordering finding.

[B-mod-47] (B-write) toolpath_entropy.tex line 68 (abstract) :: clarity
ISSUE: The abstract is information-dense but its second half reads as a run-on of dashes, semicolons, and em-dash asides. The sentence beginning 'A separate representation effect:' is a fragment (no main verb) standing as its own sentence, and the long sentence 'First, the Shannon source entropy ... the genuine precision floor.' stacks two em-dash clauses plus a final em-dash appositive, taxing the reader at the very first contact with the paper. The colon-then-fragment construction ('A separate representation effect: the V8 tokenizer's ...') is grammatically incomplete.
FIX: Convert the fragment to a full sentence: 'A separate representation effect arises: the V8 tokenizer's 0.001 grid discards the X/Y/Z ten-thousandths (R, on 0.0001, retains it), so that "structural zero" is not a source or sensor limit.' Consider breaking the first 'First, ...' sentence into two: state the digit-structure finding, then state the precision-floor conclusion separately.

[B-mod-48] (B-write) toolpath_entropy.tex line 68 (abstract) and recurring throughout :: consistency
ISSUE: The abstract says recovery accuracy 'is inversely related to entropy (r=-0.83; pooled -0.89)' without flagging that the pooled -0.89 is explicitly downgraded to 'illustrative only (n=6)' in the body (line 298). Presenting both side by side in the abstract with equal weight slightly undercuts the carefully-built distinction between the load-bearing n=30 statistic and the illustrative n=6 one.
FIX: In the abstract, either drop 'pooled -0.89' or tag it: '(r=-0.83 over 30 cells; pooled six-slot r=-0.89, illustrative).' This preserves the body's honesty hierarchy in the abstract.

[B-mod-49] (B-write) toolpath_entropy.tex line 81 (last sentence) and line 396 :: scope/framing
ISSUE: The 'this paper owns the entropy structure; the companion paper owns the decoder' phrasing (line 81) and 'division of labor with Paper A' (line 396) use ownership/territorial language that is unusual for a standalone journal submission. A reader of Entropy who has not seen Paper A will find the repeated 'owns' framing odd, and it foregrounds the two-paper split rather than this paper's self-contained contribution.
FIX: Replace 'this paper owns the entropy structure; the companion paper owns the decoder' with 'this paper analyzes the entropy structure; the decoder architecture is detailed in the companion work [paperA].' Soften 'Provenance and division of labor with Paper A' to 'Relationship to the companion decoder paper.'

[B-mod-50] (B-consistency) toolpath_entropy.tex line 92 (Contribution 4: 'Spearman $\rho=-0.91$') and line 296 lead statistic vs entropy_accuracy_correlation_fixed.json key best6_30 (pearson -0.660, spearman -0.514) :: claim/rigor
ISSUE: The lead n=30 statistic (r=-0.83, rho=-0.91) is correctly sourced from patched_numeric_analysis.json (corr_per_axis_slot_corrected: -0.831 / -0.910). However the audit directory ALSO contains entropy_accuracy_correlation_fixed.json with a 'best6_30' entry giving an unrelated -0.660 / -0.514 over a different 30-cell construction (head x op-class best-6). These are two distinct 'n=30' objects in the same audit folder. Nothing in the paper distinguishes them, so a reviewer auditing 'n=30' against entropy_accuracy_correlation_fixed.json (the file actually cited for the n=54 panel, Fig entropy_accuracy_all_heads) will find -0.66, not -0.83, and may flag a mismatch.
FIX: In the lead-result paragraph and Contribution 4, make the artifact provenance unambiguous: 'the per-(axis,slot) n=30 statistic is from audit/patched_numeric_analysis.json (corr_per_axis_slot_corrected); it is distinct from the per-(head,op-class) cell sets in audit/entropy_accuracy_correlation_fixed.json.' Consider renaming the best6_30 JSON key to avoid the 'n=30' collision.

[B-mod-51] (B-cam) toolpath_entropy.tex line 96 (Scope statement) and line 125 (Corpus); cf. per_head_entropy.tex Param-type vocab {X,Y,Z,R,F,T} :: consistency
ISSUE: The corpus contains tool-change commands (T2, 7 occurrences across the source files) and the parameter-type head vocabulary explicitly includes T (per_head_entropy.tex lists {X,Y,Z,R,F,T}), yet the scope statement and corpus description never mention tooling. The corpus uses a single tool (T2), so tool number is another deterministic (0-bit) field, analogous to constant S. Omitting T from the corpus inventory while it appears in the head vocabulary is an internal inconsistency a CAM-literate reviewer would catch, and it leaves the reader unsure whether tool variation is part of the source.
FIX: Add tool to the corpus inventory: 'a single cutting tool (T2) is used throughout, so tool number is deterministic (0 bits).' This both closes the X/Y/Z/R/F/T-vs-corpus gap and adds a second clean 0-bit endpoint.

[B-mod-52] (B-stats) toolpath_entropy.tex lines 148, 253-255, 388 (three-regime 2.10/3.32/3.31 thousandths); verified source_vs_tokenized_entropy.json three_regime_thousandths_X (face 2.10 n=324, adaptive 3.312 n=4179, pocket 3.316 n=1143) :: statistics
ISSUE: The 'three operation-class regimes' contribution reduces, at the thousandths, to a TWO-way split: facing 2.10 bits vs pocketing 3.32 approx adaptive 3.31 (the paper itself says 'a clean low/(high approx high) split' line 148 and 'not an intermediate, but at the ceiling' line 254). So the headline 'three-regime decomposition' (contribution 2, Sec three-regimes title) is statistically a two-cluster result (one low, two tied at the ceiling). The 3.312 vs 3.316 separation between adaptive and pocket is 0.004 bits — far inside sampling noise at these n and below the Miller-Madow correction magnitude — so they are statistically indistinguishable, not two regimes. Marketing this as 'three regimes' when the data support 'facing vs everything-else' is an overstatement of structure.
FIX: Either rename to a 'two-tier' or 'facing-vs-ceiling' decomposition, or keep 'three operation families' but state plainly that statistically the thousandths entropy supports a single low (facing) vs ceiling (pocket=adaptive, indistinguishable to within sampling/MM correction) split. Do not let 3.31 vs 3.32 read as a meaningful ordering.

[B-mod-53] (B-write) toolpath_entropy.tex lines 253, 388 (prior-draft artifact disclosures in body) :: scope/framing
ISSUE: Parenthetical 'the prior draft's X figure was an artifact' disclosures are scattered through the body: line 253 (the '96.5% of face X from four overruns' parenthetical inside the facing paragraph), line 249 (prior draft placed regimes at slot-5), line 262 (figure caption repeats the prior-draft slot-5 artifact), line 306 (bathtub 'not as the prior draft had it'), plus all of Section 7. These prior-draft mea culpas interrupt the forward narrative; a reader who never saw the prior draft does not need them in-line.
FIX: Consolidate all prior-draft/artifact disclosures into Section 7 (which already exists for exactly this purpose). In the body paragraphs (lines 249, 253, 262, 306), delete the parenthetical prior-draft references and let Section 7 own the historical narrative; the body should state the corrected result cleanly.

[B-mod-54] (B-cam) toolpath_entropy.tex lines 286-288 (Nyquist section) and abstract/contributions; 'G-code blocks' used 5x vs 'row-execution instances' 2x :: clarity
ISSUE: The 77.7% sub-Nyquist figure is computed over 21,541 'row-execution instances' (one per G-code line per recording, gcode_row_durations.json), but the paper presents it as '77.7% of G-code blocks occupy <=1 sample period' (abstract, contribution 3, Nyquist section, conclusion). 'G-code block' conventionally means a single program line in the SOURCE file (there are ~21,201 distinct rows across the 120-file corpus, but only ~6,051 in the nine authored toolpaths). The 77.7% is over execution instances across 120 aligned RECORDINGS, so the same source block is counted many times and weighted by how often it was run, not by its uniqueness. A reader could misread '77.7% of blocks' as a statement about distinct program lines. Additionally, median 'row executes in a single sample' conflates a per-instance duration with a per-block property.
FIX: State the unit explicitly and consistently: '77.7% of the 21,541 row-execution instances (each a G-code line as executed in one aligned recording) occupy <=1 sample period.' Add one clause noting this is execution-weighted (frequently-run short moves dominate), which is the operationally relevant quantity for a sensor stream, distinct from the fraction of distinct source lines.

[B-mod-55] (B-write) toolpath_entropy.tex lines 296, 298, 338, 437 and figure captions :: clarity
ISSUE: The distinction 'load-bearing (n=30) vs illustrative (n=6)' is correct and important, but it is restated verbatim a large number of times: lines 296, 298, 338 (figure 8 caption), 437 (conclusion), plus the 'bivariate normality cannot be checked at n=6' clause appears at both line 298 and line 338. This is over-explained.
FIX: State the n=6 illustrative caveat fully once (Section 6, line 298). In the figure 8 caption (line 338) compress to '(illustrative, n=6)'. In the conclusion (line 437), the parenthetical '(illustrative at n=6)' already suffices — remove the duplicate normality clause.

[B-mod-56] (B-write) toolpath_entropy.tex lines 85 and 88-94 :: structure
ISSUE: The 'salami-defense' paragraph (line 85) and the contribution-(5) text plus the entire Section 7 (float-epsilon) are three separate, lengthy defensive passages aimed at reviewers ('A reader may ask whether this is merely a relocated analysis...', 'we treat the absence of prior art as documented due diligence rather than asserted novelty'). These read as rebuttal-letter prose imported into the manuscript. For a final camera-ready they are defensible but verbose; the 'salami-defense' label itself is informal jargon that should not appear in a published paper.
FIX: Remove the literal phrase 'salami-defense' from the body and figure/table references (it appears at lines 85 and 180 as 'the salami-defense pivot'). Replace with neutral framing, e.g., 'The entropy structure is independent of the decoder:'. Shorten the line-85 paragraph to its load-bearing sentence (the ~0.07-bit decoder-free agreement).

[B-mod-57] (B-write) toolpath_entropy.tex lines 96, 423 (Scope statement & Limitations) :: structure
ISSUE: The Scope statement paragraph (line 96, end of Introduction) and the 'Single corpus, single platform, single material' limitation (line 423) say nearly the same thing in nearly the same words ('Whether the three-regime decomposition is platform-specific or a general property of CAM-generated toolpaths is the principal open question'; 'a second corpus through a different CAM postprocessor or a second machine is the registered next step'). The two passages are almost interchangeable.
FIX: Keep the full scope statement in the Introduction and reduce the Limitations 'single corpus' paragraph to the one detail it adds (n=5 executions, file-level CV), cross-referencing the Introduction scope statement rather than repeating the 'principal open question / second corpus' sentence verbatim.

[B-mod-58] (B-critic) toolpath_entropy.tex, Limitations 'Closed-vocabulary' (line 425) :: consistency
ISSUE: The caveat quotes '97.6%/99.0% verbatim test-line overlap,' which from audit/train_test_gcode_overlap.json is the per-ROW distinct-line / line-token overlap. But the headline recovery (and the entropy-accuracy confirmation) is computed on FULL WINDOWS, whose distinct-window overlap is only 27.3% (full_window_mean_frac_test_distinct_in_train) / 38.8% (full_window_mean_frac_test_rows_in_train) in the same JSON. The paper reports only the highest (most conservative-sounding) overlap unit and omits that the model's actual generalization unit overlaps far less.
FIX: Report both overlap units and state which matches the evaluation unit: e.g., 'test G-code lines overlap training at 97.6%/99.0% at the line level, while distinct evaluation windows overlap at only 27.3%/38.8%.' This is both more complete and more reassuring about generalization at the window level.

[B-mod-59] (B-critic) toolpath_entropy.tex, Limitations (lines 421-435) :: scope/framing
ISSUE: The limitations section omits two caveats a completeness reviewer would expect given the paper's own framing: (i) generalization of the entropy STRUCTURE across CAM postprocessors is named as the open question, but there is no caveat that the three-regime ordering (facing low / pocket~adaptive high) could be a postprocessor decimal-emission-policy artifact rather than toolpath-strategy intrinsic — the related-work itself notes emission decimals are postprocessor-governed; (ii) no caveat on the small number of physically distinct authored toolpaths (nine source files spanning three families) underlying all source-entropy figures — the per-op-class entropies derive from very few distinct programs, so 'facing X = 2.10 bits' is effectively three files' worth of coordinate geometry.
FIX: Add (i) a sentence that the regime ordering may reflect the single postprocessor's decimal-emission policy as much as toolpath strategy (testable by re-export, already the registered next step), and (ii) a sentence noting the source-entropy figures derive from nine authored programs (three per family), so per-family entropies are program-geometry-specific and the n in Table 1 counts coordinate tokens, not independent toolpaths.

[B-mod-60] (B-critic) toolpath_entropy.tex, Sec. 1/Sec. 2.1 and Limitations 'Marginal entropy' (lines 84, 110, 433) :: claim/rigor
ISSUE: The paper is admirably explicit that it measures marginal H(X), not the conditional H(X|obs) a tight Fano bound needs, and that it therefore reports an 'ordering' not a 'bound.' But it never quantifies or bounds the gap between the two — i.e., how much an informative channel could in principle lower H(X|obs) below H(X). Without any handle on this, the repeated disclaimer 'not a tight bound' is honest but unquantified, and a reviewer cannot tell whether the ordering is nearly the bound (channel weakly informative) or far from it.
FIX: Add one or two sentences (or a footnote) noting that the realized recovery accuracy itself lower-bounds the channel's informativeness: where measured accuracy approaches the prior (e.g., thousandths trough 0.42 against a 10-way prior near 0.10), H(X|obs) is close to H(X), so the ordering is near the bound there; where accuracy is high (Z tenths), the channel is informative and H(X|obs) << H(X). This turns the disclaimer into a quantitative statement at the two endpoints.

[B-mod-61] (B-critic) toolpath_entropy.tex, Sec. 3/4 'uniform ceiling' claims (lines 144-145, 254, 388-390) :: statistics
ISSUE: The central 'ceiling' interpretation rests on the fine digits being 'empirically indistinguishable from uniform over {0..9}' (3.31-3.32 vs log2(10)=3.32). This is argued purely by proximity of the entropy value; no goodness-of-fit / uniformity test (chi-square against uniform, or a confidence band) is reported, even though n is large (n=5,646 X / 5,521 Y). A 0.01-0.02 bit gap from the ceiling could still reflect a statistically detectable departure from uniform that a careful reader will ask about.
FIX: Report a one-line uniformity check on the load-bearing X/Y hundredths/thousandths digit distributions (e.g., chi-square against uniform with its p-value, or the max digit-probability deviation), so the 'indistinguishable from uniform' claim is backed by a test rather than by entropy-value proximity alone. The digit distributions are already in audit/raw_gcode_source_entropy.json.

[B-mod-62] (B-critic) toolpath_entropy.tex, Sec. 4 (line 286) and Limitations 'Field coverage' (line 429) :: scope/framing
ISSUE: The recoverability characterization is explicitly scoped to a 4 Hz envelope-extracted stack, and the paper states a higher-rate front end '(>=44.1 kHz contact microphone, >=1 kHz axis encoders) would lift the Nyquist floor but cannot lift the entropy ceiling.' This is a strong, central claim about generality of the limit, but it is asserted, not evidenced — the corpus has no higher-rate data, and the prior side-channel work it cites (Chhetri/Faezi at kHz-MHz) actually DID recover finer FDM geometry, suggesting a higher-rate channel might recover more of the fine-digit content than the 'cannot lift the entropy ceiling' claim implies (since their channel was informative about H(X|obs), not just H(X)).
FIX: Temper to a source-side statement: a higher-rate front end can lift the Nyquist floor and can in principle drive H(X|obs) below H(X) where the channel is informative, but it cannot change the marginal source entropy H(X) itself. As written, 'cannot lift the entropy ceiling' conflates the marginal-source ceiling (true, invariant) with the recoverability ceiling (channel-dependent), which is exactly the marginal-vs-conditional distinction the paper is careful about elsewhere.

[B-mod-63] (B-critic) toolpath_entropy.tex, Sec. 5 Nyquist (lines 280-284) :: methodology
ISSUE: The 4 Hz sub-Nyquist floor is presented as one of the 'two genuine limits,' but the link from '77.7% of blocks occupy <=1 sample period' to 'therefore unrecoverable' is asserted via the sampling theorem without engaging the obvious counter: the recoverer operates on overlapping WINDOWS of many samples, not on single per-block samples, and the categorical command head is recovered at 0.89 precisely because window-level/regime-level structure survives aliasing. The paper says this ('categorical/regime-level structure is recoverable while per-instruction numeric precision is not'), but does not quantify what fraction of NUMERIC content is carried by sub-sample blocks vs. the heavy tail, i.e., how much of the entropy ceiling the Nyquist floor actually accounts for independently. The two limits are claimed 'coupled but distinct' yet their relative contribution is never decomposed.
FIX: Add a sentence quantifying the coupling: e.g., what fraction of high-entropy (thousandths) coordinate mass falls in sub-sample blocks vs. the resolvable tail, using gcode_row_durations.json joined to op-class, so the Nyquist floor's independent contribution to unrecoverability is sized rather than asserted as a second limit of unknown weight.

[B-mod-64] (B-critic) toolpath_entropy.tex, Sec. 6 'cross-head' paragraph (line 300) and Fig. entropy_accuracy_all_heads caption (line 344) :: claim/rigor
ISSUE: The paper says the command head 'individually flat across operation classes (it has little entropy spread to track)' and 'essentially no slope.' But the artifact (entropy_accuracy_correlation_fixed.json, within_head.command) shows the within-command correlation is r=+0.384 — a POSITIVE slope, not a flat/zero one. 'No slope' understates a positive within-head relationship that, while not load-bearing and over few points, is the opposite sign of the cross-head story.
FIX: State the command within-head value honestly: 'the command head shows no negative slope (within-head r=+0.38, n=9, not significant), consistent with its being near-ceiling with little entropy spread.' Reporting the actual sign forecloses a reviewer noticing the positive correlation and questioning the 'flat' characterization.

[B-mod-65] (B-salami) toolpath_entropy.tex, Section 2.3 (Side-Channel), lines 116-119; venue framing :: related-work
ISSUE: The closest prior art (Chhetri et al. 2017, Faezi et al. 2020) computes mutual information / H(X|obs) between G-code and emissions — i.e. they instantiate the very conditional bound this paper declines to compute, in the adjacent AM domain. The paper differentiates on host domain (subtractive vs additive), object (source H(X) vs channel MI), and rate (4 Hz vs kHz). But for MDPI Entropy specifically, declining to compute the conditional/MI quantity while the closest analog DID compute it is the most exposed framing decision: a reviewer can argue the paper measures the easier quantity (marginal source entropy) and calls the harder one (channel MI, the actual recoverability bound) future work. The differentiation is stated but not defended as a contribution rather than a limitation.
FIX: Add one sentence in 2.3 (or the intro) positively framing why the source-entropy ORDERING is itself a contribution worth a paper even though MI is not computed: e.g. that the source-entropy structure is channel-agnostic and reusable across any sensing front-end, whereas Chhetri/Faezi's MI is tied to their specific high-rate channel — so B characterizes a property that bounds ALL channels from one side, then confirms attainability empirically. This converts the 'we measure the easier thing' optics into a deliberate channel-independence argument.

[B-mod-66] (B-salami) toolpath_entropy.tex, Section 6 (Nyquist), lines 286-288 :: claim/rigor
ISSUE: The 4 Hz sub-Nyquist floor is presented as one of the 'two genuine limits' and as co-equal anchor to the entropy ceiling, but the connection between '77.7% of blocks occupy <=1 sample period' and 'per-instruction numeric precision is unrecoverable' is asserted rather than demonstrated. The sampling theorem says a transient shorter than a sample period aliases, but the paper does not show that the SPECIFIC numeric content (a coordinate digit) is carried by a transient that the 4 Hz envelope cannot resolve — it could be that block-rate aliasing affects block boundaries while regime-level energy still carries some coordinate information. The leap from duration statistics to 'numeric precision is not recoverable' is the weakest link in the otherwise-tight two-limits argument, and it is the limit that is purely B's (not shared with A).
FIX: Either soften to 'consistent with' / 'the duration statistics make per-block numeric resolution implausible at 4 Hz' rather than stating it as a proven floor, OR add the missing step: a short argument (even back-of-envelope) relating the spectral content of a coordinate-carrying transient to the 2 Hz Nyquist frequency, so the floor is derived rather than analogized. Currently the entropy ceiling is rigorously measured while the Nyquist floor is argued by analogy — uneven for a paper that anchors on both.

[B-mod-67] (B-salami) toolpath_entropy.tex, Section 8 (float-epsilon), lines 379-388, and its prominence :: structure
ISSUE: An entire numbered section (8) plus a Limitations paragraph (421) plus repeated abstract/intro/conclusion mentions are devoted to walking back a now-resolved encoding bug from a prior draft. For a fresh submission to MDPI Entropy (where reviewers have NOT seen the prior draft), this much real estate spent disclosing-and-correcting an internal artifact reads as defensive and signals 'this number was once wrong.' The correction is honest and the recompute is sound, but a new reader does not need the full forensic history; they need the correct numbers and a brief assurance of provenance.
FIX: Compress Section 8 to a short methods subsection or an appendix: state that targets are encoded by round-to-scaled-integer (with the one-line rationale), that an earlier iterative-truncation encoding was corrected and the model retrained 5-fold, and that categorical accuracies drift <=1.5pp. Move the detailed 3.2750->[0,3,2,7,4,9] worked example and the 32.5% corpus-change-rate forensics to an appendix. Remove the prior-draft references from the abstract entirely (a first-time reader has no prior draft).

[B-mod-68] (B-salami) toolpath_entropy.tex, Section 8 line 304 / Section 9 / cross-references to paperA throughout (12 citations) :: scope/framing
ISSUE: Anti-salami posture is largely strong (decoder-free apparatus is genuinely the paper's spine), but several load-bearing quantitative claims are sourced ONLY by cross-citing Paper A with no in-B evidence: (i) the AR mode-collapse to ~0.10 (line 376) is asserted via cite{paperA_decoder} and is essential to the deployment caveat; (ii) the LOSO/leave-one-class-out floor that bounds open-vocabulary risk (line 425) is entirely Paper A; (iii) the tamper-detection operating points in the audit corollary (line 415) are Paper A. A reader who does not have Paper A in hand cannot evaluate these. For Entropy as a standalone venue, three of B's caveats are un-checkable without the companion.
FIX: For each Paper-A-only claim that is load-bearing for B's own conclusions (AR collapse value, LOSO floor, tamper operating points), add a one-line in-B number or a footnote with the specific figure so B is self-contained: e.g. state the AR digit accuracy (~0.10) with its source fold-mean here rather than only citing A, and give the LOSO floor value rather than only naming it. This keeps the division of labor while making B independently verifiable.

[B-mod-69] (B-critic) toolpath_entropy.tex, abstract (line 67), Sec. 6 (line 302), per_head_entropy.tex table, Sec. 7 (line 386) :: consistency
ISSUE: The patched decoder's command accuracy is reported as 0.891 in Table per_head_entropy and in the Sec. 7 patched-vs-released list ('command 0.891 vs 0.888'), but the abstract, the two-recoverers convergence claim (line 302), the seq2seq table, and the conclusion all use 0.888 (the RELEASED checkpoint value) for the decoder/'Main' command accuracy. The paper insists 'Every quantitative fine-digit result is computed on the patched-encoder retrain,' yet the headline command number used in the convergence claim and abstract is the released 0.888. The seq2seq table footnote does disclose the categorical entries are the released checkpoint, but the abstract and Sec. 6 do not flag that 0.888 != the 0.891 patched value tabulated elsewhere.
FIX: Add a half-sentence at the convergence claim and/or abstract clarifying the checkpoint provenance of the command number ('0.888 released / 0.891 patched, within fold variance'), or standardize on one value with a footnote. As written, a careful reader sees 0.888 and 0.891 for the same head and quantity without an adjacent reconciliation.

[B-mod-70] (B-salami) toolpath_entropy.tex, abstract (line 68) and Title (line 54) :: scope/framing
ISSUE: The title and abstract front-load three deliverables (quantization structure, operation-class ordering, 4 Hz floor) but the single most novel, venue-defining contribution for MDPI Entropy — that per-field source entropy ORDERS recoverability decoder-free — is buried mid-abstract behind the digit-slot mechanics. For an information-theory venue the headline should be the entropy-recoverability ordering itself, not the quantization plumbing. The abstract also spends two sentences on the tokenizer 'structural zero' representation artifact, which is a defensive correction (important for honesty) but reads as inside-baseball for a first-time Entropy reader and competes with the actual contribution.
FIX: Lead the abstract with the ordering claim and the two genuine limits (0-bit slots; 4 Hz Nyquist floor), then state the empirical confirmation (r=-0.83), then relegate the tokenizer source-vs-representation distinction to one compact clause. Consider tightening the title to foreground 'source-entropy-correlated recoverability ordering' rather than the colon-separated list of three sub-results.

============================================================
## MINOR (60)
============================================================

[B-min-1] (B-figtab) Embedded matplotlib titles across nearly all source-apparatus figures (bathtub overlay, slot5_by_op_class, scatter, overlay, both heatmaps, per_digit_position_curve, etc.) :: presentation
ISSUE: Most regenerated figures carry a long embedded title that substantially duplicates the LaTeX caption's first sentence (e.g. per_axis_bathtub_overlay's title 'Per-axis per-slot accuracy (patched): Z recovers through the hundredths; R is uniformly high; X/Y trough at the thousandths' is a full caption restated as a title). At subfigure widths (0.46-0.52\linewidth) these long titles render small/cramped and waste vertical space; in MDPI two-column proof they may shrink further.
FIX: Trim embedded titles to short labels (or remove them) and let the LaTeX caption carry the interpretation, especially for the four subfigure panels (bathtub a/b, entropy-accuracy a/b) where width is halved.

[B-min-2] (B-figtab) Figure ordering / forward reference: fig:entropy_accuracy_all_heads first cited at tex line 227 (Section 4) but defined at lines 342-347 (Section 6) :: structure
ISSUE: The per_head_opclass heatmap caption (Section 4, line 227) forward-references Figure entropy_accuracy_all_heads ('mirrored by measured recovery (Figure~\ref{fig:entropy_accuracy_all_heads})'), which is not introduced until ~115 lines later in Section 6. A reader following the cite jumps far ahead. Forward refs are legal but this one bridges two major sections.
FIX: Either soften the cite to '(see Section~\ref{sec:ceiling})' rather than the specific far-away figure, or note '(Figure 8, Section 6)' so the reader knows it is downstream.

[B-min-3] (B-figtab) Orphan figure labels: fig:digit_distributions (line 373), fig:entropy_accuracy (line 339), fig:per_digit_position_curve (line 315) :: figure/table
ISSUE: Three parent/subfigure \label{} are defined but never \ref'd. The text references the subfigures individually (fig:digit_value_histograms, fig:quantization_heatmap, fig:entropy_accuracy_overlay, fig:entropy_accuracy_scatter, fig:per_axis_bathtub_overlay) but never the umbrella float labels fig:digit_distributions and fig:entropy_accuracy, nor fig:per_digit_position_curve.
FIX: Either cite the parent floats once (e.g. 'Figure~\ref{fig:digit_distributions}' where both panels are introduced) or remove the unused labels to avoid dead cross-references.

[B-min-4] (B-figtab) Table 4 seq2seq_baseline (tab:seq2seq_baseline) Sequence row + caption :: figure/table
ISSUE: The Sequence row reports Baseline TF/AR and Main AR (0.026) but Main TF is '--' (deliberately, since TF sequence exact-match is treated as ill-defined). The Delta TF column for Sequence is also '--'. This is correct but the '--' cells in an otherwise fully-populated table invite a reader to think data is missing; the rationale lives only in the long footnote.
FIX: Add a superscript marker on the Sequence-row '--' cells pointing to the footnote clause ('TF sequence exact-match ill-defined under full teacher forcing'), so the blank is self-explaining at a glance.

[B-min-5] (B-figtab) Table per_digit_position (tab:per_digit_position) n column :: figure/table
ISSUE: Every slot lists the same n=79,345, which is the total numeric-position count pooled across all axes. This is correct as the pooled denominator, but because the table also discusses per-axis behavior (and the ten-thousandths recovery is dominated by X/Y/Z constants vs R), the single pooled n can mislead a reader into thinking each slot's accuracy is estimated on identical per-axis composition.
FIX: Add a one-line note that n is the pooled across-axis count and that the per-axis composition (esp. at the ten-thousandths, where X/Y/Z are tokenizer-constant and R is not) is decomposed in patched_numeric_analysis.json / Figure 7b.

[B-min-6] (B-figtab) figures/bits_recoverable_per_axis.pdf (F bar) and caption (tex line 354) vs scope statement (line 427) :: figure/table
ISSUE: The bit-budget figure includes an F bar (credited 4.5/4.9 bits) built from F source entropy, but F has only n=16 raw tokens (line 427 flags F as 'thin support... noisier than the X/Y/Z core') and F is explicitly omitted from Table 1 for that reason. The figure neither flags F's thin support nor notes that its 1.94-bit integer-slot entropies are high-variance, so the F bar reads as equally trustworthy as X/Y/Z/R.
FIX: Add a caption clause flagging F as thin-support (n=16) / noisier, or visually de-emphasize the F bar (hatch/footnote marker), consistent with its exclusion from Table 1 and the scope statement.

[B-min-7] (B-figtab) figures/entropy_vs_accuracy_overlay.pdf (embedded ceiling annotation) vs paper-wide use of 3.32 :: figure/table
ISSUE: The overlay figure's dotted ceiling line is annotated 'log2 10 ~ 3.31 ceiling' while the rest of the paper (abstract, conclusion, captions) consistently uses log2(10)~3.32. Trivial but a visible internal numeric inconsistency in the printed value of a constant.
FIX: Change the embedded annotation to 3.32 to match the manuscript's stated value of log2(10).

[B-min-8] (B-figtab) figures/per_axis_bathtub_overlay.pdf vs figures/slot5_by_op_class.pdf (color semantics) :: figure/table
ISSUE: Color coding for 'green' differs across figures: in per_axis_bathtub_overlay green = Z axis (and blue = R), while in slot5_by_op_class green = face family. Within a single figure each is consistent, but a reader scanning the figures may misread green across panels. (Different plot types, so low risk, but worth noting for a polished set.)
FIX: Where practical, reserve a consistent palette for recurring categories (e.g. always map the toolpath families face/pocket/adaptive to one fixed color triple, and the axes X/Y/Z/R/F to another), or note the legend explicitly in each caption.

[B-min-9] (B-figtab) figures/per_position_token_entropy.pdf caption (tex line 234) :: figure/table
ISSUE: Caption claims 'numeric-digit positions approach 5--6 bits', but the rendered curve peaks at roughly 5.4-5.5 bits and most numeric positions sit in the 3-4.5 range; the curve does not reach 6 bits. The '5--6' upper bound is slightly generous.
FIX: Soften to 'numeric-digit positions rise to ~5--5.5 bits' (or state the observed peak), to match what the figure shows.

[B-min-10] (B-it) toolpath_entropy.tex Sec. 1 (line 91) and Sec. 6: '77.7% of blocks occupy <=1 sample period --- one of the two genuine limits' :: claim/rigor
ISSUE: The 77.7% figure (verified, frac_le_1_sample_0p25s=0.7771) counts row-execution INSTANCES, but the inferential leap to 'genuine recoverability limit' needs the fraction of INFORMATION-bearing blocks that are sub-Nyquist. A rapid traverse (G0) or a dwell occupying <1 sample is a block, but the recoverability-relevant question is what fraction of the per-instruction NUMERIC content is carried by sub-sample blocks. If long cutting moves (the heavy tail, mean 2.90 samples) carry most of the coordinate variation while the 77.7% short blocks are mostly rapids/short links, the floor bites less than the headline 77.7% suggests.
FIX: Add the fraction of sub-sample blocks weighted by information content (e.g. by command type G0 vs G1/G2/G3, or by coordinate-token count), or at least note that 77.7% is an unweighted per-block fraction and that long cutting moves (heavy tail) are over-represented in coordinate content, so the per-block floor and the per-coordinate-bit floor differ. The gcode_row_durations.json per-file/per-command breakdown could support a one-line weighted figure.

[B-min-11] (B-it) toolpath_entropy.tex Sec. 3 (line 127): 'source entropy ~3.31 bits on X/Y/Z' for the ten-thousandths, and the recurring '~3.3-bit uniform ceiling' vs log2(10)=3.3219 :: presentation
ISSUE: The uniform ceiling H_max=log2(10)=3.3219 bits, but the X/Y/Z fine slots measured at 3.30-3.32 are sometimes described as 'at the ceiling' / 'empirically indistinguishable from uniform' (line 146) and elsewhere as '~3.3'. A measured 3.30 is 0.022 bits below uniform, i.e. ~99.3% of max; that is very close but the paper occasionally rounds 3.304/3.318 to a flat '3.31' and treats X thousandths (3.304) and Y thousandths (3.318) as identical 'ceiling'. The variation 3.20 (tenths) to 3.32 (hundredths/thousandths) is real structure (tenths are NOT at ceiling: 3.21 = 96.6% of max, with verified non-uniform top digits), yet the prose sometimes lumps tenths-through-thousandths as 'the plateau at the ceiling'.
FIX: Be consistent that the PLATEAU at/near the ceiling is the hundredths-thousandths (3.31-3.32, ~99.5% of log2 10), while the TENTHS (3.20-3.21) is a near-but-not-ceiling shoulder (the digit-value distributions show residual modal mass on 4/5/6, verified). Line 146 already says 'plateau from the hundredths onward' — ensure the abstract and Sec. 11 conclusion don't extend 'ceiling' down to the tenths, and state the ceiling band as 99%+ of log2(10) rather than 'identical to uniform' (it is statistically distinguishable from uniform given n>5000).

[B-min-12] (B-it) toolpath_entropy.tex Sec. 3 (line 131) and Sec. 4: the six-slot decimal decomposition is described as 'the Lloyd--Max view ... each slot is a uniform quantizer of one decade, and its entropy is the entropy of the quantization residual' :: claim/rigor
ISSUE: Per-decade digit slots are uniform SCALAR quantizers only in the trivial decimal sense; calling them 'Lloyd-Max' (which specifically means the MSE-OPTIMAL non-uniform quantizer for a given source density) is a misuse. A decimal digit slot is a fixed uniform quantizer, not the Lloyd-Max optimal quantizer for the coordinate density. Also 'entropy of the quantization residual' is imprecise: the slot entropy is the entropy of the digit at that decade, which equals the residual entropy only under specific independence/uniformity assumptions across decades.
FIX: Replace 'Lloyd-Max view' with 'uniform scalar (fixed-rate) quantization view', citing Lloyd-Max only as the optimal-quantizer reference and noting the decimal grid is the fixed uniform (non-optimal) quantizer the postprocessor uses. Reword 'entropy of the quantization residual' to 'entropy of the digit occupying that decade'. This is a citation-accuracy fix; the substantive analysis is unaffected.

[B-min-13] (B-it) toolpath_entropy.tex Sec. 4 (line 141): 'The released JSON stores the uncorrected plug-in entropies and these counts, not the corrected values' :: consistency
ISSUE: Good and honest disclosure, but it creates a reproducibility seam: every entropy number IN THE PAPER (Tables 2, 3, the per-head table, the figures) is the uncorrected plug-in value (verified: Table 2 X thousandths 3.30 = JSON 3.304 plug-in, not MM-corrected). The paper says MM is 'negligible' on load-bearing slots so the printed plug-in values stand, which is fine — but a reader comparing the paper's numbers to a recomputation that APPLIES the advertised MM correction (it is in the keywords and a named contribution) will find a systematic small discrepancy and may think the paper forgot to apply it.
FIX: Add a one-line global note (in the Sec. 4 bias paragraph or the data-availability statement) that ALL entropy values reported in tables/figures are uncorrected plug-in estimates and that MM corrections, where computed, are reported in-text as deltas only (because they are <=0.004 bits on the load-bearing slots); this prevents a reproducer from mistaking the deliberate plug-in reporting for an omission of the named MM contribution.

[B-min-14] (B-it) toolpath_entropy.tex Sec. 4 (line 145) and Table 2: 'Slot-0 (tens) entropy is 0.000 bits ... every coordinate satisfies |v|<10' — and units slot treated as low-entropy structure :: claim/rigor
ISSUE: The tens-slot 0-bit result is presented as a recoverability fact ('a digit the source never varies cannot be recovered wrongly') but it is purely an artifact of the small-part corpus (all |v|<10 in., i.e. <254 mm workpiece). This is the single clearest example of the H(X)=0 'genuine limit' being entirely corpus/geometry-specific, not an information-theoretic property of CNC toolpaths in general. The paper's scope statement covers single-corpus generality broadly, but the 0-bit tens slot is specifically a workpiece-size accident being counted toward the '0-bit slots are one of the two genuine limits' framing.
FIX: Where the 0-bit slots are elevated to 'one of the two genuine limits' (lines 81, 91, 294, 437), note that the specific 0-bit slots here (tens, and Z units) are workpiece-extent artifacts of this small-part corpus and would shift for larger parts; the GENERAL limit is 'any slot the source holds constant is trivially recovered and carries no information', instantiated here by the part geometry. This keeps the conceptual limit while disclosing its corpus-specific instantiation.

[B-min-15] (B-it) toolpath_entropy.tex Sec. 4 (line 146): 'empirically indistinguishable from uniform over {0,...,9}' :: statistics
ISSUE: 'Empirically indistinguishable from uniform' is a statistical claim that, at n=5646 (X), is testable and likely FALSE: a chi-square goodness-of-fit against uniform with the observed max-deviation-from-uniform of 0.018-0.041 (from the JSON dist arrays) at n>5000 will almost certainly reject uniformity (e.g. X thousandths has digit-5 at 0.141 vs uniform 0.10, a 4.1pp excess at n=5646 is highly significant). The honest statement is that entropy is ~99.5% of the uniform maximum, not that the distribution is indistinguishable from uniform.
FIX: Replace 'empirically indistinguishable from uniform' with 'within ~0.02 bits of the uniform maximum (>=99% of log2 10)'; avoid the indistinguishability claim, which a goodness-of-fit test at n>5000 would reject. The entropy-proximity statement is what the argument actually needs and is defensible.

[B-min-16] (B-it) toolpath_entropy.tex Sec. 4 (line 220) and Table per_head_entropy footnote: per-head MARGINAL command entropy 1.67 bits vs op-class-conditional mean 1.50 bits, 'the difference being exactly the between-op-class component that conditioning removes' :: claim/rigor
ISSUE: The statement 'the difference [1.67-1.50=0.17] being exactly the between-op-class component that conditioning removes' is the decomposition H(cmd) = H(cmd|opclass) + I(cmd;opclass), i.e. 0.17 bits = I(command; op-class). This is correct in form, but 'exactly' requires that the op-class-conditional mean be the n-weighted (not unweighted) average of H(cmd|opclass) for the identity to hold; the paper describes it as 'the average ... over the nine classes' (line 220), which if unweighted would NOT exactly equal the mutual-information decomposition. Worth confirming the average is n-weighted, else 'exactly' is too strong.
FIX: Confirm and state that the op-class-conditional mean is the n-WEIGHTED average over op-classes (= H(cmd|op-class) in the chain-rule sense); if it is unweighted, change 'exactly the between-op-class component' to 'approximately' and clarify the averaging. This makes the I(command;op-class)=0.17-bit interpretation rigorous.

[B-min-17] (B-cam) toolpath_entropy.tex Table 2 (tab:raw_vs_npz) R row and line 296 ('R differing by ~0.5 bits owing to its small windowed support') :: statistics
ISSUE: The R model-side entropies (Table 2: thousandths 2.327, ten-thousandths 1.989) differ from the source values (2.47, 2.51) by ~0.15-0.5 bits, attributed to 'small windowed support.' R has only n=431 raw tokens and the windowed model-side support is even thinner. Given R is the one axis carrying the paper's 'ten-thousandths is genuinely represented' claim, the thin support makes the R model-side entropy noisy, and the 0.5-bit gap is large relative to the <=0.07-bit agreement claimed for X/Y/Z. The paper acknowledges this in passing but the bit-budget figure (line 354, R 7.2/8.1) and the 'R recoverable across all six slots' claim lean on these thin-support numbers.
FIX: Report the R windowed support n explicitly in Table 2 / line 296 and add a one-line caveat that R fine-slot entropies are the least well-supported in the comparison, so the R bit-budget and all-slot-recoverable claim should be read as indicative. This is consistent with the careful Miller-Madow / small-n treatment given elsewhere.

[B-min-18] (B-consistency) toolpath_entropy.tex Table 3 (tab:raw_vs_npz) model-side column for X thousandths ($3.236$) vs patched_numeric_analysis.json corrected_per_axis_slot_entropy.X[4]=3.236; and Table 2 source X[4]=3.30 vs JSON 3.304 :: figure/table
ISSUE: Minor precision/rounding inconsistency between the two tables for the same axis-slot. Table 2 (source_vs_tokenized) reports X thousandths source = 3.30 (JSON 3.304) and Table 3 reports X thousandths source = 3.30 as well, but the in-text line 180 says 'source 3.30-3.32, model-side 3.24-3.31' — the model-side X value is actually 3.236 (rounds to 3.24), consistent, but the text earlier (line 146) says 'X/Y the 10^-1 through 10^-4 slots rise to 3.20-3.31 bits' while the JSON X hundredths source is 3.313 (rounds to 3.31) and thousandths 3.304 (3.30); the stated upper bound 3.31 is fine but the mix of 2- and 3-decimal rounding across Tables 2/3 and text invites confusion (e.g., Z hundredths is 3.23 in Table 2 but 3.234 source / 3.236 model in Table 3).
FIX: Standardize to two decimals everywhere for source entropies (Table 2 already does) or to three everywhere (Table 3 does for model-side). At minimum make the Z hundredths value consistent: Table 2 source Z[3]=3.23, Table 3 lists X model 3.236 — ensure no cell appears with different roundings in two tables.

[B-min-19] (B-it) toolpath_entropy.tex Table 3 (tab:raw_vs_npz, lines 192-195): X model-side thousandths 3.236 vs source 3.30; R model-side 2.327 vs source 2.47 :: consistency
ISSUE: The 'salami-defense pivot' claim (line 180, line 85) is that source and model-side agree 'to within ~0.07 bits at the hundredths-thousandths'. Verified: X thousandths source 3.304 vs model-side 3.236 = 0.068 bits (OK, ~0.07). But R thousandths source 2.465 vs model-side 2.327 = 0.138 bits, and the paper itself notes R 'differing by ~0.5 bits owing to its small windowed support' at line 296 (R slot ten-thousandths source 2.508 vs model 1.989 = 0.519). The '~0.07 bits' agreement headline (lines 85, 180) is true ONLY for X/Y/Z and explicitly excludes R, but the prose 'reproduces ... to within ~0.07 bits' (line 85) reads as a global claim before the X/Y/Z qualifier arrives.
FIX: In line 85 (salami-defense paragraph) add 'on the load-bearing X/Y/Z slots' to the ~0.07-bit agreement claim up front (the qualifier exists at line 180 but not at the first statement), so the global-sounding agreement number is scoped to the axes where it holds and the R discrepancy is not a later surprise.

[B-min-20] (B-consistency) toolpath_entropy.tex Table 5 (tab:per_digit_position) Entropy column vs patched_numeric_analysis.json corrected_pooled_slot_entropy :: figure/table
ISSUE: Table 5 pooled entropy column: slot0=0.007, slot1=1.690, slot2=3.079, slot3=3.301, slot4=3.287, slot5=0.311. JSON corrected_pooled_slot_entropy = [0.00720, 1.68981, 3.07891, 3.30075, 3.28740, 0.31135] — all match to 3 decimals (PASS). The accuracy column (1.0000,0.9223,0.7478,0.5465,0.4165,0.9931) matches pooled_per_slot_bathtub.patched_mean (PASS). Verification confirms Table 5 is faithful. Flagging only: slot4 entropy (3.287) is LOWER than slot3 (3.301) yet slot4 accuracy (0.4165) is the trough below slot3 (0.5465) — i.e. accuracy is NOT strictly monotone in this pooled entropy column (slot3 has higher entropy but higher accuracy than slot4). The text (line 306, line 146) calls the descent 'monotonic'; strictly, the pooled entropy itself is non-monotone (3.301 at hundredths > 3.287 at thousandths), so 'recovery falls monotonically as entropy rises' is slightly imprecise at the hundredths/thousandths pair.
FIX: Soften 'monotonically' to 'near-monotonically' for the pooled curve, or note that hundredths and thousandths are statistically tied in entropy (3.30 vs 3.29) so their accuracy ordering (0.55 vs 0.42) reflects sampling/axis-mix rather than an entropy gap. The cell-level r=-0.83 is the rigorous claim; the pooled monotonicity is illustrative.

[B-min-21] (B-it) toolpath_entropy.tex abstract (line 68) and Sec. 5 (line 296): abstract says cell-level 'r=-0.83', body reports Pearson r=-0.83 AND Spearman rho=-0.91; abstract omits which is which and gives 'pooled -0.89' :: presentation
ISSUE: The abstract reports 'r=-0.83; pooled -0.89' without specifying Pearson vs Spearman or n. Given the paper's (correct) emphasis that Pearson assumes bivariate normality uncheckable at n=6, the abstract's bare 'r' values invite the reader to take the Pearson coefficients as primary; the more robust Spearman (rho=-0.91 cell-level, -0.94 pooled) is arguably the safer headline for a monotone-ordering claim that explicitly disclaims linearity/normality.
FIX: In the abstract, label the coefficients (Pearson) and add n (n=30 cell-level, n=6 pooled), and consider leading with Spearman rho for the monotone-ordering framing since the paper's whole stance is 'ordering, not linear bound'. At minimum state n=6 for the pooled value so the -0.89 is not read as equally well-supported as the n=30 cell-level result.

[B-min-22] (B-consistency) toolpath_entropy.tex abstract (line 68: 'facing $2.10$~bits; pocketing/adaptive at the ceiling') and line 148/255/437; source three_regime_thousandths_X JSON (face 2.10, pocket 3.316, adaptive 3.312) :: claim/rigor
ISSUE: VERIFICATION PASS with a rounding nit: pocketing X thousandths source = 3.316 (JSON), reported as 3.32 (line 148, 437) — rounds correctly. Adaptive = 3.312, reported 3.31 — ok. Facing = 2.10 (JSON 2.10 exactly) — ok. The conclusion (line 437) writes 'pocketing 3.32 bits ~ adaptive HSM 3.31 bits' but the JSON has pocket (3.316) > adaptive (3.312), so pocket is marginally HIGHER than adaptive, consistent with '~'. Section three-regimes line 255 says 'pocketing ... X thousandths source entropy 3.32 bits, essentially tied with adaptive (3.31)'. All consistent. Only nit: line 148 lists the regimes as '2.10 (facing), 3.32 (pocketing), and 3.31 (adaptive)' — correct order — but the X-axis n for these (324/1143/4179) is never given alongside, while facing's low entropy rests on only n=324, the smallest support; worth surfacing.
FIX: Optionally annotate the three-regime thousandths values with their per-family n (face n=324, pocket n=1143, adaptive n=4179) at first statement (line 148), since the load-bearing low-entropy facing regime has the smallest support and a reader will want the count to gauge the Miller-Madow bias the paper otherwise emphasizes.

[B-min-23] (B-cam) toolpath_entropy.tex line 149 / Table 1 (R axis) / line 306 ('small arc-radius vocabulary') :: claim/rigor
ISSUE: R is repeatedly called a 'small repeated CAM arc-radius set / small discrete arc-radius vocabulary' but the physical origin of the dominant radii is never given. The arc-radius histogram is sharply dominated by R0.0128 (144 occurrences) and R0.0371 (90), with a long tail. R0.0128 inch = 0.325 mm is a tiny radius — almost certainly tool-radius-driven corner rounding / cutter-compensation fillets, not part geometry. Giving the physical mechanism (small radii = tool-radius corner fillets emitted by the CAM contour/pocket strategy; larger radii = part features) would make the 'low-entropy discrete vocabulary' claim mechanistic rather than merely descriptive, matching the mechanistic treatment given to facing repeats and Z depth.
FIX: Add a clause: 'the radius set is dominated by a few small values (R0.0128 in ≈ 0.33 mm, R0.0371 in) consistent with tool-radius-driven corner fillets, with a sparse tail of larger part-feature radii — hence the low, discrete R entropy.' This parallels the mechanistic explanations given for facing and Z.

[B-min-24] (B-consistency) toolpath_entropy.tex line 192-195 (Table 3 model-side R thousandths=2.327, ten-thousandths=1.989) vs line 195 in-text and Table 2 R source=2.47/2.51 :: figure/table
ISSUE: Table 3 caption says R 'retains it on both (~2.0-2.5 bits)' but the model-side R thousandths is 2.327 and ten-thousandths 1.989 (patched_numeric_analysis corrected_per_axis_slot_entropy.R = [...,1.969,2.327,1.989]). The source R thousandths is 2.47 and ten-thousandths 2.51. The line-296 text separately notes 'R differing by ~0.5 bits owing to its small windowed support.' The ~0.5-bit source-vs-model R gap is real and disclosed, but Table 3's blanket '~2.0-2.5 bits' obscures that the model-side R ten-thousandths (1.989) is actually BELOW the source thousandths and ~0.5 bit under source — i.e. the agreement claimed for X/Y/Z does NOT hold for R, which the table caption's loose range hides.
FIX: In Table 3 caption, replace '~2.0-2.5 bits' with the explicit source-vs-model R values and note the ~0.5-bit downward shift on R is the small-windowed-support effect flagged in Section ceiling, so R is not presented as a clean source=model agreement like X/Y/Z.

[B-min-25] (B-consistency) toolpath_entropy.tex line 201 (model-side sign 'H_X^sign=0.38, H_Y^sign=0.08, H_Z^sign=1.04') and Table 4 model-side column (0.38/0.08/1.04) vs digit_entropy.json per_axis sign :: claim/rigor
ISSUE: Could not directly dump digit_entropy.json per_axis.{X,Y,Z}.sign in this pass (large file), so the model-side sign values 0.38/0.08/1.04 are cited but unverified against digit_entropy.json here. Paper A's per_axis_recoverability table independently uses the same 0.38/0.08/1.04, so the two papers agree, but the underlying JSON key (per_axis.{X,Y,Z}.sign) should be confirmed to contain exactly these before submission.
FIX: Spot-check audit/digit_entropy.json key per_axis.X.sign / per_axis.Y.sign / per_axis.Z.sign equal 0.38/0.08/1.04 to confirm Table 4's model-side column and the cross-reference to Paper A. (Low risk given Paper A concordance, but the only cited number I could not directly re-derive.)

[B-min-26] (B-stats) toolpath_entropy.tex line 220 (5-fold mean +/- s.d. for per-head op-class entropy; command 1.468->1.498+/-0.032) :: statistics
ISSUE: The 5-fold-vs-fold-1 reconciliation reports the per-head op-class-conditional mean as 5-fold mean+/-s.d. (command 1.498+/-0.032) and concludes the fold-provenance question is 'cosmetic.' But the folds are NOT independent — file-level CV folds share the same 9 source toolpath files, so the across-fold s.d. underestimates true sampling variability of the entropy estimate (the +/-0.032 is a measure of fold-partition jitter, not of corpus-level uncertainty). Reporting it as a +/- s.d. invites reading it as an SE-like uncertainty. This is minor for entropy (the values are stable) but the same dependence caveat applies more importantly to the per-fold accuracy s.d. used throughout (e.g., 0.888+/-0.056 command), which similarly understates uncertainty because the 5 folds reuse the same 9 underlying parts.
FIX: Add a one-line caveat (in Sec raw-entropy or limitations) that the 5-fold s.d. reflects fold-partition variability over a shared 9-file corpus, not independent replication, so reported +/- s.d. values are lower bounds on true sampling uncertainty. This is the honest framing given single-corpus scope.

[B-min-27] (B-consistency) toolpath_entropy.tex line 234 (Fig per_position_token_entropy: 'numeric-digit positions approach $5$--$6$~bits, well below the vocabulary ceiling $H_{\max}=\log_2|V|\approx 11.24$~bits') :: consistency
ISSUE: H_max=11.24 bits implies |V|=2^11.24~2418, consistent with the token vocabulary K=2418 stated at line 129 and in tab:per_head_entropy (Token H_max=11.24). Consistent. However line 220's per-head token entropy is 6.97 bits (per_head_source_entropy_5fold token h_emp_mean=6.9735) while this figure says numeric-digit positions 'approach 5-6 bits' — these are different objects (per-position vs per-head pooled) and could read as inconsistent with the 6.97-bit token-head figure. Minor clarity risk.
FIX: Add a clause distinguishing per-POSITION token entropy (5-6 bits at numeric positions, Fig per_position) from the per-HEAD pooled token entropy (6.97 bits, Table per_head_entropy), so the two token-entropy numbers are not read as contradictory.

[B-min-28] (B-cam) toolpath_entropy.tex line 251 vs Table 1 / Table 2 (Z tenths value: 0.61 vs 0.64 vs 0.637) :: consistency
ISSUE: The Z tenths-slot source entropy is reported with three slightly different numbers: 0.61 bits (line 251, adaptive-only = 0.6101 verified), 0.64 bits (lines 152, 257, 349), and 0.637 in the underlying JSON (pooled = 0.6373). Both 0.61 (adaptive family) and 0.64 (pooled X/Y/Z) are individually defensible, but the text does not consistently say which population each refers to, so the same physical quantity appears to take two values within a few paragraphs.
FIX: Label each: '0.61 bits (adaptive-family Z tenths)' at line 251 and '0.64 bits (pooled Z tenths)' elsewhere, or standardize on the pooled 0.64 with a note that the adaptive family alone is 0.61. Ensures the reader does not read it as an inconsistency.

[B-min-29] (B-cam) toolpath_entropy.tex line 253 (facing X dominant coordinate values) :: figure/table
ISSUE: The specific value-level claim 'X=-0.1254 at 27%, X=3.5354 at 21%, X=3.5725 and X=-0.1625 at ~7% each --- together ~62% of facing X-coordinates' is correct (verified directly against the raw face*.gcode: 88/324=27.2%, 69/324=21.3%, 23/324=7.1%, 23/324=7.1%, sum 62.7%) but is NOT reproducible from any released audit JSON. The released raw_gcode_source_entropy.json stores only per-digit-slot distributions (per_op_family_slots), not value-level coordinate histograms, so a reader cannot regenerate these four percentages from the cited artifacts. The Data Availability statement points only to the slot-level JSONs.
FIX: Either (a) emit a small value-level histogram for facing X to an audit JSON (e.g. a 'per_op_family_top_values' key) and cite it, or (b) add a footnote stating these four percentages are computed directly from data_clean/face*.gcode and give the counts (88/69/23/23 of 324) so the claim is checkable from the released source files.

[B-min-30] (B-write) toolpath_entropy.tex line 286 :: presentation
ISSUE: The Nyquist section reports the same fact ('77.7% of blocks occupy <=1 sample period') that already appears in the abstract (line 68), contribution (3) (line 91), the Conclusion (line 437), and twice within Section 5 (lines 284 'most G-code blocks' and 286). The percentage cascade in line 286 ('77.7%... 83.1%<=0.5s... 86.3%<=1s') is fine, but the bare repetition of 77.7% across five locations is heavy.
FIX: Keep the full duration breakdown in Section 5. In the abstract and contribution list, the 77.7% is justified as the headline; in the Conclusion (line 437) consider 'aliases the majority of G-code blocks' rather than re-quoting 77.7% a fifth time.

[B-min-31] (B-consistency) toolpath_entropy.tex line 286 ('$77.7\%$') / line 91 / abstract / conclusion vs gcode_row_durations.json frac_le_1_sample=0.7771226962536558 :: claim/rigor
ISSUE: 77.7% is correct (0.77712 rounds to 77.7%). But line 286 also states '83.1% <= 0.5 s and 86.3% <= 1 s' citing the same JSON: JSON gives frac_le_2_samples_0p50s=0.8306 (83.1%, ok) and frac_le_4_samples_1p00s=0.8628 (86.3%, ok). However line 286 labels these as '<=0.5 s' and '<=1 s' — note 4 samples at 0.25 s = 1.0 s, correct; 2 samples = 0.5 s, correct. All consistent. The only nit: the text says 'median 1.0 sample, 0.25 s' and 'mean 2.90 samples' — JSON mean=2.8969 (rounds 2.90, ok) and duration_seconds median 0.25 (ok). No error, but p90 is reported as '7 samples' (JSON p90=7.0 samples = 1.75 s, and duration_seconds.p90=1.75 confirms). Consistent. This finding is a verification PASS; flagging only that the heavy-tail 'max 181 samples' (=45.25 s) is stated without the seconds equivalent, unlike the other figures.
FIX: Optionally add the seconds equivalent for max (181 samples = 45.25 s) to match the seconds annotations given for median/p90, for reader consistency. No numeric correction needed.

[B-min-32] (B-consistency) toolpath_entropy.tex line 286 ('mean $2.90$ samples') and line 437 conclusion / abstract '4 Hz sub-Nyquist floor: 77.7%' :: consistency
ISSUE: The Nyquist 77.7% figure is used as a top-line anchor in abstract, contributions (line 91), Section nyquist (line 286), and conclusion (line 437). All four occurrences agree on 77.7%. The phrasing varies: line 91 says '77.7% of blocks occupy <=1 sample period', abstract says '77.7% of G-code blocks occupy <=1 sample period', conclusion says 'aliases 77.7% of G-code blocks'. 'Block' vs 'row-execution instance' is used interchangeably; the JSON unit is 'n_row_execution_instances' (21,541), and one G-code block can have multiple row-execution instances across the 120 files. So '77.7% of blocks' slightly conflates per-instance fraction with per-unique-block fraction.
FIX: Use a single consistent term ('row-execution instances', as in line 286 and the JSON) in the abstract, contributions, and conclusion rather than 'blocks', or define once that 'block' here means a row-execution instance, so the 77.7% denominator (21,541 instances) is unambiguous.

[B-min-33] (B-write) toolpath_entropy.tex line 286 vs 288 :: clarity
ISSUE: In Section 5, line 286 says the median row 'executes in a single sample (median 1.0 sample, 0.25 s)' and separately states '77.7% occupy <=1 sample period.' Then line 288 says 'a transient shorter than one sample period aliases.' There is a subtle slip: a row occupying exactly 1 sample period is not 'shorter than one sample period.' The 77.7% figure is '<=1 sample' (i.e., includes exactly-1-sample rows), but the Nyquist argument strictly applies to durations < 1 sample. The boundary case is glossed.
FIX: Clarify the boundary: state that blocks at or below one sample period cannot be resolved as distinct transients at 4 Hz (the Nyquist criterion requires >=2 samples per transient, so even a 1-sample block is sub-Nyquist for resolving its internal structure). One added clause removes the apparent <= vs < mismatch.

[B-min-34] (B-stats) toolpath_entropy.tex line 302 / Table seq2seq_baseline; '0.877 baseline vs 0.888 decoder, well within fold variance' :: statistics
ISSUE: The convergence claim '0.877 vs 0.888, well within fold variance' is asserted without a paired test, despite both being 5-fold CV on the same splits (baseline 0.877+/-0.031, main 0.888+/-0.056). A paired comparison across the 5 shared folds is the right test and is cheap; 'well within fold variance' is an eyeball judgment. Given the architecture-invariance claim rests on this single number, a paired t or sign test (or a stated 'difference 0.011, 95% CI includes 0') would harden it. Note the main TF command s.d. (0.056) is nearly 2x the baseline's (0.031), itself worth a word.
FIX: Report a paired test or CI on the 0.011 TF-command difference across the 5 shared folds (e.g., paired t, or bootstrap CI on the per-fold delta). Replace 'well within fold variance' with the actual paired statistic.

[B-min-35] (B-stats) toolpath_entropy.tex line 306 / Table per_digit_position (pooled bathtub thousandths trough 0.4165) vs Z slot-4 0.2733 :: statistics
ISSUE: The pooled per-slot bathtub trough is reported as 0.42 at the thousandths, but the per-axis decomposition shows Z slot-4 at 0.2733, X 0.4296, Y 0.3758 — the pooled 0.42 is an n-weighted average dominated by X (n=5646) and Y (n=5521), with Z (n=1707) pulling it down. The pooled number is fine, but presenting 0.42 as 'the true precision floor' obscures that the actual per-axis floors range 0.27-0.43 and that Z is the worst at the thousandths despite being the best at coarser slots. This per-axis crossover (Z best at tenths/hundredths, worst at thousandths) is interesting and underexploited.
FIX: When stating the trough, give the range (0.27-0.43 across X/Y/Z) rather than only the pooled 0.42, and note Z's crossover (best coarse, worst fine) explicitly — it strengthens the cutting-depth-vs-fine-residual mechanism story.

[B-min-36] (B-stats) toolpath_entropy.tex line 349 + Table per_axis_recoverability caption; per-axis MAE F=1.115 in (table) vs Z 0.009 etc. :: statistics
ISSUE: Per-axis value MAE is reported as a point with std across folds (X 0.248+/-0.150, Y 0.205+/-0.075, F 1.115+/-0.346) but is computed only on samples where both predicted and true carry the axis, with no statement of n per axis. F's MAE std (0.346) is ~30% of its mean, and X's std (0.150) is 60% of its mean — these are very noisy estimates being used to support the 'coarse-resolution recoverer bounded at the uniform-noise ceiling' characterization. The MAE is also not in the same currency as the entropy/accuracy story (it is a regression error on a bucketized target), so its interpretation relative to the digit-slot ceiling is indirect.
FIX: Add per-axis n for the MAE column and note the high relative variance on X and F. Clarify that MAE on the bucketized numeric target is a coarse proxy and the digit-slot accuracy (Table per_digit_position) is the primary recoverability metric.

[B-min-37] (B-consistency) toolpath_entropy.tex line 384 ('$32.5\%$ of numeric positions ($25{,}765$ of $79{,}345$)') vs patched_numeric_analysis.json corpus_target_change_rate (fraction_changed=0.3247, positions=25765/79345) :: claim/rigor
ISSUE: 0.3247 rounds to 32.5% (ok). The text then says 'of which 92.8% (23,909) are exactly the terminal-zero 9->0 fixes.' JSON: terminal_zero_9to0_fixed=23909; 23909/25765 = 0.9280 (92.8%, ok). BUT the JSON also reports frac_terminal_zero_fixed=0.3013, i.e. 23909/79345=30.1% of ALL numeric positions, whereas the text's 92.8% is the fraction OF CHANGED positions. Both are correct but the paper only gives the 92.8%-of-changed figure; a reader might mis-apply it. Minor: also confirm '79,345 numeric positions' matches the per_digit_position table n=79,345 (it does).
FIX: No correction required; optionally note '92.8% of the changed positions (30.1% of all numeric positions)' to forestall a reader mis-reading 92.8% as a corpus-wide rate.

[B-min-38] (B-stats) toolpath_entropy.tex line 400 (k-spectrum within-methodology); verified k_spectrum_stats.json rm_anova :: statistics
ISSUE: The k-spectrum within-methodology claim cites the exact Friedman test (p=0.0018 for AR token) over the asymptotic chi2, correctly. But the same JSON also contains an RM-ANOVA (struct_token_acc F=3.42, df 4,16, p=0.033, partial eta^2=0.46) whose condition_means INCLUDE the K2418_designB matched control (0.469) alongside the four matched variants — i.e., the omnibus is run over 5 conditions, not the 4 matched variants the text describes ('Across the four matched variants ... the omnibus is significant on the ... Friedman test'). If the reported Friedman p=0.0018 is over 5 conditions (including the headline or the designB control) rather than the 4 matched variants, the test does not cleanly match the sentence. Worth verifying the omnibus was run on exactly the 4 matched variants the prose claims.
FIX: Confirm and state which conditions the cited omnibus p-value covers (the JSON lists 5 conditions including K2418_designB). If it is 5 conditions, adjust the prose ('across the matched family of variants') or rerun on the 4 the sentence describes; the monotone-in-K claim (0.405,0.415,0.429,0.470) is over 4 points and should be tested over those 4.

[B-min-39] (B-write) toolpath_entropy.tex line 415 (Audit corollary) and line 81 :: clarity
ISSUE: Section 9's opening sentence is long and abstract ('complementing cryptographic file-hash signing and controller attestation along an entropy axis'). The core insight — sensor audit is most useful exactly where high-entropy paths make it useless, so it is complementary to crypto — is good but takes the whole paragraph to land, and the phrase 'where humans are most blind' is rhetorical rather than precise.
FIX: Open Section 9 with the punchline: 'Sensor-side audit and cryptographic verification are complementary along the operation-class entropy axis.' Then give the two regimes (low-entropy: sensor-auditable; high-entropy: crypto-only). Replace 'where humans are most blind' with a precise phrase ('on predictable repetitive operations where a single substituted coordinate is statistically conspicuous').

[B-min-40] (B-cam) toolpath_entropy.tex line 415 (Audit-Leverage Corollary), 'drilling cycles' example :: claim/rigor
ISSUE: The audit corollary cites 'drilling cycles' as a low-coordinate-entropy operation class ideal for sensor-side audit, but the corpus contains no drilling operations (only facing, pocketing, adaptive HSM, and their feed/speed and damage variants; no G81/G83 canned drilling cycles appear — the G-code inventory is G0/G1/G2/G3/G53/G55/G17/G20/G90/G94). Using drilling as an exemplar is unsupported by this single corpus and slightly overreaches the 'scoped to this corpus' framing the paper otherwise maintains.
FIX: Replace 'drilling cycles' with an operation class actually present (e.g. 'facing-pass edge repeats') or explicitly mark drilling as a hypothetical extension: 'low-coordinate-entropy operations (here, facing-pass repeats; canned drilling cycles would be another, though absent from this corpus)'.

[B-min-41] (B-consistency) toolpath_entropy.tex line 425 ('$97.6\%$/$99.0\%$ verbatim test-line overlap') vs train_test_gcode_overlap.json :: claim/rigor
ISSUE: VERIFICATION: 97.6% = per_row_mean_frac_test_distinct_in_train 0.97566 (ok); 99.0% = per_row_mean_frac_test_rows_in_train 0.99040 (ok). These are the PER-ROW overlap figures. The JSON also has full_window figures that are very different (distinct_in_train=0.273, rows_in_train=0.388, but distinct_LINES_in_train=0.979 and line_tokens=0.991). Since this paper's recovery confirmation is on the FULL-WINDOW regime (per_digit_position n=79,345, full_window), citing the per-row 97.6/99.0 overlap to characterize the full-window closed-vocabulary confirmation is a basis mismatch: the full-window window-level overlap is only 27-39%, though the line-level overlap is 97.9/99.1%.
FIX: Specify which overlap basis the 97.6/99.0% figures use (per-row line overlap). Since the recovery results are full-window, consider citing the full-window line-level overlap (97.9%/99.1%, nearly identical) and explicitly note that window-level novelty is high (~61-73% of test windows are not verbatim in train) so the closed-vocabulary caveat is about LINE content, not whole windows.

[B-min-42] (B-write) toolpath_entropy.tex line 54 (title) :: presentation
ISSUE: The title is 30+ words with three colons-worth of subclauses ('Quantization Structure, an Operation-Class Ordering, and the 4 Hz Sub-Nyquist Floor'). It is descriptive but long even by MDPI standards, and 'an Operation-Class Ordering' is slightly opaque out of context.
FIX: Consider tightening to e.g. 'Source-Entropy-Correlated Recoverability of CNC Toolpath Coordinates under Low-Rate Sensing: Quantization Structure and a 4 Hz Sub-Nyquist Floor.' The operation-class regime decomposition is a finding, not necessarily title-level, and dropping it shortens the title without losing the two anchoring limits.

[B-min-43] (B-write) toolpath_entropy.tex line 68 (abstract) and lines 81, 89, 96, 125 — terminology :: consistency
ISSUE: The paper alternates between 'axis', 'field', and 'primary axis' for X/Y/Z/R/F. The 30-cell statistic is described as 'five fields x six decimal slots' (line 296) and 'per-(axis, slot)' elsewhere, but R and F are address fields, not motion axes. The phrase 'across 30 per-(axis, slot) cells' in the abstract (line 68) and 'primary axes' (line 349) vs 'five fields' (line 296) may confuse a reader counting 3 axes (X/Y/Z) vs 5 fields (X/Y/Z/R/F).
FIX: Standardize: use 'field' for the five address symbols (X/Y/Z/R/F), reserve 'axis' / 'primary axis' for X/Y/Z motion only, and make the 30-cell count read 'five fields x six slots' consistently (the abstract's 'per-(axis, slot)' should become 'per-(field, slot)').

[B-min-44] (B-stats) toolpath_entropy.tex line 68 abstract + line 146/152 ('plateau near the uniform ceiling'); Table source_vs_tokenized X s2=3.207, s3=3.313 :: claim/rigor
ISSUE: The repeated claim that fine digits 'plateau near the uniform ceiling across the hundredths-thousandths' and are 'empirically indistinguishable from uniform' (line 146) is supported (3.30-3.32 vs log2(10)=3.322), but no formal test of uniformity (e.g., chi-square goodness-of-fit, or the entropy gap 3.322-3.31=0.012 bits vs its sampling uncertainty) is reported. 'Empirically indistinguishable from uniform' is a statistical statement that warrants either a GoF test or an explicit 'within X bits of the ceiling, below the n-induced bias' framing. The tenths (X s2=3.21, Y s2=3.20) are NOT at the ceiling (~0.11 bits below) yet are swept into the 'plateau' language in places.
FIX: Either run a one-line chi-square uniformity test on the hundredths/thousandths digit distributions and report it, or soften to 'within ~0.02 bits of the log2(10) ceiling, below the plug-in/MM bias scale.' Keep the tenths explicitly OUT of the 'at the ceiling' set (they are ~0.1 bits below).

[B-min-45] (B-write) toolpath_entropy.tex line 84 (Introduction) :: clarity
ISSUE: The sentence 'a digit the source never varies cannot be recovered wrongly, and equally carries no information to recover' (line 84, also echoed at line 109 and 81) is a nice aphorism but phrased awkwardly ('recovered wrongly'). It appears in three slightly different wordings across the paper.
FIX: Pick one canonical phrasing, e.g., 'a digit the source never varies carries no information and is trivially recovered as its constant value,' and use it consistently; drop the 'recovered wrongly' double-negative.

[B-min-46] (B-stats) toolpath_entropy.tex line 92 contribution (4) and line 296: '30 well-supported cells' :: consistency
ISSUE: Line 296 calls the 30 cells 'well-supported,' but 13 of the 30 are degenerate zero-entropy constants (no support question, but also no information), and several (R, F slots) rest on thin windowed support (R ~0.5-bit entropy error, F n=16 raw / thin windowed). Calling all 30 'well-supported' overstates uniformity. Separately, the abstract (line 68) says 'across 30 per-(axis, slot) cells' and the contribution (4) line 92 says '30 per-(axis, slot) cells' without the cross-axis caveat that 5 of the axes include F (thin) and R (0.5-bit entropy gap).
FIX: Replace 'well-supported' with a precise statement: '30 cells = 5 fields x 6 slots; 13 are tokenizer-deterministic (0 bits) and enter trivially, 17 carry source variation; R/F cells rest on thinner windowed support.' Align abstract/contribution wording with this.

[B-min-47] (B-cam) toolpath_entropy.tex line 96 / line 125 ('inch mode (1 in = 25.4 mm)') and line 127 micron conversions :: presentation
ISSUE: Inch-mode is correctly identified (G20 confirmed in the source) and the micron conversions at line 127 (10^-2 in = 254 um, 10^-3 in = 25.4 um) are arithmetically correct. However, the paper gives metric equivalents only for the slot decades and not for the headline coordinate/feature scales, which would help the predominantly metric Entropy/Sensors readership gauge the physical regime. The dominant arc radius (0.0128 in = 0.33 mm) and cut depth (~0.12 in = 3 mm) and feedrates (185-1016 mm/min) are never given in metric.
FIX: Add parenthetical metric values at the first mention of the key physical scales: cut depth (~0.12 in ≈ 3 mm), smallest arc radius (R0.0128 in ≈ 0.33 mm), and feed range (185-1016 mm/min). Minor but improves accessibility for the journal's metric-default audience.

[B-min-48] (B-consistency) toolpath_entropy.tex line 96 / line 125 / line 427 ('no I/J', 'arc-interpolation (I/J) variation', 'no I/J interpolation') vs raw_gcode_source_entropy.json per_axis keys I and J present (n=0) :: consistency
ISSUE: The paper repeatedly states the corpus has no I/J. The audit JSON per_axis does contain I and J keys but with n=0 ('no_data'), confirming the claim. However the regex in line 139 is ([XYZIJKRF]) which captures I, J, K — yet the extraction counts report only X/Y/Z/R/F tokens and K is never mentioned. K is in the regex but absent from both the reported token counts and the no-support list (line 427 lists S, I, J but not K). Minor completeness gap: the regex captures an address (K) that is never accounted for.
FIX: Add K to the 'effectively zero support' list alongside S, I, J (or state K had n=0), so the regex's captured address set ([XYZIJKRF]) is fully reconciled with the reported per-axis token inventory.

[B-min-49] (B-write) toolpath_entropy.tex lines 300, 345 (cross-head vs within-head) :: clarity
ISSUE: The 'cross-head not within-head' point is well made but stated twice in close succession (Section 6 paragraph line 300 and figure 9 caption line 345), and the parenthetical 'Methodological note: an earlier head x op-class scatter silently fell back to a few hardcoded headline constants' (line 300) is again a prior-draft/debugging disclosure embedded mid-argument, with the same content in the figure 9 caption ('after the head x op-class alignment repair', 'supersedes the r=-0.55').
FIX: State the cross-head mechanism once in the body. Move the 'alignment bug repaired / supersedes r=-0.55' provenance note to a single location (either the figure caption or a methods note), not both.

[B-min-50] (B-write) toolpath_entropy.tex lines 36-39, 109, 119, 125, etc. (\etal, \eg, \ie usage) :: presentation
ISSUE: Mixed and sometimes inconsistent use of the custom \eg, \ie, \etal macros vs spelled-out forms, and several '\eg\ X3.3121' style examples. Also 'e.g.' appears un-macro'd in at least the figure-9 caption context. MDPI house style generally prefers 'e.g.,' / 'i.e.,' with a following comma; the macros (\textit{e.g.}) omit the comma.
FIX: Audit \eg/\ie usage for a trailing comma per MDPI style ('\eg,' typically wants a comma after) and ensure consistency. Low priority but worth a single pass before submission.

[B-min-51] (B-critic) toolpath_entropy.tex, Sec. 5 (line 286) teti2010 citation :: related-work
ISSUE: The Nyquist argument contrasts the 4 Hz stack against conventional >=10 kHz acoustic/vibration tool-condition-monitoring (teti2010), but does not engage why the released system was built at 4 Hz (cost/multi-modal envelope) versus what a minimally-higher rate would buy. The paper asserts a higher-rate front end 'would lift the Nyquist floor' without indicating the threshold rate needed to resolve the median block (median 1.0 sample at 4 Hz; p90 of 7 samples / 1.75 s implies a modest rate increase resolves most blocks).
FIX: Add the implied design target: to resolve the median block (0.25 s) as multiple samples, only ~16-40 Hz is needed (not kHz); state this so the Nyquist floor is framed as a low-cost-design consequence with a concrete remediation rate, not an inherent barrier — this also strengthens the 'genuine limit of THIS stack' framing over an absolute-limit reading.

[B-min-52] (B-critic) toolpath_entropy.tex, Sec. 6 (line 296) and Sec. 6 (line 298 pooled n=6) :: statistics
ISSUE: The lead n=30 cell-level relationship pools 5 fields x 6 slots but treats the 30 cells as independent for the Pearson/Spearman, when cells within an axis share the same coordinate distribution and the X/Y/Z 10^-4 constant cell (0 bits, ~1.0 accuracy) is a structurally-induced anchor point present on three axes. The paper flags the n=6 pooled case for non-checkable bivariate normality but does not note that the n=30 case has within-axis dependence and three near-identical 0-bit anchor points that inflate the correlation.
FIX: Add a sentence acknowledging the cell non-independence and reporting the correlation with the three tokenizer-constant 10^-4 anchor cells excluded (the relationship should remain negative on the genuinely-varying cells), so the r=-0.83 is shown not to be driven by the structural-zero anchors.

[B-min-53] (B-critic) toolpath_entropy.tex, Sec. 6 numeric-head correlation (line 300) :: claim/rigor
ISSUE: The text states the within-head numeric (sign+digit) relationship is 'strong and within-head (r approx -0.9).' The artifact value is numeric_18 pearson_r=-0.873 (spearman -0.917). Rounding -0.873 to '-0.9' is a mild upward rounding of the Pearson value; the Spearman is -0.92.
FIX: Quote the actual values: 'numeric heads alone (n=18): r=-0.87, Spearman rho=-0.92.' This is still 'strong' and avoids rounding -0.873 up to -0.9.

[B-min-54] (B-critic) toolpath_entropy.tex, Sec. 7 (line 380) and abstract/keywords :: presentation
ISSUE: The float-epsilon artifact section (Sec. 7) is given substantial prominence (a full section, repeated mentions in intro, limitations, conclusion, abstract) for what is ultimately a self-corrected internal bug that strengthened the result. For a fresh external reader of Paper B (who never saw the buggy draft), the heavy 'we fixed our own bug' framing reads as litigating a history they were not party to, and slightly undercuts confidence by foregrounding the error rather than the corrected result.
FIX: Consider compressing Sec. 7 to a methods subsection or appendix titled 'Numeric target encoding (round-to-scaled-integer)' that states the encoding used and verifies it is artifact-free, with the 'earlier draft' narrative reduced to a single sentence. The corrected result is the contribution; the prior-draft archaeology can be cited to the companion/repository for reviewers who want it.

[B-min-55] (B-salami) toolpath_entropy.tex, Section 4 line 220 (marginal 1.67 vs op-class-conditional 1.50 command entropy) and tables/per_head_entropy.tex (H_emp command 1.67) :: clarity
ISSUE: Two command-head entropies are quoted close together — marginal 1.67 bits (Table per_head_entropy, verified) and op-class-conditional mean 1.50 bits (line 220, verified as fold1 1.468 -> 5fold 1.498) — with the explanation that the difference is 'the between-op-class component that conditioning removes.' This is correct and well-explained, but the reader must hold two near-identical numbers (1.67, 1.50) for the same head and the distinction (marginal vs conditional-mean) is easy to lose, especially since 1.498 is also rounded to 1.50 here but the per-head table shows command H_emp = 1.67.
FIX: Add an explicit label at line 220: 'marginal H(command)=1.67 bits (Table 5) vs mean conditional H(command|op-class)=1.50 bits here; the 0.17-bit gap is the between-class mutual information I(command; op-class).' Naming it as the mutual information makes the relationship crisp and ties back to the information-theoretic frame the venue expects.

[B-min-56] (B-salami) toolpath_entropy.tex, Section 7 line 300 (parenthetical methodological note about hardcoded constants / alignment bug) :: presentation
ISSUE: A parenthetical confession — 'an earlier head x op-class scatter silently fell back to a few hardcoded headline constants broadcast as flat bands; the relationship reported here uses live per-cell accuracies after that alignment bug was repaired' — appears in the main body of Section 7, and a similar note appears in Figure entropy_accuracy_all_heads caption (line 345, r=-0.55 -> r=-0.68 after repair). Like the float-epsilon disclosures, these are internal-bug-history notes that a new reviewer does not need inline and that subtly undermine confidence in the released numbers.
FIX: Move both bug-repair notes (the hardcoded-constants alignment bug and the r=-0.55->-0.68 supersession) to the reproducibility appendix or a single 'analysis provenance' footnote. The main text should report the correct live-data correlations cleanly; the repair history belongs in supplementary provenance.

[B-min-57] (B-salami) toolpath_entropy.tex, Section 7 line 349, F-axis bit budget '4.5/4.9 bits' and per-axis MAE 1.115 :: consistency
ISSUE: F is simultaneously (a) credited with 4.5/4.9 recoverable bits in the bit-budget and included in the lead 30-cell correlation, and (b) flagged in Limitations (line 427) as thin support (n=16 raw / 159 windowed), 'not estimable' alongside S/I/J in spirit, and noisier. The bit-budget figure caption (line 354) presents F's 4.5/4.9 as a clean recovery success, but with n=16 raw tokens the F entropy estimate (sum 4.86 bits across slots 0-2) is itself fragile. There is no Miller-Madow note applied to F despite F being exactly the small-n case the bias correction targets.
FIX: Add a thin-support flag to F wherever its bit budget appears (Figure bits_recoverable_per_axis caption and line 349), and either apply/report the Miller-Madow correction for F's slots (n=16) or explicitly state F is reported descriptively and excluded from inferential claims. This aligns F's treatment between Section 7 (success) and Section 11 (caveat).

[B-min-58] (B-critic) toolpath_entropy.tex, Table 5 sign entropy (lines 198-210) :: figure/table
ISSUE: Table 5 frames raw per-axis sign as a 'one-bit source channel' and labels the model-side column as the '3-way (positive/negative/zero)' entropy, implying the raw column is 2-way. But audit/raw_gcode_source_entropy.json per_axis_sign also carries a nonzero zero_frac (X 0.0014, Y 0.0011, Z 0.0158), so the raw entropy is likewise computed over a pos/neg/zero alphabet. The raw vs model-side difference on X/Y is attributed solely to 'the windowed split re-weighting rare negative moves,' but part of the difference is the zero-state mass, which the prose does not mention.
FIX: Note that both raw and model-side sign entropies are computed over pos/neg/zero (the raw zero fractions are X 0.14%, Y 0.11%, Z 1.6%), so the columns are on the same alphabet; keep the re-weighting explanation but add the zero-state as a contributing factor on Z in particular.

[B-min-59] (B-salami) toolpath_entropy.tex, abstract line 68 ('a separate representation effect') and line 71 keywords :: clarity
ISSUE: The abstract sentence 'A separate representation effect: the V8 tokenizer's 0.001 grid discards the X/Y/Z ten-thousandths (R, on 0.0001, retains it), so that "structural zero" is not a source or sensor limit' is a sentence fragment (no main verb) and presumes the reader already knows what the 'structural zero' refers to, which has not been introduced. This is the densest, most jargon-loaded sentence in the abstract and will lose a general Entropy reader.
FIX: Rewrite as a full sentence and defer the 'structural zero' terminology: e.g. 'Separately, a representation effect: the tokenizer's 0.001 coordinate grid discards the X/Y/Z ten-thousandths digit, so its apparent zero entropy is a tokenization choice, not a property of the source or the sensing channel (the R axis, gridded at 0.0001, retains that digit).'

[B-min-60] (B-critic) toolpath_entropy.tex, tables directory vs paper \input set :: structure
ISSUE: Several generated tables exist in the tables/ directory but are never \input into the paper: per_axis_recoverability.tex, data_coverage.tex, anova.tex, bootstrap_ci.tex, ablations_summary.tex, lomo_*.tex, sensor_ablation.tex, nonumeric.tex, per_class_*.tex. A per-axis recoverability table and a data-coverage/corpus-inventory table in particular are content a completeness reviewer would expect (corpus sizes, per-axis bits recoverable in tabular form rather than only in the bit-budget figure). The corpus inventory currently lives only in prose (6,051 lines; 5,646 X / 5,521 Y / 1,707 Z / 431 R / 16 F tokens) and in operation_class_inventory.json.
FIX: Add a compact data-coverage table (per-op-class file counts, G-code rows, aligned samples from operation_class_inventory.json, plus the per-axis token counts) so the corpus is documented in one place; consider \input of per_axis_recoverability.tex as the tabular companion to Fig. bits_recoverable_per_axis. If these tables are intentionally excluded, they are dead artifacts that should be removed from the build directory to avoid confusion at submission.