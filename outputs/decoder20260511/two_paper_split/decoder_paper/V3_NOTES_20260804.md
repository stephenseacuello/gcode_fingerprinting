# decoder_paper_v3 — Single Self-Contained Edition (2026-08-04)

**Deliverable:** `latex/decoder_paper_v3.{tex,pdf}` — **55 pages, one document, no supplementary**, 0 LaTeX errors, 0 undefined references or citations, zero "Supplementary" mentions. Built from the rules-rewritten v2 (49pp) + the supplementary (62pp) via a mapped fold. v2 and the standalone supplementary are untouched; v3 is additive (plus two v3-only table variants: `tables/headline_5fold_v3.tex`, `tables/ablations_summary_v3.tex`).

## Architecture

Body (unchanged section structure, ~47pp) + five appendices in the same PDF:
- **Specification Addenda** (`app3:addenda`) — coverage-repair procedure detail, encoder representation-reuse bounds (leakage cap 0.572, reuse |Δ|≤0.016), full tokenizer grid (precisions, bucket formula, per-axis counts).
- **Companion Contributions and Data Availability** (kept from v2, compressed).
- **Reproducibility** (`app3:repro`) — code/data entry points + checkpoint map + SHA256s, four-seed robustness (0.8906 ± 0.0043 across seeds vs ±0.0558 across folds), hardware/compute (106 runs, ~57 GPU-h, ~17 kWh), pinned software versions + HGB sklearn-1.5.0 sensitivity, full hyperparameter ranges, inference cost (GPU + CPU).
- **Extended Protocols and Ablation Detail** (`app3:protocol`) — forward-pass pseudocode (Algorithm), calibration Brier/Murphy + fitted temperatures, LOSO per-board study + TOST/BF01 equivalence, nested 6×6 detail, single-fold pilots, FSM full spec + ablation statistics, configuration-choices record (pointer form).
- **Statistical Robustness Exhibits** (`app3:stats`) — bootstrap 95% CIs (prose form).

## What was folded where (from the 7-agent disposition map)

- 94 body pointers to the supplement: every one resolved — 15 exhibit slots, 18 appendix folds, 18 inline-fact insertions (e.g. the sign-head audit-script caveat, slot-5 per-class stratification, float-epsilon mechanism with corruption rates, LOMO channel-identity gate, beam-3/5 negative result — which also RESOLVES the v2-flagged "beam not evaluated" inconsistency), 19 redundant pointers → released-artifact citations.
- New in-body figures: per-operation-class breakdown; mode-collapse pairwise-overlap numbers (60–80% within-class vs ~20% across; 0.20→0.17 with metadata) carried in prose.
- Restored the G1↔G2 confusion cells (n=373/253, directional symmetry → local-pattern-ambiguity reading) after verification caught the sentence being consumed by an edit.

## Page-budget decisions (55 was binding; every choice reversible via the git-recoverable sources)

Information-preserving order was: dedup (rule 2.1) → typography/width → appendix-packing → **released-artifact citations** for exhibits whose every load-bearing number is stated in body prose/tables. Converted to artifact citations (filenames cited in-text): five_fold_spread, loco_per_holdout, per-digit bathtub curve, sensor overlay, numeric-error histograms, nested 6×6 heatmap, command/sign/digit confusion matrices, failure plate, tamper ROC + ROC-scatter, k_spectrum / seq2seq / non-neural / lomo_sensor / anova / lit-comparison grids (the positioning table became a cited prose comparison). The 5-fold spread caption's unique numbers (fold-1 0.776 vs 0.91–0.92) were moved into prose before the figure left.

**To restore any exhibit:** its verbatim source lives in `latex/supplementary_materials.tex` (S39 and original sections) — reinsert the figure/table block and revert the one citing sentence.

## Verification

- Citation set: v3 ⊇ v2 exactly; all new cites (IEC 62443 pair etc.) come from the supplement's bib usage; bibliography compiles with zero undefined citations.
- Numeric set: v3 ∪ v3-tables covers every v2 number; sole "losses" are S-subsection labels (S10.1, S13.4, S19.7…) — pointer artifacts, not data.
- Rules compliance: all new/edited prose written under the scientific-writing skill; claim-invariance enforced on every fold and compression (numbers, hedges, cites byte-preserved).

## Known flags carried forward

All v2 REWRITE_NOTES flags still apply. New: the drafting agents flagged (a) body "30–80 ms" vs appendix "30–60 ms" per-row GPU latency (CPU audit JSON cannot arbitrate; reconcile at proof); (b) a defective-looking round-encoding exponent in the supplement source (`10^{1/p_a}`), stated correctly in v3's addenda as `|v|/p_a`; (c) the supplement's stale "n=5 seeds" figure caption (v3 uses the artifact-verified four seeds).
