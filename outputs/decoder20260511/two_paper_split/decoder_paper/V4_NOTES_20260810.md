# decoder_paper_v4 — Figure-First Self-Contained Edition (2026-08-10)

**Deliverable:** `latex/decoder_paper_v4.{tex,pdf}` — **57 pages, single document, no supplementary, 16 figures** (four as two-panel pairs), 0 errors, 0 undefined references/citations, zero supplementary mentions. Response to the "too wordy / a picture is worth 1000 words" feedback: v4 pays the page budget with prose, not figures (the inverse of v3's trade). Plan and cut-ranking: `V4_PLAN.md`. v2/v3/supplementary untouched.

## What v4 contains

- **All of v3's integration** (the supplement folded in: appendices for reproducibility, protocols/FSM/LOSO/calibration detail, addenda, bootstrap CIs; 94 pointers resolved; per-cell grids as cited `audit/*.json` artifacts).
- **The figure set restored:** sensor/G-code overlay, encoder-memory PCA, five-fold spread + command confusion (pair), per-axis + per-digit bathtub (pair), per-op-class + LOCO per-holdout (pair), sensor-ablation + LOMO bars (pair, the redundancy contrast), mode-collapse heatmap, failure-mode plate, tamper ROC pair, sign/digit confusion pair, reliability diagram, nested 6×6 heatmap, system overview, algorithm.
- **~1,760 words of wordiness cut** (26,091 → 24,334) under the scientific-writing rules, claim-invariant: figure-narrated prose collapsed to claim + ref; caveats stated once at their canonical home; meta-commentary and reading instructions removed; qualifier chains reduced to one qualifier per claim; TF/AR invariance now explained once (was 4×), orthogonal-probe statistics once (was 3×), seed-robustness numbers once (body points to the appendix).
- **Abstract reduced 222 → ~200 words** (MDPI limit) claim-invariantly.
- **Two flagged v2 issues resolved:** the self-referencing "Label set scale" cross-ref now points to the released overlap artifact; the beam-search contradiction was already resolved in v3 (beam-3/5 negative result stated in body).

## Verification

Cite set identical to v3 (hence to v2 ∪ supplement); numeric set lossless vs v3 (the 3.31-bit source-entropy value, absent since v3, was restored to the bathtub caption); every restored caption verbatim except pair-merges, whose dropped caption clauses were verified body-stated first. Compile chain clean (pdflatex + bibtex ×3).

## Page accounting and the two levers to 55

The v3 55-page allowance was met by sacrificing figures; with all 16 figures in and the supplement integrated, the dense-packed floor is 57 pages (only the bibliography tail page is under-full). If the journal requires 55, the least-loss levers, in order: (1) pair the overlay with the PCA figure and drop the reliability diagram to an artifact citation (~1pp); (2) a fourth prose pass over the Discussion threat-model subsection (~0.5pp); (3) `\footnotesize` → smaller bibliography (format deviation). Each is a one-edit change; none was taken unilaterally because v4's instruction ranked figures above prose.

## Version map

- **v2** (49pp) + `supplementary_materials` (62pp incl. S39): the certified two-document release, rules-rewritten.
- **v3** (55pp): single document, minimal figures (page-budget-first).
- **v4** (57pp): single document, figure-first, wordiness-cut. **Recommended candidate for the co-author read.**

## Post-delivery review round (2026-08-10, inline — the 5-agent panel died on the spend limit 4×)

**Fixed during review:** (1) mdpi.cls's global `\item` wrapper forces negative itemsep and overlapped every `algorithmic` line — root-caused via minimal reproduction; fixed float-locally with `\let\item\olditem` in v4 AND v3 (bug present since the v2 era; text-based certification never rendered the float). Conclusion/RQ/contribution lists given the same fix for readable item separation. (2) Three floats had lost their in-text citations during pair-merges (numeric-tier pair, LOMO bars, reliability diagram) — references restored. (3) Abstract tightened further for the MDPI 200-word limit.

**Scripted consistency audit:** all headline quantities agree between prose, tables, and captions; tamper alert-budget arithmetic verified; abstract numbers are legitimate roundings of body values; retargeted appendix pointers deliver their promised content.

**Confirmed advisory findings (author decisions, not applied):** (1) sign-asymmetry attribution tension — Discussion suggests ±X/±Z "less separable" (board-layout hypothesis) while Results states the asymmetry is "prior-driven, not a sensor-separability ranking"; reconcile before submission. (2) Per-row GPU latency 30–80 ms (body) vs 30–60 ms (appendix); CPU-only audit JSON cannot arbitrate — rerun or pick the sourced figure. (3) Standing minor flags: "plant-IT storage tiers" uncited; grammar-mask "improves both calibration and exact-match accuracy" without in-section support; the ~20 evidence-category flags in REWRITE_NOTES_20260803.

**Limitation of this round:** single-reviewer + scripts, not the multi-agent adversarial panel. Rerun `v4-referee-panel` (script saved in session workflows) when the spend limit resets, before actual submission.

## Referee panel round 2 (2026-08-10) — ML-methods referee completed; others died on spend limit

Panel rerun on the fixed v4. Only the ML-methods referee finished (4 referees + all verifiers died on the spend limit); it returned **6 minor findings, 0 blockers/majors** (its "major_revision" verdict is inconsistent with its own findings — 6 minors is minor-revision quality). Each verified inline against the text:

**Applied (4 consistency fixes, claim-invariant):**
- F1: Sec 7.4 line 841 "sign/feed detectors below chance" → "at or below chance" (feed-rate LOCO is BA 0.50 = at chance; now matches line 670 and the table).
- F2: tamper table row label "Sign-flip (X+→X-)" → "Sign-flip (first signed axis)" (the mutation is on each sample's first signed axis, and the pooled result is Y-dominated per line 815; the X-only label was inaccurate).
- F3: Sec 7.4 line 839 dangling "(The earlier 10^-3 target...)" — a leftover reference to the two-document ancestor — rewritten self-containedly to "(An FPR=10^-3 target would extrapolate below the 1/483≈2.1×10^-3 empirical resolution.)"
- F4: Sec 3.5 line 251 cross-ref overstated the tamper table (only 3 of 5 attack classes have rows) → now "Detectability for the substitution and parameter-tampering classes is reported in Table...; classes (iii)-(v) are assessed qualitatively in Section 7.4."

**Flagged for author/security-co-author decision (not applied — content/citation framing):**
- F5 (threat model): the load-time attestation receipt (line 247, trusted elements) is generated controller-side while the controller/firmware are declared untrusted (line 249); a receipt from an untrusted controller needs its attestation root-of-trust (hardware TCB per zonouz2014plc) named as a trusted element, or restated as an external operational precondition. Genuine logical gap; small fix; security-co-author call.
- F6 (citation accuracy): the SOC alert budget "≤10^2 per analyst per shift implicit in NIST SP 800-61 Rev. 2" (line 837) — the word "implicit" concedes SP 800-61 states no such number, and the load-bearing "two-to-three-orders-of-magnitude" comparison rests on it. Either cite a source that states an alert-budget figure or present 10^2 as the authors' working assumption (keeping SP 800-61 for the incident-handling framing).

**Not yet reviewed:** CNC/manufacturing, security, editor/statistics referees, and the number-level consistency auditor never ran (spend limit). The panel should be completed before submission.

## Tier A + B applied (2026-08-10, deletion-only, per V4_FIX_PLAN.md)
All 7 done as cuts/swaps, no prose added (net ~−40 w). v4 recompiles 57pp, 0 err, 0 undefined, abstract now 200 w (MDPI-compliant).
- A1 abstract 214→200 (connective cuts; file-level CV term preserved). A2 latency body 30–80→30–60 ms (matches sourced appendix). A3 deleted uncited "plant-IT storage tiers" aside. A4 deleted uncited grammar-mask "improves calibration/exact-match" clause.
- B1 sign-asymmetry: deleted the speculative Discussion sentences; now points to the Results prior-driven account (Section per-axis) — tension resolved, no contradiction. B2 deleted the controller-side attestation-receipt parenthetical (removes the untrusted-controller trust contradiction; zonouz2014plc still cited 4×). B3 reframed the ≤10² SOC alert budget as "an assumed ... budget, with NIST SP 800-61 framing the incident-handling process" (no longer attributes the number to the source).
- Remaining: **Tier C** — the ~20 evidence-category flags for the Romesh co-author pass (batch, all deletions/softenings).

## Tier C applied (2026-08-10) — evidence-category flags, all resolved
12 edits, all deletions/softenings, no prose added. v4 recompiles 57pp, 0 err, 0 undefined refs/cites. Zero residual unjustified "substantially" in the paper.
- C1 Intro "substantially harder"→"harder". C2 deleted uncited AM "comparatively low-noise" clause. C3 "produce"→"can produce" (many-to-one, soften fact→possibility). C4 RelWork "achieved strong results on"→"have been used for". C5 deleted uncited "achieving high agreement with the source G-code". C6 EOS attachment disambiguated (target sequence, not loss). C7 dropped in-sentence-unjustified "substantially lower" (magnitude quantified in Results). C8 scheduled sampling "reduces"→"is intended to reduce". C9 aux-head "serve two purposes"→"have two intended roles". C11 Conclusion "Two decoder-side mechanisms"→"Two mechanisms" (aliasing is physical, not decoder-side). Plus 2 sweep-caught "substantially" drops (Sec target-mode, Sec label-scale).
- KEPT after adjudication (already honest): "factory-rated sensitivities" (category-B lab-procedure fact); "Replay is detectable indirectly (argued, not benchmarked)" (self-labels its evidence category).

**All review findings now resolved** (A1–A4, B1–B3, C1–C11 + sweep). Remaining pre-submission items are external: complete the 4 spend-limit-killed referees when the limit resets; Zenodo DOI + paperB_entropy bib-title sync at proof; human co-author flip-through. v4 is the recommended candidate.

## Referee panel round 3 (2026-08-11) — 4 remaining lenses completed (full run, 0 agent errors)
Recs: CNC minor, security **major**, editor minor, consistency minor. 5 confirmed serious (0 refuted), 9 minors.

**Applied (2 data/label fixes, claim-invariant):**
- Confusion-matrix caption (fig:confusion_command): "off-diagonal mass concentrates on the G2/G3 pair" → "G1/G2 pair". VERIFIED against the artifact figures/confusion_matrix_command_counts.png: G1↔G2 = 373+253 = 626 counts dominates; G2/G3 = 40+58 = 98. Body's counted sentence (G1→G2 n=373, G2→G1 n=253) was already correct; only the caption (added in v4 restoration) was wrong. Flagged independently by editor AND consistency reviewers.
- Grammar addendum: "p_F=1.0 (mm/min)" → "(in/min)". The per-axis table commits to inch mode ("the platform runs in inch mode"; F-row in inch/min) and coordinates are inch (3.8 in X fits the ~140 mm envelope); the tokenizer precision applies to inch-mode F. Other G2/G3 mentions left (line 320 "arcs valid with G2/G3"; power-analysis MDE grouping; noise-aug class list — all legitimate).

**FLAGGED for author / security-co-author (content / posture / data decisions — NOT applied):**
- SEC-major #1 (overclaimed coverage): lines 253 & 755 say the decoder "uniquely addresses ... selective skip/duplicate, and wrong-workpiece/wrong-fixturing," but line 781 says skip/duplicate "not directly addressed," line 845 "only command/axis/sign substitutions," line 92 triage "command, axis, motion-sign edits"; wrong-workpiece is not one of the 5 attack classes and is never evaluated (and is structurally undetectable by recover-and-compare — a wrong workpiece runs correct G-code). CONFIRMED. Fix = cut "skip/duplicate, and wrong-workpiece/fixturing" and change "uniquely addressing"→"primarily targeting runtime command/axis/sign substitution," OR hedge "(argued, not benchmarked)". Held because it narrows the paper's stated security contribution.
- SEC-major #2 (operating-point trust boundary): the primary security recommendation (item 1) uses the with-metadata detector, whose lift comes from window-index/source-file — controller-derived input, while the controller is the untrusted element. Paper acknowledges the tension but still leads with the compromised config. CONFIRMED. Fix = make sensors-only the default security/forensic operating point, or require metadata from the trusted acquisition path. Posture decision.
- CNC (150025 gloss): "150 mm/min feed" (line 391) — confirm whether 150 is a metric CAM-side setting (posts to ~5.9 in/min inch-mode) or an inch value; the p_F label is now inch/min, so the gloss should be reconciled (author knows the CAM workflow).
- CONSISTENCY: per-class command support counts disagree — body ~"G1 ~830/fold, G3 ~315, G0 ~65" vs power-analysis "G0 n=86, G1 n=1,095, G2 n=475, G3 n=395" (~19–32% apart for the same quantity). Need the true per-fold vs pooled basis to reconcile.

**9 minors (batch, for co-author pass):** VMC quantitative claim uncited (line 393, same class as the resolved plant-IT flag); M3/M5 declared COMMAND tokens but absent from the 6-way command head; n=91 per-file vs 120-file corpus (coverage-repair basis?); detectability-map framing; acronyms not in Abbreviations block (MDPI); tamper-table caption estimator rule vs LOCO rows; sec:disc-failure-mode label-on-a-redirect; body-vs-appendix calibration ECE mismatch; per-class support double-statement (= the consistency major above).

## Round-3 fixes + compression pass (2026-08-11)
**All round-3 findings applied.** Data/label: confusion caption G2/G3->G1/G2 (verified vs figures/confusion_matrix_command_counts.png: G1<->G2=626 dominates); p_F (mm/min)->(in/min) (inch-mode table authoritative). Security majors: overclaim narrowed at both sites to "runtime command/axis/sign substitution" / "primarily targeting" (matches scope at 92/781/845); operating-point (1) given sensors-only option inline (with-metadata depends on controller-derived input). Feed gloss: "150 mm/min CAM feed (posted to inch-mode G-code)". Labels: VMC claim->"expected to differ"; calibration appendix "cross-fold ECE"; tamper caption "In-distribution command-swap rows"; power-analysis "per-fold" basis. Added 6 acronyms to Abbreviations (HGB/IMU/MLP/SOC/TRL/VMC).
**Compression: 57->56pp, 24335->23485 words (-850, -3.5%), all 16 figures + all tables kept, 0 undefined, claim-invariant (cites & numbers preserved vs v3).** Method/Setup/Intro/threat-model tightened inline; Results/Discussion/tail via 3 parallel agents (each self-verified claim-invariant; Discussion agent died on spend limit after writing a valid -1.7% block); 7 single-figure widths trimmed; 4 verbose captions tightened; float spacing tightened.
**HARD FLOOR = 56pp.** No underfilled pages (layout fully packed, no slack). Three independent tightening agents + inline passes converged on ~3.5% as the claim-invariant prose ceiling — the paper is number/float/hedge-dense. Reaching 52 requires CONTENT removal: (a) relocate ~3-4 low-value tables (seq2seq, non-neural baseline, k-spectrum) to audit/*.json citations [numbers already in body prose; NOT images; ~2-3pp] — user declined this in the strategy question; or (b) drop/merge 2-3 figures [violates "no image cuts"]. Prose+figure-sizing alone cannot reach 52.
