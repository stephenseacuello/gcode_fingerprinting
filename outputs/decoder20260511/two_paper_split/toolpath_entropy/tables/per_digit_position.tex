% V8 FULL_WINDOW 5-FOLD --- per-digit-position breakdown.
% Regenerated by scripts/analysis/digit_entropy_analysis.py
%   (use --emit-table to rewrite this file).
% Note: scripts/analysis/diagnose_numeric_accuracy.py's --sweep-root
% default points at a non-existent per_row_5fold path; the accuracy
% column is in fact computed on the full_window predictions globbed
% at checkpoints/full_window_5fold/fold_*/*/results/predictions.npz
% (audit/numeric_accuracy_diagnosis.json), confirmed by matching the
% per-fold n_samples [132, 110, 108, 108, 93].
\begin{table}[ht]
\centering
\caption{Per-digit-position accuracy and source entropy on V8 full\_window (5-fold mean $\pm$ std), \textbf{patched encoder}. All accuracies and entropies are computed on the float-epsilon-corrected, 5-fold-retrained checkpoint with a consistent round-encoding (Section~\ref{sec:float-epsilon}; \texttt{audit/patched\_numeric\_analysis.json}). Slot 0 is the most-significant magnitude digit (tens place); slot 5 is the least-significant decimal (ten-thousandths). Entropy is Shannon entropy in bits of the empirical digit-value distribution across all supervised positions, pooled across axes (matching the accuracy denominator). The bathtub-shaped accuracy curve tracks per-slot source entropy: the trough is at the \emph{thousandths} ($10^{-3}$, $0.42$), the finest digit the corpus varies and the true precision floor; the ten-thousandths ($10^{-4}$) recovers to $0.99$ because the $0.001$ V8 tokenizer grid discards the near-uniform ($\sim\!3.31$-bit) source digit, making the tokenized target a structural zero, not because the fine digit is recovered. See Figure~\ref{fig:entropy_accuracy_overlay} for the overlay and Figure~\ref{fig:digit_value_histograms} for the per-axis decomposition.}
\label{tab:per_digit_position}
\begin{tabular}{c l r r r}
\toprule
Slot & Place & Accuracy (5-fold) & Entropy (bits) & $n$ \\
\midrule
0 & $10^{1}$ (tens) & $1.0000 \pm 0.0001$ & $0.007$ & $79,345$ \\
1 & $10^{0}$ (units) & $0.9223 \pm 0.0180$ & $1.690$ & $79,345$ \\
2 & $10^{-1}$ (tenths) & $0.7478 \pm 0.0253$ & $3.079$ & $79,345$ \\
3 & $10^{-2}$ (hundredths) & $0.5465 \pm 0.0269$ & $3.301$ & $79,345$ \\
4 & $10^{-3}$ (thousandths) & $0.4165 \pm 0.0239$ & $3.287$ & $79,345$ \\
5 & $10^{-4}$ (ten-thousandths) & $0.9931 \pm 0.0067$ & $0.311$ & $79,345$ \\
\bottomrule
\end{tabular}
\par\smallskip
\footnotesize\textit{The reported $n=79{,}345$ is the pooled across-axis numeric-position count (identical per slot because every axis contributes a digit at every slot); the per-axis composition differs by slot --- in particular at the ten-thousandths, where X/Y/Z are tokenizer-constant and R is not. Per-axis decomposition is in \texttt{audit/patched\_numeric\_analysis.json}. The thousandths is the true information-theoretic precision floor (high-entropy quasi-uniform); the X/Y/Z ten-thousandths is a tokenizer-induced structural zero (the $0.001$ grid discards the near-uniform source digit), while R on its $0.0001$ grid retains it. The pooled slot-5 entropy ($0.311$~bits) is therefore nonzero only because the across-axis pool mixes the X/Y/Z tokenizer-zeroed ten-thousandths ($0.00$~bits) with R's retained $0.0001$ digit ($\sim\!2.0$~bits tokenized); on X/Y/Z alone slot-5 is $0.00$~bits (Table~\ref{tab:source_vs_tokenized}).}
\end{table}
